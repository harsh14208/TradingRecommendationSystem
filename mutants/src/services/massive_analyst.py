"""
Massive Partners — Analyst Intelligence

Three endpoints merged into one service:
  1. Bulls Bears Say    — structured bull/bear thesis per ticker
  2. Analyst Ratings   — consensus + price target from historical ratings
  3. Corporate Guidance — EPS/revenue guidance raise/cut signals

Signal scoring:
  Bulls Bears Say:
    bull_count > bear_count → +3 pts (max +6 if ratio ≥ 3:1)
    bear_count > bull_count → −3 pts (max −6)
  Analyst Ratings (consensus):
    Strong Buy consensus  → +6 pts
    Buy consensus         → +4 pts
    Hold consensus        → 0 pts
    Sell / Strong Sell    → −4 / −6 pts
  Corporate Guidance:
    EPS guidance raise    → +8 pts (strong catalyst)
    EPS guidance cut      → −10 pts
    Revenue guidance flat → 0 pts

Per-ticker cache: 6 hours (quarterly fundamentals don't move faster).
"""

import asyncio
import logging
import os
import time

import aiohttp

log = logging.getLogger("signal.trade.massive_analyst")

_cache: dict[str, dict] = {}
_TTL = 3600  # 1 hour — analyst sentiment can shift intraday on news

_BASE = "https://api.polygon.io"

import ssl as _ssl

import certifi as _certifi
from services.http_client import shared_session

_SSL_CTX = _ssl.create_default_context(cafile=_certifi.where())


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__fetch__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fetch__mutmut)
async def _fetch(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_orig(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_1(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            None, params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_2(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=None, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_3(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=None, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_4(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=None
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_5(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_6(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_7(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_8(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_9(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=None)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_10(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=9)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_11(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_12(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 201:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_13(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = None
            results = data.get("results") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_14(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = None
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_15(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") and []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_16(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get(None) or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_17(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("XXresultsXX") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_18(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("RESULTS") or []
            return results[0] if results else {}
    except Exception:
        return {}


async def x__fetch__mutmut_19(session: aiohttp.ClientSession, endpoint: str, params: dict) -> dict:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return {}
            data = await resp.json()
            results = data.get("results") or []
            return results[1] if results else {}
    except Exception:
        return {}

mutants_x__fetch__mutmut['_mutmut_orig'] = x__fetch__mutmut_orig # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_1'] = x__fetch__mutmut_1 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_2'] = x__fetch__mutmut_2 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_3'] = x__fetch__mutmut_3 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_4'] = x__fetch__mutmut_4 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_5'] = x__fetch__mutmut_5 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_6'] = x__fetch__mutmut_6 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_7'] = x__fetch__mutmut_7 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_8'] = x__fetch__mutmut_8 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_9'] = x__fetch__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_10'] = x__fetch__mutmut_10 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_11'] = x__fetch__mutmut_11 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_12'] = x__fetch__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_13'] = x__fetch__mutmut_13 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_14'] = x__fetch__mutmut_14 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_15'] = x__fetch__mutmut_15 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_16'] = x__fetch__mutmut_16 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_17'] = x__fetch__mutmut_17 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_18'] = x__fetch__mutmut_18 # type: ignore # mutmut generated
mutants_x__fetch__mutmut['x__fetch__mutmut_19'] = x__fetch__mutmut_19 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fetch_list__mutmut)
async def _fetch_list(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_orig(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_1(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            None, params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_2(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=None, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_3(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=None, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_4(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=None
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_5(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_6(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_7(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_8(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_9(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=None)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_10(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=9)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_11(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 200:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_12(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 201:
                return []
            data = await resp.json()
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_13(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = None
            return data.get("results") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_14(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("results") and []
    except Exception:
        return []


async def x__fetch_list__mutmut_15(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get(None) or []
    except Exception:
        return []


async def x__fetch_list__mutmut_16(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("XXresultsXX") or []
    except Exception:
        return []


async def x__fetch_list__mutmut_17(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 200:
                return []
            data = await resp.json()
            return data.get("RESULTS") or []
    except Exception:
        return []

mutants_x__fetch_list__mutmut['_mutmut_orig'] = x__fetch_list__mutmut_orig # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_1'] = x__fetch_list__mutmut_1 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_2'] = x__fetch_list__mutmut_2 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_3'] = x__fetch_list__mutmut_3 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_4'] = x__fetch_list__mutmut_4 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_5'] = x__fetch_list__mutmut_5 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_6'] = x__fetch_list__mutmut_6 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_7'] = x__fetch_list__mutmut_7 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_8'] = x__fetch_list__mutmut_8 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_9'] = x__fetch_list__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_10'] = x__fetch_list__mutmut_10 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_11'] = x__fetch_list__mutmut_11 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_12'] = x__fetch_list__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_13'] = x__fetch_list__mutmut_13 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_14'] = x__fetch_list__mutmut_14 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_15'] = x__fetch_list__mutmut_15 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_16'] = x__fetch_list__mutmut_16 # type: ignore # mutmut generated
mutants_x__fetch_list__mutmut['x__fetch_list__mutmut_17'] = x__fetch_list__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_analyst_intelligence__mutmut)
async def get_analyst_intelligence(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_orig(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_1(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = None
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_2(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache or now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_3(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker not in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_4(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now + _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_5(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["XXtsXX"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_6(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["TS"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_7(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] <= _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_8(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["XXdataXX"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_9(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["DATA"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_10(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = None
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_11(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv(None)
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_12(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("XXMASSIVE_API_KEYXX")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_13(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("massive_api_key")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_14(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_15(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = None

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_16(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"XXapiKeyXX": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_17(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apikey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_18(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"APIKEY": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_19(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "XXtickerXX": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_20(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "TICKER": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_21(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = None

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_22(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            None,
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_23(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            None,
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_24(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            None,
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_25(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            None,
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_26(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=None,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_27(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_28(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_29(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_30(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_31(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_32(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(None, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_33(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, None, {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_34(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", None),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_35(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch("partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_36(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_37(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", ),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_38(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "XXpartners/bulls_bears_sayXX", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_39(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "PARTNERS/BULLS_BEARS_SAY", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_40(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "XXlimitXX": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_41(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "LIMIT": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_42(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 2}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_43(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(None, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_44(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, None, base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_45(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", None),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_46(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch("partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_47(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_48(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", ),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_49(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "XXpartners/consensus_ratingsXX", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_50(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "PARTNERS/CONSENSUS_RATINGS", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_51(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(None, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_52(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, None, {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_53(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", None),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_54(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list("partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_55(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_56(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", ),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_57(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "XXpartners/corporate_guidanceXX", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_58(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "PARTNERS/CORPORATE_GUIDANCE", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_59(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "XXlimitXX": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_60(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "LIMIT": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_61(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 4}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_62(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(None, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_63(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, None, {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_64(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", None),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_65(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list("partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_66(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_67(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", ),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_68(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "XXpartners/analyst_ratingsXX", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_69(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "PARTNERS/ANALYST_RATINGS", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_70(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "XXlimitXX": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_71(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "LIMIT": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_72(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 11}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_73(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=False,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_74(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = None
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_75(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = None
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_76(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = None
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_77(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = None

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_78(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = None
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_79(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(None)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_80(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") and 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_81(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") and bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_82(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get(None) or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_83(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("XXbull_countXX") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_84(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("BULL_COUNT") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_85(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get(None) or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_86(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("XXbullsXX") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_87(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("BULLS") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_88(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 1)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_89(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = None
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_90(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(None)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_91(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") and 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_92(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") and bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_93(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get(None) or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_94(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("XXbear_countXX") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_95(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("BEAR_COUNT") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_96(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get(None) or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_97(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("XXbearsXX") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_98(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("BEARS") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_99(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 1)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_100(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = None
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_101(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") and ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_102(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") and bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_103(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get(None) or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_104(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("XXbull_summaryXX") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_105(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("BULL_SUMMARY") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_106(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get(None) or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_107(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("XXbull_caseXX") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_108(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("BULL_CASE") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_109(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or "XXXX"
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_110(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = None

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_111(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") and ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_112(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") and bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_113(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get(None) or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_114(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("XXbear_summaryXX") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_115(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("BEAR_SUMMARY") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_116(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get(None) or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_117(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("XXbear_caseXX") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_118(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("BEAR_CASE") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_119(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or "XXXX"

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_120(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = None
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_121(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts * bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_122(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts >= 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_123(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 1 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_124(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts >= 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_125(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 1 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_126(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 2)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_127(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts >= bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_128(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = None
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_129(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(None, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_130(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, None)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_131(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_132(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, )
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_133(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(7.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_134(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 - (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_135(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 4.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_136(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) / 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_137(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio + 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_138(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 2) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_139(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 1.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_140(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = None
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_141(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "XXbullishXX"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_142(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "BULLISH"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_143(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts >= bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_144(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = None
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_145(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts * bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_146(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts >= 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_147(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 1 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_148(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = None
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_149(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = +min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_150(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(None, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_151(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, None)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_152(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_153(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, )
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_154(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(7.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_155(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 - (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_156(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 4.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_157(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) / 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_158(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio + 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_159(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 2) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_160(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 1.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_161(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = None
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_162(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "XXbearishXX"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_163(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "BEARISH"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_164(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = None

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_165(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 1.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_166(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "XXneutralXX"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_167(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "NEUTRAL"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_168(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = None
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_169(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").upper()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_170(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") and "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_171(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") and consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_172(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get(None) or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_173(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("XXconsensusXX") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_174(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("CONSENSUS") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_175(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get(None) or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_176(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("XXrecommendationXX") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_177(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("RECOMMENDATION") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_178(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "XXholdXX").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_179(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "HOLD").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_180(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = None
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_181(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") and consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_182(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get(None) or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_183(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("XXtarget_price_averageXX") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_184(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("TARGET_PRICE_AVERAGE") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_185(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get(None)
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_186(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("XXtarget_meanXX")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_187(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("TARGET_MEAN")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_188(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = None
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_189(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(None)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_190(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") and 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_191(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") and consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_192(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get(None) or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_193(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("XXanalyst_countXX") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_194(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("ANALYST_COUNT") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_195(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get(None) or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_196(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("XXnum_analystsXX") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_197(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("NUM_ANALYSTS") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_198(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 1)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_199(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = None
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_200(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "XXstrong_buyXX": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_201(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "STRONG_BUY": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_202(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 7,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_203(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "XXbuyXX": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_204(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "BUY": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_205(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 5,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_206(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "XXoverweightXX": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_207(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "OVERWEIGHT": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_208(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 4,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_209(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "XXholdXX": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_210(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "HOLD": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_211(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 1,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_212(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "XXunderweightXX": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_213(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "UNDERWEIGHT": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_214(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": +3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_215(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -4,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_216(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "XXsellXX": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_217(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "SELL": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_218(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": +4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_219(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -5,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_220(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "XXstrong_sellXX": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_221(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "STRONG_SELL": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_222(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": +6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_223(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -7,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_224(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = None

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_225(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(None)

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_226(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(None, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_227(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, None))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_228(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_229(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, ))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_230(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 1))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_231(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = None
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_232(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 1.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_233(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = None
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_234(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = "XXXX"
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_235(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = None
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_236(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").upper()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_237(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") and "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_238(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") and g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_239(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get(None) or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_240(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("XXeps_directionXX") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_241(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("EPS_DIRECTION") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_242(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get(None) or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_243(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("XXeps_changeXX") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_244(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("EPS_CHANGE") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_245(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "XXXX").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_246(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = None
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_247(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").upper()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_248(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") and "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_249(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") and g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_250(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get(None) or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_251(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("XXrevenue_directionXX") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_252(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("REVENUE_DIRECTION") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_253(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get(None) or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_254(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("XXrevenue_changeXX") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_255(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("REVENUE_CHANGE") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_256(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "XXXX").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_257(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction and "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_258(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction and "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_259(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "XXraiseXX" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_260(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "RAISE" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_261(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" not in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_262(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "XXaboveXX" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_263(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "ABOVE" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_264(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" not in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_265(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "XXbeatXX" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_266(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "BEAT" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_267(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" not in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_268(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score = 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_269(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score -= 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_270(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 9.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_271(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = None
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_272(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get(None, '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_273(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', None)}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_274(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_275(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', )}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_276(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('XXeps_estimateXX', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_277(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('EPS_ESTIMATE', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_278(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', 'XXXX')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_279(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            return
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_280(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction and "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_281(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction and "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_282(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "XXcutXX" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_283(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "CUT" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_284(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" not in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_285(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "XXbelowXX" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_286(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "BELOW" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_287(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" not in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_288(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "XXmissXX" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_289(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "MISS" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_290(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" not in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_291(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score = 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_292(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score += 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_293(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 11.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_294(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = None
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_295(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get(None, '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_296(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', None)}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_297(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_298(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', )}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_299(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('XXeps_estimateXX', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_300(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('EPS_ESTIMATE', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_301(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', 'XXXX')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_302(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            return

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_303(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = None

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_304(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "XXbulls_bearsXX": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_305(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "BULLS_BEARS": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_306(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "XXscoreXX": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_307(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "SCORE": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_308(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(None, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_309(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, None),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_310(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_311(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, ),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_312(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 2),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_313(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "XXlabelXX": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_314(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "LABEL": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_315(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "XXbull_textXX": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_316(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "BULL_TEXT": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_317(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:301],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_318(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "XXbear_textXX": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_319(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "BEAR_TEXT": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_320(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:301],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_321(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "XXbull_countXX": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_322(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "BULL_COUNT": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_323(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(None),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_324(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "XXbear_countXX": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_325(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "BEAR_COUNT": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_326(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(None),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_327(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "XXconsensusXX": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_328(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "CONSENSUS": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_329(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "XXscoreXX": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_330(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "SCORE": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_331(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(None, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_332(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, None),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_333(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_334(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, ),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_335(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 2),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_336(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "XXkeyXX": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_337(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "KEY": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_338(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "XXtarget_priceXX": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_339(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "TARGET_PRICE": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_340(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(None) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_341(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "XXanalyst_countXX": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_342(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "ANALYST_COUNT": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_343(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "XXguidanceXX": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_344(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "GUIDANCE": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_345(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "XXscoreXX": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_346(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "SCORE": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_347(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(None, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_348(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, None),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_349(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_350(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, ),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_351(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 2),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_352(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "XXsummaryXX": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_353(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "SUMMARY": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_354(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "XXtotal_scoreXX": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_355(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "TOTAL_SCORE": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_356(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(None, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_357(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, None),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_358(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_359(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, ),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_360(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score - guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_361(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score - cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_362(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 2),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_363(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = None
    return result


async def x_get_analyst_intelligence__mutmut_364(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"XXdataXX": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_365(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"DATA": result, "ts": now}
    return result


async def x_get_analyst_intelligence__mutmut_366(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "XXtsXX": now}
    return result


async def x_get_analyst_intelligence__mutmut_367(ticker: str) -> dict:
    """
    Fetch Bulls Bears Say, consensus ratings, and corporate guidance for a ticker.
    Returns a unified dict used by signal_engine.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    base_params = {"apiKey": api_key, "ticker": ticker}

    async with shared_session() as session:
        bulls_bears, consensus, guidance, ratings = await asyncio.gather(
            _fetch(session, "partners/bulls_bears_say", {**base_params, "limit": 1}),
            _fetch(session, "partners/consensus_ratings", base_params),
            _fetch_list(session, "partners/corporate_guidance", {**base_params, "limit": 3}),
            _fetch_list(session, "partners/analyst_ratings", {**base_params, "limit": 10}),
            return_exceptions=True,
        )

    # Make sure exceptions become empty results
    if isinstance(bulls_bears, Exception):
        bulls_bears = {}
    if isinstance(consensus, Exception):
        consensus = {}
    if isinstance(guidance, Exception):
        guidance = []
    if isinstance(ratings, Exception):
        ratings = []

    # ── Parse Bulls Bears Say ─────────────────────────────────────────────────
    bull_pts = float(bulls_bears.get("bull_count") or bulls_bears.get("bulls") or 0)
    bear_pts = float(bulls_bears.get("bear_count") or bulls_bears.get("bears") or 0)
    bull_text = bulls_bears.get("bull_summary") or bulls_bears.get("bull_case") or ""
    bear_text = bulls_bears.get("bear_summary") or bulls_bears.get("bear_case") or ""

    ratio = (bull_pts / bear_pts) if bear_pts > 0 else (bull_pts if bull_pts > 0 else 1)
    if bull_pts > bear_pts:
        bbs_score = min(6.0, 3.0 + (ratio - 1) * 0.5)
        bbs_label = "bullish"
    elif bear_pts > bull_pts:
        inv_ratio = (bear_pts / bull_pts) if bull_pts > 0 else bear_pts
        bbs_score = -min(6.0, 3.0 + (inv_ratio - 1) * 0.5)
        bbs_label = "bearish"
    else:
        bbs_score, bbs_label = 0.0, "neutral"

    # ── Parse Consensus Ratings ───────────────────────────────────────────────
    cons_key = (consensus.get("consensus") or consensus.get("recommendation") or "hold").lower()
    cons_pt = consensus.get("target_price_average") or consensus.get("target_mean")
    cons_count = int(consensus.get("analyst_count") or consensus.get("num_analysts") or 0)
    cons_score_map = {
        "strong_buy": 6,
        "buy": 4,
        "overweight": 3,
        "hold": 0,
        "underweight": -3,
        "sell": -4,
        "strong_sell": -6,
    }
    cons_score = float(cons_score_map.get(cons_key, 0))

    # ── Parse Corporate Guidance ──────────────────────────────────────────────
    guidance_score = 0.0
    guidance_summary = ""
    for g in guidance if isinstance(guidance, list) else []:
        eps_direction = (g.get("eps_direction") or g.get("eps_change") or "").lower()
        rev_direction = (g.get("revenue_direction") or g.get("revenue_change") or "").lower()
        if "raise" in eps_direction or "above" in eps_direction or "beat" in eps_direction:
            guidance_score += 8.0
            guidance_summary = f"EPS guidance raised: {g.get('eps_estimate', '')}"
            break
        elif "cut" in eps_direction or "below" in eps_direction or "miss" in eps_direction:
            guidance_score -= 10.0
            guidance_summary = f"EPS guidance cut: {g.get('eps_estimate', '')}"
            break

    result = {
        "bulls_bears": {
            "score": round(bbs_score, 1),
            "label": bbs_label,
            "bull_text": bull_text[:300],
            "bear_text": bear_text[:300],
            "bull_count": int(bull_pts),
            "bear_count": int(bear_pts),
        },
        "consensus": {
            "score": round(cons_score, 1),
            "key": cons_key,
            "target_price": float(cons_pt) if cons_pt else None,
            "analyst_count": cons_count,
        },
        "guidance": {
            "score": round(guidance_score, 1),
            "summary": guidance_summary,
        },
        "total_score": round(bbs_score + cons_score + guidance_score, 1),
    }

    _cache[ticker] = {"data": result, "TS": now}
    return result

mutants_x_get_analyst_intelligence__mutmut['_mutmut_orig'] = x_get_analyst_intelligence__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_1'] = x_get_analyst_intelligence__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_2'] = x_get_analyst_intelligence__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_3'] = x_get_analyst_intelligence__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_4'] = x_get_analyst_intelligence__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_5'] = x_get_analyst_intelligence__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_6'] = x_get_analyst_intelligence__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_7'] = x_get_analyst_intelligence__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_8'] = x_get_analyst_intelligence__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_9'] = x_get_analyst_intelligence__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_10'] = x_get_analyst_intelligence__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_11'] = x_get_analyst_intelligence__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_12'] = x_get_analyst_intelligence__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_13'] = x_get_analyst_intelligence__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_14'] = x_get_analyst_intelligence__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_15'] = x_get_analyst_intelligence__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_16'] = x_get_analyst_intelligence__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_17'] = x_get_analyst_intelligence__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_18'] = x_get_analyst_intelligence__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_19'] = x_get_analyst_intelligence__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_20'] = x_get_analyst_intelligence__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_21'] = x_get_analyst_intelligence__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_22'] = x_get_analyst_intelligence__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_23'] = x_get_analyst_intelligence__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_24'] = x_get_analyst_intelligence__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_25'] = x_get_analyst_intelligence__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_26'] = x_get_analyst_intelligence__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_27'] = x_get_analyst_intelligence__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_28'] = x_get_analyst_intelligence__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_29'] = x_get_analyst_intelligence__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_30'] = x_get_analyst_intelligence__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_31'] = x_get_analyst_intelligence__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_32'] = x_get_analyst_intelligence__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_33'] = x_get_analyst_intelligence__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_34'] = x_get_analyst_intelligence__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_35'] = x_get_analyst_intelligence__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_36'] = x_get_analyst_intelligence__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_37'] = x_get_analyst_intelligence__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_38'] = x_get_analyst_intelligence__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_39'] = x_get_analyst_intelligence__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_40'] = x_get_analyst_intelligence__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_41'] = x_get_analyst_intelligence__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_42'] = x_get_analyst_intelligence__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_43'] = x_get_analyst_intelligence__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_44'] = x_get_analyst_intelligence__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_45'] = x_get_analyst_intelligence__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_46'] = x_get_analyst_intelligence__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_47'] = x_get_analyst_intelligence__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_48'] = x_get_analyst_intelligence__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_49'] = x_get_analyst_intelligence__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_50'] = x_get_analyst_intelligence__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_51'] = x_get_analyst_intelligence__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_52'] = x_get_analyst_intelligence__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_53'] = x_get_analyst_intelligence__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_54'] = x_get_analyst_intelligence__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_55'] = x_get_analyst_intelligence__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_56'] = x_get_analyst_intelligence__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_57'] = x_get_analyst_intelligence__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_58'] = x_get_analyst_intelligence__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_59'] = x_get_analyst_intelligence__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_60'] = x_get_analyst_intelligence__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_61'] = x_get_analyst_intelligence__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_62'] = x_get_analyst_intelligence__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_63'] = x_get_analyst_intelligence__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_64'] = x_get_analyst_intelligence__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_65'] = x_get_analyst_intelligence__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_66'] = x_get_analyst_intelligence__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_67'] = x_get_analyst_intelligence__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_68'] = x_get_analyst_intelligence__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_69'] = x_get_analyst_intelligence__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_70'] = x_get_analyst_intelligence__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_71'] = x_get_analyst_intelligence__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_72'] = x_get_analyst_intelligence__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_73'] = x_get_analyst_intelligence__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_74'] = x_get_analyst_intelligence__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_75'] = x_get_analyst_intelligence__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_76'] = x_get_analyst_intelligence__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_77'] = x_get_analyst_intelligence__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_78'] = x_get_analyst_intelligence__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_79'] = x_get_analyst_intelligence__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_80'] = x_get_analyst_intelligence__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_81'] = x_get_analyst_intelligence__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_82'] = x_get_analyst_intelligence__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_83'] = x_get_analyst_intelligence__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_84'] = x_get_analyst_intelligence__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_85'] = x_get_analyst_intelligence__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_86'] = x_get_analyst_intelligence__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_87'] = x_get_analyst_intelligence__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_88'] = x_get_analyst_intelligence__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_89'] = x_get_analyst_intelligence__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_90'] = x_get_analyst_intelligence__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_91'] = x_get_analyst_intelligence__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_92'] = x_get_analyst_intelligence__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_93'] = x_get_analyst_intelligence__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_94'] = x_get_analyst_intelligence__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_95'] = x_get_analyst_intelligence__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_96'] = x_get_analyst_intelligence__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_97'] = x_get_analyst_intelligence__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_98'] = x_get_analyst_intelligence__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_99'] = x_get_analyst_intelligence__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_100'] = x_get_analyst_intelligence__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_101'] = x_get_analyst_intelligence__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_102'] = x_get_analyst_intelligence__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_103'] = x_get_analyst_intelligence__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_104'] = x_get_analyst_intelligence__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_105'] = x_get_analyst_intelligence__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_106'] = x_get_analyst_intelligence__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_107'] = x_get_analyst_intelligence__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_108'] = x_get_analyst_intelligence__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_109'] = x_get_analyst_intelligence__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_110'] = x_get_analyst_intelligence__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_111'] = x_get_analyst_intelligence__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_112'] = x_get_analyst_intelligence__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_113'] = x_get_analyst_intelligence__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_114'] = x_get_analyst_intelligence__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_115'] = x_get_analyst_intelligence__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_116'] = x_get_analyst_intelligence__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_117'] = x_get_analyst_intelligence__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_118'] = x_get_analyst_intelligence__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_119'] = x_get_analyst_intelligence__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_120'] = x_get_analyst_intelligence__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_121'] = x_get_analyst_intelligence__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_122'] = x_get_analyst_intelligence__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_123'] = x_get_analyst_intelligence__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_124'] = x_get_analyst_intelligence__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_125'] = x_get_analyst_intelligence__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_126'] = x_get_analyst_intelligence__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_127'] = x_get_analyst_intelligence__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_128'] = x_get_analyst_intelligence__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_129'] = x_get_analyst_intelligence__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_130'] = x_get_analyst_intelligence__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_131'] = x_get_analyst_intelligence__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_132'] = x_get_analyst_intelligence__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_133'] = x_get_analyst_intelligence__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_134'] = x_get_analyst_intelligence__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_135'] = x_get_analyst_intelligence__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_136'] = x_get_analyst_intelligence__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_137'] = x_get_analyst_intelligence__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_138'] = x_get_analyst_intelligence__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_139'] = x_get_analyst_intelligence__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_140'] = x_get_analyst_intelligence__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_141'] = x_get_analyst_intelligence__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_142'] = x_get_analyst_intelligence__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_143'] = x_get_analyst_intelligence__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_144'] = x_get_analyst_intelligence__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_145'] = x_get_analyst_intelligence__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_146'] = x_get_analyst_intelligence__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_147'] = x_get_analyst_intelligence__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_148'] = x_get_analyst_intelligence__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_149'] = x_get_analyst_intelligence__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_150'] = x_get_analyst_intelligence__mutmut_150 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_151'] = x_get_analyst_intelligence__mutmut_151 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_152'] = x_get_analyst_intelligence__mutmut_152 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_153'] = x_get_analyst_intelligence__mutmut_153 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_154'] = x_get_analyst_intelligence__mutmut_154 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_155'] = x_get_analyst_intelligence__mutmut_155 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_156'] = x_get_analyst_intelligence__mutmut_156 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_157'] = x_get_analyst_intelligence__mutmut_157 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_158'] = x_get_analyst_intelligence__mutmut_158 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_159'] = x_get_analyst_intelligence__mutmut_159 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_160'] = x_get_analyst_intelligence__mutmut_160 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_161'] = x_get_analyst_intelligence__mutmut_161 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_162'] = x_get_analyst_intelligence__mutmut_162 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_163'] = x_get_analyst_intelligence__mutmut_163 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_164'] = x_get_analyst_intelligence__mutmut_164 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_165'] = x_get_analyst_intelligence__mutmut_165 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_166'] = x_get_analyst_intelligence__mutmut_166 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_167'] = x_get_analyst_intelligence__mutmut_167 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_168'] = x_get_analyst_intelligence__mutmut_168 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_169'] = x_get_analyst_intelligence__mutmut_169 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_170'] = x_get_analyst_intelligence__mutmut_170 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_171'] = x_get_analyst_intelligence__mutmut_171 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_172'] = x_get_analyst_intelligence__mutmut_172 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_173'] = x_get_analyst_intelligence__mutmut_173 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_174'] = x_get_analyst_intelligence__mutmut_174 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_175'] = x_get_analyst_intelligence__mutmut_175 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_176'] = x_get_analyst_intelligence__mutmut_176 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_177'] = x_get_analyst_intelligence__mutmut_177 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_178'] = x_get_analyst_intelligence__mutmut_178 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_179'] = x_get_analyst_intelligence__mutmut_179 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_180'] = x_get_analyst_intelligence__mutmut_180 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_181'] = x_get_analyst_intelligence__mutmut_181 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_182'] = x_get_analyst_intelligence__mutmut_182 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_183'] = x_get_analyst_intelligence__mutmut_183 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_184'] = x_get_analyst_intelligence__mutmut_184 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_185'] = x_get_analyst_intelligence__mutmut_185 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_186'] = x_get_analyst_intelligence__mutmut_186 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_187'] = x_get_analyst_intelligence__mutmut_187 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_188'] = x_get_analyst_intelligence__mutmut_188 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_189'] = x_get_analyst_intelligence__mutmut_189 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_190'] = x_get_analyst_intelligence__mutmut_190 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_191'] = x_get_analyst_intelligence__mutmut_191 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_192'] = x_get_analyst_intelligence__mutmut_192 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_193'] = x_get_analyst_intelligence__mutmut_193 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_194'] = x_get_analyst_intelligence__mutmut_194 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_195'] = x_get_analyst_intelligence__mutmut_195 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_196'] = x_get_analyst_intelligence__mutmut_196 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_197'] = x_get_analyst_intelligence__mutmut_197 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_198'] = x_get_analyst_intelligence__mutmut_198 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_199'] = x_get_analyst_intelligence__mutmut_199 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_200'] = x_get_analyst_intelligence__mutmut_200 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_201'] = x_get_analyst_intelligence__mutmut_201 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_202'] = x_get_analyst_intelligence__mutmut_202 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_203'] = x_get_analyst_intelligence__mutmut_203 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_204'] = x_get_analyst_intelligence__mutmut_204 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_205'] = x_get_analyst_intelligence__mutmut_205 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_206'] = x_get_analyst_intelligence__mutmut_206 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_207'] = x_get_analyst_intelligence__mutmut_207 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_208'] = x_get_analyst_intelligence__mutmut_208 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_209'] = x_get_analyst_intelligence__mutmut_209 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_210'] = x_get_analyst_intelligence__mutmut_210 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_211'] = x_get_analyst_intelligence__mutmut_211 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_212'] = x_get_analyst_intelligence__mutmut_212 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_213'] = x_get_analyst_intelligence__mutmut_213 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_214'] = x_get_analyst_intelligence__mutmut_214 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_215'] = x_get_analyst_intelligence__mutmut_215 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_216'] = x_get_analyst_intelligence__mutmut_216 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_217'] = x_get_analyst_intelligence__mutmut_217 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_218'] = x_get_analyst_intelligence__mutmut_218 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_219'] = x_get_analyst_intelligence__mutmut_219 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_220'] = x_get_analyst_intelligence__mutmut_220 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_221'] = x_get_analyst_intelligence__mutmut_221 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_222'] = x_get_analyst_intelligence__mutmut_222 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_223'] = x_get_analyst_intelligence__mutmut_223 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_224'] = x_get_analyst_intelligence__mutmut_224 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_225'] = x_get_analyst_intelligence__mutmut_225 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_226'] = x_get_analyst_intelligence__mutmut_226 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_227'] = x_get_analyst_intelligence__mutmut_227 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_228'] = x_get_analyst_intelligence__mutmut_228 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_229'] = x_get_analyst_intelligence__mutmut_229 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_230'] = x_get_analyst_intelligence__mutmut_230 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_231'] = x_get_analyst_intelligence__mutmut_231 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_232'] = x_get_analyst_intelligence__mutmut_232 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_233'] = x_get_analyst_intelligence__mutmut_233 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_234'] = x_get_analyst_intelligence__mutmut_234 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_235'] = x_get_analyst_intelligence__mutmut_235 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_236'] = x_get_analyst_intelligence__mutmut_236 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_237'] = x_get_analyst_intelligence__mutmut_237 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_238'] = x_get_analyst_intelligence__mutmut_238 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_239'] = x_get_analyst_intelligence__mutmut_239 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_240'] = x_get_analyst_intelligence__mutmut_240 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_241'] = x_get_analyst_intelligence__mutmut_241 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_242'] = x_get_analyst_intelligence__mutmut_242 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_243'] = x_get_analyst_intelligence__mutmut_243 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_244'] = x_get_analyst_intelligence__mutmut_244 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_245'] = x_get_analyst_intelligence__mutmut_245 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_246'] = x_get_analyst_intelligence__mutmut_246 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_247'] = x_get_analyst_intelligence__mutmut_247 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_248'] = x_get_analyst_intelligence__mutmut_248 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_249'] = x_get_analyst_intelligence__mutmut_249 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_250'] = x_get_analyst_intelligence__mutmut_250 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_251'] = x_get_analyst_intelligence__mutmut_251 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_252'] = x_get_analyst_intelligence__mutmut_252 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_253'] = x_get_analyst_intelligence__mutmut_253 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_254'] = x_get_analyst_intelligence__mutmut_254 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_255'] = x_get_analyst_intelligence__mutmut_255 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_256'] = x_get_analyst_intelligence__mutmut_256 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_257'] = x_get_analyst_intelligence__mutmut_257 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_258'] = x_get_analyst_intelligence__mutmut_258 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_259'] = x_get_analyst_intelligence__mutmut_259 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_260'] = x_get_analyst_intelligence__mutmut_260 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_261'] = x_get_analyst_intelligence__mutmut_261 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_262'] = x_get_analyst_intelligence__mutmut_262 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_263'] = x_get_analyst_intelligence__mutmut_263 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_264'] = x_get_analyst_intelligence__mutmut_264 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_265'] = x_get_analyst_intelligence__mutmut_265 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_266'] = x_get_analyst_intelligence__mutmut_266 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_267'] = x_get_analyst_intelligence__mutmut_267 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_268'] = x_get_analyst_intelligence__mutmut_268 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_269'] = x_get_analyst_intelligence__mutmut_269 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_270'] = x_get_analyst_intelligence__mutmut_270 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_271'] = x_get_analyst_intelligence__mutmut_271 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_272'] = x_get_analyst_intelligence__mutmut_272 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_273'] = x_get_analyst_intelligence__mutmut_273 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_274'] = x_get_analyst_intelligence__mutmut_274 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_275'] = x_get_analyst_intelligence__mutmut_275 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_276'] = x_get_analyst_intelligence__mutmut_276 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_277'] = x_get_analyst_intelligence__mutmut_277 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_278'] = x_get_analyst_intelligence__mutmut_278 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_279'] = x_get_analyst_intelligence__mutmut_279 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_280'] = x_get_analyst_intelligence__mutmut_280 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_281'] = x_get_analyst_intelligence__mutmut_281 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_282'] = x_get_analyst_intelligence__mutmut_282 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_283'] = x_get_analyst_intelligence__mutmut_283 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_284'] = x_get_analyst_intelligence__mutmut_284 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_285'] = x_get_analyst_intelligence__mutmut_285 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_286'] = x_get_analyst_intelligence__mutmut_286 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_287'] = x_get_analyst_intelligence__mutmut_287 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_288'] = x_get_analyst_intelligence__mutmut_288 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_289'] = x_get_analyst_intelligence__mutmut_289 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_290'] = x_get_analyst_intelligence__mutmut_290 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_291'] = x_get_analyst_intelligence__mutmut_291 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_292'] = x_get_analyst_intelligence__mutmut_292 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_293'] = x_get_analyst_intelligence__mutmut_293 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_294'] = x_get_analyst_intelligence__mutmut_294 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_295'] = x_get_analyst_intelligence__mutmut_295 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_296'] = x_get_analyst_intelligence__mutmut_296 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_297'] = x_get_analyst_intelligence__mutmut_297 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_298'] = x_get_analyst_intelligence__mutmut_298 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_299'] = x_get_analyst_intelligence__mutmut_299 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_300'] = x_get_analyst_intelligence__mutmut_300 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_301'] = x_get_analyst_intelligence__mutmut_301 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_302'] = x_get_analyst_intelligence__mutmut_302 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_303'] = x_get_analyst_intelligence__mutmut_303 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_304'] = x_get_analyst_intelligence__mutmut_304 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_305'] = x_get_analyst_intelligence__mutmut_305 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_306'] = x_get_analyst_intelligence__mutmut_306 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_307'] = x_get_analyst_intelligence__mutmut_307 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_308'] = x_get_analyst_intelligence__mutmut_308 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_309'] = x_get_analyst_intelligence__mutmut_309 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_310'] = x_get_analyst_intelligence__mutmut_310 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_311'] = x_get_analyst_intelligence__mutmut_311 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_312'] = x_get_analyst_intelligence__mutmut_312 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_313'] = x_get_analyst_intelligence__mutmut_313 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_314'] = x_get_analyst_intelligence__mutmut_314 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_315'] = x_get_analyst_intelligence__mutmut_315 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_316'] = x_get_analyst_intelligence__mutmut_316 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_317'] = x_get_analyst_intelligence__mutmut_317 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_318'] = x_get_analyst_intelligence__mutmut_318 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_319'] = x_get_analyst_intelligence__mutmut_319 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_320'] = x_get_analyst_intelligence__mutmut_320 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_321'] = x_get_analyst_intelligence__mutmut_321 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_322'] = x_get_analyst_intelligence__mutmut_322 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_323'] = x_get_analyst_intelligence__mutmut_323 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_324'] = x_get_analyst_intelligence__mutmut_324 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_325'] = x_get_analyst_intelligence__mutmut_325 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_326'] = x_get_analyst_intelligence__mutmut_326 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_327'] = x_get_analyst_intelligence__mutmut_327 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_328'] = x_get_analyst_intelligence__mutmut_328 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_329'] = x_get_analyst_intelligence__mutmut_329 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_330'] = x_get_analyst_intelligence__mutmut_330 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_331'] = x_get_analyst_intelligence__mutmut_331 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_332'] = x_get_analyst_intelligence__mutmut_332 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_333'] = x_get_analyst_intelligence__mutmut_333 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_334'] = x_get_analyst_intelligence__mutmut_334 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_335'] = x_get_analyst_intelligence__mutmut_335 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_336'] = x_get_analyst_intelligence__mutmut_336 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_337'] = x_get_analyst_intelligence__mutmut_337 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_338'] = x_get_analyst_intelligence__mutmut_338 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_339'] = x_get_analyst_intelligence__mutmut_339 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_340'] = x_get_analyst_intelligence__mutmut_340 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_341'] = x_get_analyst_intelligence__mutmut_341 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_342'] = x_get_analyst_intelligence__mutmut_342 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_343'] = x_get_analyst_intelligence__mutmut_343 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_344'] = x_get_analyst_intelligence__mutmut_344 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_345'] = x_get_analyst_intelligence__mutmut_345 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_346'] = x_get_analyst_intelligence__mutmut_346 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_347'] = x_get_analyst_intelligence__mutmut_347 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_348'] = x_get_analyst_intelligence__mutmut_348 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_349'] = x_get_analyst_intelligence__mutmut_349 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_350'] = x_get_analyst_intelligence__mutmut_350 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_351'] = x_get_analyst_intelligence__mutmut_351 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_352'] = x_get_analyst_intelligence__mutmut_352 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_353'] = x_get_analyst_intelligence__mutmut_353 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_354'] = x_get_analyst_intelligence__mutmut_354 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_355'] = x_get_analyst_intelligence__mutmut_355 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_356'] = x_get_analyst_intelligence__mutmut_356 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_357'] = x_get_analyst_intelligence__mutmut_357 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_358'] = x_get_analyst_intelligence__mutmut_358 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_359'] = x_get_analyst_intelligence__mutmut_359 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_360'] = x_get_analyst_intelligence__mutmut_360 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_361'] = x_get_analyst_intelligence__mutmut_361 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_362'] = x_get_analyst_intelligence__mutmut_362 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_363'] = x_get_analyst_intelligence__mutmut_363 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_364'] = x_get_analyst_intelligence__mutmut_364 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_365'] = x_get_analyst_intelligence__mutmut_365 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_366'] = x_get_analyst_intelligence__mutmut_366 # type: ignore # mutmut generated
mutants_x_get_analyst_intelligence__mutmut['x_get_analyst_intelligence__mutmut_367'] = x_get_analyst_intelligence__mutmut_367 # type: ignore # mutmut generated
