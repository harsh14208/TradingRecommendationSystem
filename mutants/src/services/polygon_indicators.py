"""
Polygon.io Pre-Computed Technical Indicators (Starter plan — unlimited calls)

Endpoints used:
  v1/indicators/rsi/{ticker}   — RSI(14) daily + weekly
  v1/indicators/macd/{ticker}  — MACD(12,26,9) daily
  v1/indicators/sma/{ticker}   — SMA(20, 50, 200) daily + SMA(20) weekly
  v1/indicators/ema/{ticker}   — EMA(8, 21, 200) daily

Cache: 30 minutes per ticker (indicators are end-of-day stable during market hours)
"""

import asyncio
import logging
import os
import ssl
import time

import aiohttp
import certifi
from services.http_client import shared_session

log = logging.getLogger("signal.trade.polygon_indicators")

_cache: dict[str, dict] = {}
_weekly_cache: dict[str, dict] = {}
_TTL = 600  # 10 minutes — unlimited Polygon calls
_WEEKLY_TTL = 3600  # 1 hour — weekly bars change once per week
_BASE = "https://api.polygon.io"
_SSL_CTX = ssl.create_default_context(cafile=certifi.where())


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__get_key__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__get_key__mutmut)
def _get_key() -> str:
    return os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY") or ""


def x__get_key__mutmut_orig() -> str:
    return os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY") or ""


def x__get_key__mutmut_1() -> str:
    return os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY") and ""


def x__get_key__mutmut_2() -> str:
    return os.getenv("POLYGON_API_KEY") and os.getenv("MASSIVE_API_KEY") or ""


def x__get_key__mutmut_3() -> str:
    return os.getenv(None) or os.getenv("MASSIVE_API_KEY") or ""


def x__get_key__mutmut_4() -> str:
    return os.getenv("XXPOLYGON_API_KEYXX") or os.getenv("MASSIVE_API_KEY") or ""


def x__get_key__mutmut_5() -> str:
    return os.getenv("polygon_api_key") or os.getenv("MASSIVE_API_KEY") or ""


def x__get_key__mutmut_6() -> str:
    return os.getenv("POLYGON_API_KEY") or os.getenv(None) or ""


def x__get_key__mutmut_7() -> str:
    return os.getenv("POLYGON_API_KEY") or os.getenv("XXMASSIVE_API_KEYXX") or ""


def x__get_key__mutmut_8() -> str:
    return os.getenv("POLYGON_API_KEY") or os.getenv("massive_api_key") or ""


def x__get_key__mutmut_9() -> str:
    return os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY") or "XXXX"

mutants_x__get_key__mutmut['_mutmut_orig'] = x__get_key__mutmut_orig # type: ignore # mutmut generated
mutants_x__get_key__mutmut['x__get_key__mutmut_1'] = x__get_key__mutmut_1 # type: ignore # mutmut generated
mutants_x__get_key__mutmut['x__get_key__mutmut_2'] = x__get_key__mutmut_2 # type: ignore # mutmut generated
mutants_x__get_key__mutmut['x__get_key__mutmut_3'] = x__get_key__mutmut_3 # type: ignore # mutmut generated
mutants_x__get_key__mutmut['x__get_key__mutmut_4'] = x__get_key__mutmut_4 # type: ignore # mutmut generated
mutants_x__get_key__mutmut['x__get_key__mutmut_5'] = x__get_key__mutmut_5 # type: ignore # mutmut generated
mutants_x__get_key__mutmut['x__get_key__mutmut_6'] = x__get_key__mutmut_6 # type: ignore # mutmut generated
mutants_x__get_key__mutmut['x__get_key__mutmut_7'] = x__get_key__mutmut_7 # type: ignore # mutmut generated
mutants_x__get_key__mutmut['x__get_key__mutmut_8'] = x__get_key__mutmut_8 # type: ignore # mutmut generated
mutants_x__get_key__mutmut['x__get_key__mutmut_9'] = x__get_key__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fetch_indicator__mutmut)
async def _fetch_indicator(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_orig(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_1(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            None, params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_2(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=None, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_3(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=None, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_4(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=None
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_5(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_6(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_7(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_8(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_9(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=None)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_10(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=9)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_11(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status != 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_12(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 430:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_13(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(None)
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_14(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status == 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_15(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 201:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_16(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = None
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_17(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = None
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_18(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get(None, {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_19(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", None)
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_20(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get({})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_21(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", )
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_22(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("XXresultsXX", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_23(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("RESULTS", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_24(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get(None, []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_25(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", None) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_26(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get([]) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_27(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", ) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_28(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("XXvaluesXX", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_29(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("VALUES", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(f"[polygon_indicators] {endpoint}: {e}")
        return []


async def x__fetch_indicator__mutmut_30(session: aiohttp.ClientSession, endpoint: str, params: dict) -> list:
    """Fetch a single indicator series. Returns list of {timestamp, value} or [] on error."""
    try:
        async with session.get(
            f"{_BASE}/{endpoint}", params=params, ssl=_SSL_CTX, timeout=aiohttp.ClientTimeout(total=8)
        ) as resp:
            if resp.status == 429:
                log.debug(f"[polygon_indicators] rate limited on {endpoint}")
                return []
            if resp.status != 200:
                return []
            data = await resp.json()
            results = data.get("results", {})
            return results.get("values", []) if isinstance(results, dict) else []
    except Exception as e:
        log.debug(None)
        return []

mutants_x__fetch_indicator__mutmut['_mutmut_orig'] = x__fetch_indicator__mutmut_orig # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_1'] = x__fetch_indicator__mutmut_1 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_2'] = x__fetch_indicator__mutmut_2 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_3'] = x__fetch_indicator__mutmut_3 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_4'] = x__fetch_indicator__mutmut_4 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_5'] = x__fetch_indicator__mutmut_5 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_6'] = x__fetch_indicator__mutmut_6 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_7'] = x__fetch_indicator__mutmut_7 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_8'] = x__fetch_indicator__mutmut_8 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_9'] = x__fetch_indicator__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_10'] = x__fetch_indicator__mutmut_10 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_11'] = x__fetch_indicator__mutmut_11 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_12'] = x__fetch_indicator__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_13'] = x__fetch_indicator__mutmut_13 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_14'] = x__fetch_indicator__mutmut_14 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_15'] = x__fetch_indicator__mutmut_15 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_16'] = x__fetch_indicator__mutmut_16 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_17'] = x__fetch_indicator__mutmut_17 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_18'] = x__fetch_indicator__mutmut_18 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_19'] = x__fetch_indicator__mutmut_19 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_20'] = x__fetch_indicator__mutmut_20 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_21'] = x__fetch_indicator__mutmut_21 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_22'] = x__fetch_indicator__mutmut_22 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_23'] = x__fetch_indicator__mutmut_23 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_24'] = x__fetch_indicator__mutmut_24 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_25'] = x__fetch_indicator__mutmut_25 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_26'] = x__fetch_indicator__mutmut_26 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_27'] = x__fetch_indicator__mutmut_27 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_28'] = x__fetch_indicator__mutmut_28 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_29'] = x__fetch_indicator__mutmut_29 # type: ignore # mutmut generated
mutants_x__fetch_indicator__mutmut['x__fetch_indicator__mutmut_30'] = x__fetch_indicator__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_indicators__mutmut)
async def get_indicators(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_orig(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_1(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = None
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_2(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache or now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_3(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker not in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_4(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now + _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_5(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["XXtsXX"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_6(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["TS"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_7(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] <= _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_8(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["XXdataXX"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_9(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["DATA"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_10(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = None
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_11(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_12(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = None
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_13(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "XXapiKeyXX": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_14(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apikey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_15(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "APIKEY": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_16(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "XXtimespanXX": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_17(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "TIMESPAN": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_18(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "XXdayXX",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_19(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "DAY",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_20(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "XXseries_typeXX": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_21(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "SERIES_TYPE": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_22(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "XXcloseXX",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_23(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "CLOSE",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_24(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "XXadjustedXX": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_25(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "ADJUSTED": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_26(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "XXtrueXX",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_27(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "TRUE",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_28(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "XXlimitXX": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_29(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "LIMIT": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_30(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 4,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_31(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "XXorderXX": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_32(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "ORDER": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_33(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "XXdescXX",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_34(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "DESC",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_35(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = None

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_36(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.lower()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_37(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = None
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_38(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        None,
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_39(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params=None,
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_40(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=None,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_41(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=None,
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_42(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_43(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_44(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_45(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_46(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "XXshort_windowXX": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_47(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "SHORT_WINDOW": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_48(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 13, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_49(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "XXlong_windowXX": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_50(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "LONG_WINDOW": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_51(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 27, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_52(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "XXsignal_windowXX": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_53(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "SIGNAL_WINDOW": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_54(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 10},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_55(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=None),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_56(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=9),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_57(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status != 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_58(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 201:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_59(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = None
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_60(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get(None, [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_61(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", None)
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_62(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get([])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_63(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", )
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_64(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) and {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_65(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get(None, {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_66(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", None) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_67(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get({}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_68(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", ) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_69(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("XXresultsXX", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_70(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("RESULTS", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_71(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("XXvaluesXX", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_72(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("VALUES", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_73(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = None

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_74(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                None,
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_75(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                None,
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_76(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                None,
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_77(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                None,
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_78(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                None,
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_79(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                None,
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_80(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_81(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_82(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_83(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_84(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_85(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_86(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(None, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_87(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, None, {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_88(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", None),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_89(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_90(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_91(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", ),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_92(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "XXwindowXX": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_93(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "WINDOW": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_94(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 15}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_95(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(None, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_96(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, None, {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_97(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", None),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_98(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_99(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_100(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", ),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_101(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "XXwindowXX": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_102(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "WINDOW": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_103(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 21}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_104(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(None, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_105(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, None, {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_106(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", None),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_107(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_108(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_109(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", ),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_110(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "XXwindowXX": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_111(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "WINDOW": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_112(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 51}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_113(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(None, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_114(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, None, {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_115(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", None),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_116(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_117(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_118(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", ),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_119(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "XXwindowXX": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_120(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "WINDOW": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_121(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 201}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_122(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(None, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_123(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, None, {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_124(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", None),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_125(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_126(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_127(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", ),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_128(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "XXwindowXX": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_129(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "WINDOW": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_130(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 201}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_131(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = None

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_132(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["XXrsiXX"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_133(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["RSI"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_134(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(None, 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_135(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), None)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_136(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_137(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), )

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_138(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(None), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_139(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get(None, 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_140(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", None)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_141(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get(0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_142(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", )), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_143(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[1].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_144(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("XXvalueXX", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_145(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("VALUE", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_146(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 1)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_147(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 3)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_148(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = None
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_149(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[1]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_150(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = None
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_151(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["XXmacdXX"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_152(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["MACD"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_153(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(None, 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_154(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), None)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_155(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_156(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), )
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_157(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(None), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_158(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get(None, 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_159(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", None)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_160(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get(0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_161(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", )), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_162(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("XXvalueXX", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_163(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("VALUE", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_164(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 1)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_165(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 5)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_166(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = None
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_167(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["XXmacd_signalXX"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_168(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["MACD_SIGNAL"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_169(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(None, 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_170(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), None)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_171(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_172(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), )
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_173(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(None), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_174(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get(None, 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_175(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", None)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_176(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get(0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_177(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", )), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_178(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("XXsignalXX", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_179(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("SIGNAL", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_180(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 1)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_181(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 5)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_182(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = None

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_183(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["XXmacd_histXX"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_184(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["MACD_HIST"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_185(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(None, 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_186(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), None)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_187(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_188(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), )

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_189(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(None), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_190(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get(None, 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_191(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", None)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_192(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get(0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_193(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", )), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_194(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("XXhistogramXX", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_195(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("HISTOGRAM", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_196(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 1)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_197(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 5)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_198(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = None
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_199(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["XXsma20XX"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_200(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["SMA20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_201(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(None, 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_202(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), None)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_203(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_204(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), )
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_205(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(None), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_206(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get(None, 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_207(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", None)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_208(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get(0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_209(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", )), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_210(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[1].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_211(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("XXvalueXX", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_212(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("VALUE", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_213(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 1)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_214(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 3)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_215(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = None
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_216(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["XXsma50XX"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_217(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["SMA50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_218(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(None, 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_219(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), None)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_220(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_221(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), )
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_222(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(None), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_223(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get(None, 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_224(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", None)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_225(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get(0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_226(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", )), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_227(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[1].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_228(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("XXvalueXX", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_229(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("VALUE", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_230(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 1)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_231(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 3)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_232(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = None

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_233(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["XXsma200XX"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_234(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["SMA200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_235(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(None, 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_236(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), None)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_237(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_238(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), )

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_239(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(None), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_240(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get(None, 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_241(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", None)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_242(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get(0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_243(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", )), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_244(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[1].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_245(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("XXvalueXX", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_246(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("VALUE", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_247(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 1)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_248(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 3)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_249(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = None

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_250(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["XXema200XX"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_251(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["EMA200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_252(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(None, 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_253(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), None)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_254(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_255(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), )

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_256(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(None), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_257(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get(None, 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_258(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", None)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_259(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get(0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_260(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", )), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_261(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[1].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_262(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("XXvalueXX", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_263(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("VALUE", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_264(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 1)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_265(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 3)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_266(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(None)

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_267(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = None
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_268(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"XXdataXX": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_269(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"DATA": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_270(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "XXtsXX": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_271(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "TS": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_272(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            None
        )
    return result


async def x_get_indicators__mutmut_273(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get(None, '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_274(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', None)} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_275(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_276(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', )} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_277(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('XXrsiXX', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_278(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('RSI', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_279(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', 'XX?XX')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_280(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get(None, '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_281(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', None)} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_282(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_283(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', )} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_284(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('XXmacd_histXX', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_285(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('MACD_HIST', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_286(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', 'XX?XX')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_287(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get(None, '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_288(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', None)} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_289(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_290(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', )} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_291(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('XXsma200XX', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_292(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('SMA200', '?')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_293(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', 'XX?XX')} "
            f"EMA200={result.get('ema200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_294(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get(None, '?')}"
        )
    return result


async def x_get_indicators__mutmut_295(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', None)}"
        )
    return result


async def x_get_indicators__mutmut_296(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('?')}"
        )
    return result


async def x_get_indicators__mutmut_297(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', )}"
        )
    return result


async def x_get_indicators__mutmut_298(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('XXema200XX', '?')}"
        )
    return result


async def x_get_indicators__mutmut_299(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('EMA200', '?')}"
        )
    return result


async def x_get_indicators__mutmut_300(ticker: str) -> dict:
    """
    Fetch RSI(14), MACD(12,26,9), SMA(20/50/200), EMA(8/21/200) for a ticker.
    Returns dict with current values, or empty dict if unavailable.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    base_params = {
        "apiKey": key,
        "timespan": "day",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()

    result: dict = {}
    try:
        async with shared_session() as session:

            async def _fetch_macd() -> list:
                try:
                    async with session.get(
                        f"{_BASE}/v1/indicators/macd/{t}",
                        params={**base_params, "short_window": 12, "long_window": 26, "signal_window": 9},
                        ssl=_SSL_CTX,
                        timeout=aiohttp.ClientTimeout(total=8),
                    ) as resp:
                        if resp.status == 200:
                            d = await resp.json()
                            return (d.get("results", {}) or {}).get("values", [])
                except Exception:
                    pass
                return []

            (rsi_vals, macd_data_raw, sma20_vals, sma50_vals, sma200_vals, ema200_vals) = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**base_params, "window": 14}),
                _fetch_macd(),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 20}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 50}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**base_params, "window": 200}),
                _fetch_indicator(session, f"v1/indicators/ema/{t}", {**base_params, "window": 200}),
            )

        # Parse RSI
        if rsi_vals:
            result["rsi"] = round(float(rsi_vals[0].get("value", 0)), 2)

        # Parse MACD
        if macd_data_raw:
            m = macd_data_raw[0]
            result["macd"] = round(float(m.get("value", 0)), 4)
            result["macd_signal"] = round(float(m.get("signal", 0)), 4)
            result["macd_hist"] = round(float(m.get("histogram", 0)), 4)

        # Parse SMAs
        if sma20_vals:
            result["sma20"] = round(float(sma20_vals[0].get("value", 0)), 2)
        if sma50_vals:
            result["sma50"] = round(float(sma50_vals[0].get("value", 0)), 2)
        if sma200_vals:
            result["sma200"] = round(float(sma200_vals[0].get("value", 0)), 2)

        # Parse EMA(200)
        if ema200_vals:
            result["ema200"] = round(float(ema200_vals[0].get("value", 0)), 2)

    except Exception as e:
        log.warning(f"[polygon_indicators] {ticker}: {e}")

    _cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_indicators] {ticker}: RSI={result.get('rsi', '?')} "
            f"MACD={result.get('macd_hist', '?')} SMA200={result.get('sma200', '?')} "
            f"EMA200={result.get('ema200', 'XX?XX')}"
        )
    return result

mutants_x_get_indicators__mutmut['_mutmut_orig'] = x_get_indicators__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_1'] = x_get_indicators__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_2'] = x_get_indicators__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_3'] = x_get_indicators__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_4'] = x_get_indicators__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_5'] = x_get_indicators__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_6'] = x_get_indicators__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_7'] = x_get_indicators__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_8'] = x_get_indicators__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_9'] = x_get_indicators__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_10'] = x_get_indicators__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_11'] = x_get_indicators__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_12'] = x_get_indicators__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_13'] = x_get_indicators__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_14'] = x_get_indicators__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_15'] = x_get_indicators__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_16'] = x_get_indicators__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_17'] = x_get_indicators__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_18'] = x_get_indicators__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_19'] = x_get_indicators__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_20'] = x_get_indicators__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_21'] = x_get_indicators__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_22'] = x_get_indicators__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_23'] = x_get_indicators__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_24'] = x_get_indicators__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_25'] = x_get_indicators__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_26'] = x_get_indicators__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_27'] = x_get_indicators__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_28'] = x_get_indicators__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_29'] = x_get_indicators__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_30'] = x_get_indicators__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_31'] = x_get_indicators__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_32'] = x_get_indicators__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_33'] = x_get_indicators__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_34'] = x_get_indicators__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_35'] = x_get_indicators__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_36'] = x_get_indicators__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_37'] = x_get_indicators__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_38'] = x_get_indicators__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_39'] = x_get_indicators__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_40'] = x_get_indicators__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_41'] = x_get_indicators__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_42'] = x_get_indicators__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_43'] = x_get_indicators__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_44'] = x_get_indicators__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_45'] = x_get_indicators__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_46'] = x_get_indicators__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_47'] = x_get_indicators__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_48'] = x_get_indicators__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_49'] = x_get_indicators__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_50'] = x_get_indicators__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_51'] = x_get_indicators__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_52'] = x_get_indicators__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_53'] = x_get_indicators__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_54'] = x_get_indicators__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_55'] = x_get_indicators__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_56'] = x_get_indicators__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_57'] = x_get_indicators__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_58'] = x_get_indicators__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_59'] = x_get_indicators__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_60'] = x_get_indicators__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_61'] = x_get_indicators__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_62'] = x_get_indicators__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_63'] = x_get_indicators__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_64'] = x_get_indicators__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_65'] = x_get_indicators__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_66'] = x_get_indicators__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_67'] = x_get_indicators__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_68'] = x_get_indicators__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_69'] = x_get_indicators__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_70'] = x_get_indicators__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_71'] = x_get_indicators__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_72'] = x_get_indicators__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_73'] = x_get_indicators__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_74'] = x_get_indicators__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_75'] = x_get_indicators__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_76'] = x_get_indicators__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_77'] = x_get_indicators__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_78'] = x_get_indicators__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_79'] = x_get_indicators__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_80'] = x_get_indicators__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_81'] = x_get_indicators__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_82'] = x_get_indicators__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_83'] = x_get_indicators__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_84'] = x_get_indicators__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_85'] = x_get_indicators__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_86'] = x_get_indicators__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_87'] = x_get_indicators__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_88'] = x_get_indicators__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_89'] = x_get_indicators__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_90'] = x_get_indicators__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_91'] = x_get_indicators__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_92'] = x_get_indicators__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_93'] = x_get_indicators__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_94'] = x_get_indicators__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_95'] = x_get_indicators__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_96'] = x_get_indicators__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_97'] = x_get_indicators__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_98'] = x_get_indicators__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_99'] = x_get_indicators__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_100'] = x_get_indicators__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_101'] = x_get_indicators__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_102'] = x_get_indicators__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_103'] = x_get_indicators__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_104'] = x_get_indicators__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_105'] = x_get_indicators__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_106'] = x_get_indicators__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_107'] = x_get_indicators__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_108'] = x_get_indicators__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_109'] = x_get_indicators__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_110'] = x_get_indicators__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_111'] = x_get_indicators__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_112'] = x_get_indicators__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_113'] = x_get_indicators__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_114'] = x_get_indicators__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_115'] = x_get_indicators__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_116'] = x_get_indicators__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_117'] = x_get_indicators__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_118'] = x_get_indicators__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_119'] = x_get_indicators__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_120'] = x_get_indicators__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_121'] = x_get_indicators__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_122'] = x_get_indicators__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_123'] = x_get_indicators__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_124'] = x_get_indicators__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_125'] = x_get_indicators__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_126'] = x_get_indicators__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_127'] = x_get_indicators__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_128'] = x_get_indicators__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_129'] = x_get_indicators__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_130'] = x_get_indicators__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_131'] = x_get_indicators__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_132'] = x_get_indicators__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_133'] = x_get_indicators__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_134'] = x_get_indicators__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_135'] = x_get_indicators__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_136'] = x_get_indicators__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_137'] = x_get_indicators__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_138'] = x_get_indicators__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_139'] = x_get_indicators__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_140'] = x_get_indicators__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_141'] = x_get_indicators__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_142'] = x_get_indicators__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_143'] = x_get_indicators__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_144'] = x_get_indicators__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_145'] = x_get_indicators__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_146'] = x_get_indicators__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_147'] = x_get_indicators__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_148'] = x_get_indicators__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_149'] = x_get_indicators__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_150'] = x_get_indicators__mutmut_150 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_151'] = x_get_indicators__mutmut_151 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_152'] = x_get_indicators__mutmut_152 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_153'] = x_get_indicators__mutmut_153 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_154'] = x_get_indicators__mutmut_154 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_155'] = x_get_indicators__mutmut_155 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_156'] = x_get_indicators__mutmut_156 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_157'] = x_get_indicators__mutmut_157 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_158'] = x_get_indicators__mutmut_158 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_159'] = x_get_indicators__mutmut_159 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_160'] = x_get_indicators__mutmut_160 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_161'] = x_get_indicators__mutmut_161 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_162'] = x_get_indicators__mutmut_162 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_163'] = x_get_indicators__mutmut_163 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_164'] = x_get_indicators__mutmut_164 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_165'] = x_get_indicators__mutmut_165 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_166'] = x_get_indicators__mutmut_166 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_167'] = x_get_indicators__mutmut_167 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_168'] = x_get_indicators__mutmut_168 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_169'] = x_get_indicators__mutmut_169 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_170'] = x_get_indicators__mutmut_170 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_171'] = x_get_indicators__mutmut_171 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_172'] = x_get_indicators__mutmut_172 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_173'] = x_get_indicators__mutmut_173 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_174'] = x_get_indicators__mutmut_174 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_175'] = x_get_indicators__mutmut_175 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_176'] = x_get_indicators__mutmut_176 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_177'] = x_get_indicators__mutmut_177 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_178'] = x_get_indicators__mutmut_178 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_179'] = x_get_indicators__mutmut_179 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_180'] = x_get_indicators__mutmut_180 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_181'] = x_get_indicators__mutmut_181 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_182'] = x_get_indicators__mutmut_182 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_183'] = x_get_indicators__mutmut_183 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_184'] = x_get_indicators__mutmut_184 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_185'] = x_get_indicators__mutmut_185 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_186'] = x_get_indicators__mutmut_186 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_187'] = x_get_indicators__mutmut_187 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_188'] = x_get_indicators__mutmut_188 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_189'] = x_get_indicators__mutmut_189 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_190'] = x_get_indicators__mutmut_190 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_191'] = x_get_indicators__mutmut_191 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_192'] = x_get_indicators__mutmut_192 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_193'] = x_get_indicators__mutmut_193 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_194'] = x_get_indicators__mutmut_194 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_195'] = x_get_indicators__mutmut_195 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_196'] = x_get_indicators__mutmut_196 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_197'] = x_get_indicators__mutmut_197 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_198'] = x_get_indicators__mutmut_198 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_199'] = x_get_indicators__mutmut_199 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_200'] = x_get_indicators__mutmut_200 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_201'] = x_get_indicators__mutmut_201 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_202'] = x_get_indicators__mutmut_202 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_203'] = x_get_indicators__mutmut_203 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_204'] = x_get_indicators__mutmut_204 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_205'] = x_get_indicators__mutmut_205 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_206'] = x_get_indicators__mutmut_206 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_207'] = x_get_indicators__mutmut_207 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_208'] = x_get_indicators__mutmut_208 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_209'] = x_get_indicators__mutmut_209 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_210'] = x_get_indicators__mutmut_210 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_211'] = x_get_indicators__mutmut_211 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_212'] = x_get_indicators__mutmut_212 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_213'] = x_get_indicators__mutmut_213 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_214'] = x_get_indicators__mutmut_214 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_215'] = x_get_indicators__mutmut_215 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_216'] = x_get_indicators__mutmut_216 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_217'] = x_get_indicators__mutmut_217 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_218'] = x_get_indicators__mutmut_218 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_219'] = x_get_indicators__mutmut_219 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_220'] = x_get_indicators__mutmut_220 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_221'] = x_get_indicators__mutmut_221 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_222'] = x_get_indicators__mutmut_222 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_223'] = x_get_indicators__mutmut_223 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_224'] = x_get_indicators__mutmut_224 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_225'] = x_get_indicators__mutmut_225 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_226'] = x_get_indicators__mutmut_226 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_227'] = x_get_indicators__mutmut_227 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_228'] = x_get_indicators__mutmut_228 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_229'] = x_get_indicators__mutmut_229 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_230'] = x_get_indicators__mutmut_230 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_231'] = x_get_indicators__mutmut_231 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_232'] = x_get_indicators__mutmut_232 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_233'] = x_get_indicators__mutmut_233 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_234'] = x_get_indicators__mutmut_234 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_235'] = x_get_indicators__mutmut_235 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_236'] = x_get_indicators__mutmut_236 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_237'] = x_get_indicators__mutmut_237 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_238'] = x_get_indicators__mutmut_238 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_239'] = x_get_indicators__mutmut_239 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_240'] = x_get_indicators__mutmut_240 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_241'] = x_get_indicators__mutmut_241 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_242'] = x_get_indicators__mutmut_242 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_243'] = x_get_indicators__mutmut_243 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_244'] = x_get_indicators__mutmut_244 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_245'] = x_get_indicators__mutmut_245 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_246'] = x_get_indicators__mutmut_246 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_247'] = x_get_indicators__mutmut_247 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_248'] = x_get_indicators__mutmut_248 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_249'] = x_get_indicators__mutmut_249 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_250'] = x_get_indicators__mutmut_250 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_251'] = x_get_indicators__mutmut_251 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_252'] = x_get_indicators__mutmut_252 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_253'] = x_get_indicators__mutmut_253 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_254'] = x_get_indicators__mutmut_254 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_255'] = x_get_indicators__mutmut_255 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_256'] = x_get_indicators__mutmut_256 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_257'] = x_get_indicators__mutmut_257 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_258'] = x_get_indicators__mutmut_258 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_259'] = x_get_indicators__mutmut_259 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_260'] = x_get_indicators__mutmut_260 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_261'] = x_get_indicators__mutmut_261 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_262'] = x_get_indicators__mutmut_262 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_263'] = x_get_indicators__mutmut_263 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_264'] = x_get_indicators__mutmut_264 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_265'] = x_get_indicators__mutmut_265 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_266'] = x_get_indicators__mutmut_266 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_267'] = x_get_indicators__mutmut_267 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_268'] = x_get_indicators__mutmut_268 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_269'] = x_get_indicators__mutmut_269 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_270'] = x_get_indicators__mutmut_270 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_271'] = x_get_indicators__mutmut_271 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_272'] = x_get_indicators__mutmut_272 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_273'] = x_get_indicators__mutmut_273 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_274'] = x_get_indicators__mutmut_274 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_275'] = x_get_indicators__mutmut_275 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_276'] = x_get_indicators__mutmut_276 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_277'] = x_get_indicators__mutmut_277 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_278'] = x_get_indicators__mutmut_278 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_279'] = x_get_indicators__mutmut_279 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_280'] = x_get_indicators__mutmut_280 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_281'] = x_get_indicators__mutmut_281 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_282'] = x_get_indicators__mutmut_282 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_283'] = x_get_indicators__mutmut_283 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_284'] = x_get_indicators__mutmut_284 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_285'] = x_get_indicators__mutmut_285 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_286'] = x_get_indicators__mutmut_286 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_287'] = x_get_indicators__mutmut_287 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_288'] = x_get_indicators__mutmut_288 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_289'] = x_get_indicators__mutmut_289 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_290'] = x_get_indicators__mutmut_290 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_291'] = x_get_indicators__mutmut_291 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_292'] = x_get_indicators__mutmut_292 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_293'] = x_get_indicators__mutmut_293 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_294'] = x_get_indicators__mutmut_294 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_295'] = x_get_indicators__mutmut_295 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_296'] = x_get_indicators__mutmut_296 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_297'] = x_get_indicators__mutmut_297 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_298'] = x_get_indicators__mutmut_298 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_299'] = x_get_indicators__mutmut_299 # type: ignore # mutmut generated
mutants_x_get_indicators__mutmut['x_get_indicators__mutmut_300'] = x_get_indicators__mutmut_300 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_weekly_indicators__mutmut)
async def get_weekly_indicators(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_orig(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_1(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = None
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_2(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache or now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_3(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker not in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_4(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now + _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_5(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["XXtsXX"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_6(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["TS"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_7(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] <= _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_8(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["XXdataXX"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_9(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["DATA"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_10(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = None
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_11(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_12(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = None
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_13(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "XXapiKeyXX": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_14(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apikey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_15(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "APIKEY": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_16(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "XXtimespanXX": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_17(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "TIMESPAN": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_18(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "XXweekXX",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_19(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "WEEK",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_20(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "XXseries_typeXX": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_21(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "SERIES_TYPE": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_22(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "XXcloseXX",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_23(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "CLOSE",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_24(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "XXadjustedXX": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_25(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "ADJUSTED": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_26(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "XXtrueXX",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_27(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "TRUE",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_28(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "XXlimitXX": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_29(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "LIMIT": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_30(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 4,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_31(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "XXorderXX": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_32(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "ORDER": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_33(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "XXdescXX",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_34(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "DESC",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_35(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = None
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_36(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.lower()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_37(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = None

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_38(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = None

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_39(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                None,
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_40(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                None,
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_41(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_42(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_43(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(None, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_44(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, None, {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_45(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", None),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_46(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_47(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_48(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", ),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_49(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "XXwindowXX": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_50(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "WINDOW": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_51(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 15}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_52(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(None, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_53(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, None, {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_54(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", None),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_55(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_56(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_57(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", ),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_58(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "XXwindowXX": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_59(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "WINDOW": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_60(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 21}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_61(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = None
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_62(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["XXweekly_rsiXX"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_63(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["WEEKLY_RSI"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_64(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(None, 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_65(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), None)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_66(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_67(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), )
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_68(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(None), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_69(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get(None, 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_70(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", None)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_71(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get(0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_72(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", )), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_73(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[1].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_74(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("XXvalueXX", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_75(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("VALUE", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_76(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 1)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_77(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 3)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_78(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = None

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_79(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["XXweekly_sma20XX"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_80(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["WEEKLY_SMA20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_81(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(None, 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_82(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), None)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_83(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_84(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), )

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_85(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(None), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_86(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get(None, 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_87(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", None)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_88(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get(0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_89(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", )), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_90(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[1].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_91(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("XXvalueXX", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_92(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("VALUE", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_93(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 1)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_94(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 3)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_95(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(None)

    _weekly_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_96(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = None
    return result


async def x_get_weekly_indicators__mutmut_97(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"XXdataXX": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_98(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"DATA": result, "ts": now}
    return result


async def x_get_weekly_indicators__mutmut_99(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "XXtsXX": now}
    return result


async def x_get_weekly_indicators__mutmut_100(ticker: str) -> dict:
    """
    Fetch weekly RSI(14) and SMA(20) for double-timeframe confirmation.
    Weekly RSI < 40 AND daily RSI < 35 = double-confirmed oversold (72% win rate).
    Cache: 1 hour (weekly bars only change once per week).
    """
    now = time.time()
    if ticker in _weekly_cache and now - _weekly_cache[ticker]["ts"] < _WEEKLY_TTL:
        return _weekly_cache[ticker]["data"]

    key = _get_key()
    if not key:
        return {}

    weekly_params = {
        "apiKey": key,
        "timespan": "week",
        "series_type": "close",
        "adjusted": "true",
        "limit": 3,
        "order": "desc",
    }
    t = ticker.upper()
    result: dict = {}

    try:
        async with shared_session() as session:
            rsi_w, sma20_w = await asyncio.gather(
                _fetch_indicator(session, f"v1/indicators/rsi/{t}", {**weekly_params, "window": 14}),
                _fetch_indicator(session, f"v1/indicators/sma/{t}", {**weekly_params, "window": 20}),
            )

        if rsi_w:
            result["weekly_rsi"] = round(float(rsi_w[0].get("value", 0)), 2)
        if sma20_w:
            result["weekly_sma20"] = round(float(sma20_w[0].get("value", 0)), 2)

    except Exception as e:
        log.debug(f"[polygon_indicators] weekly {ticker}: {e}")

    _weekly_cache[ticker] = {"data": result, "TS": now}
    return result

mutants_x_get_weekly_indicators__mutmut['_mutmut_orig'] = x_get_weekly_indicators__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_1'] = x_get_weekly_indicators__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_2'] = x_get_weekly_indicators__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_3'] = x_get_weekly_indicators__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_4'] = x_get_weekly_indicators__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_5'] = x_get_weekly_indicators__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_6'] = x_get_weekly_indicators__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_7'] = x_get_weekly_indicators__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_8'] = x_get_weekly_indicators__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_9'] = x_get_weekly_indicators__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_10'] = x_get_weekly_indicators__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_11'] = x_get_weekly_indicators__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_12'] = x_get_weekly_indicators__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_13'] = x_get_weekly_indicators__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_14'] = x_get_weekly_indicators__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_15'] = x_get_weekly_indicators__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_16'] = x_get_weekly_indicators__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_17'] = x_get_weekly_indicators__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_18'] = x_get_weekly_indicators__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_19'] = x_get_weekly_indicators__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_20'] = x_get_weekly_indicators__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_21'] = x_get_weekly_indicators__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_22'] = x_get_weekly_indicators__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_23'] = x_get_weekly_indicators__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_24'] = x_get_weekly_indicators__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_25'] = x_get_weekly_indicators__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_26'] = x_get_weekly_indicators__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_27'] = x_get_weekly_indicators__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_28'] = x_get_weekly_indicators__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_29'] = x_get_weekly_indicators__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_30'] = x_get_weekly_indicators__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_31'] = x_get_weekly_indicators__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_32'] = x_get_weekly_indicators__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_33'] = x_get_weekly_indicators__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_34'] = x_get_weekly_indicators__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_35'] = x_get_weekly_indicators__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_36'] = x_get_weekly_indicators__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_37'] = x_get_weekly_indicators__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_38'] = x_get_weekly_indicators__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_39'] = x_get_weekly_indicators__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_40'] = x_get_weekly_indicators__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_41'] = x_get_weekly_indicators__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_42'] = x_get_weekly_indicators__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_43'] = x_get_weekly_indicators__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_44'] = x_get_weekly_indicators__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_45'] = x_get_weekly_indicators__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_46'] = x_get_weekly_indicators__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_47'] = x_get_weekly_indicators__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_48'] = x_get_weekly_indicators__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_49'] = x_get_weekly_indicators__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_50'] = x_get_weekly_indicators__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_51'] = x_get_weekly_indicators__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_52'] = x_get_weekly_indicators__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_53'] = x_get_weekly_indicators__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_54'] = x_get_weekly_indicators__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_55'] = x_get_weekly_indicators__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_56'] = x_get_weekly_indicators__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_57'] = x_get_weekly_indicators__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_58'] = x_get_weekly_indicators__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_59'] = x_get_weekly_indicators__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_60'] = x_get_weekly_indicators__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_61'] = x_get_weekly_indicators__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_62'] = x_get_weekly_indicators__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_63'] = x_get_weekly_indicators__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_64'] = x_get_weekly_indicators__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_65'] = x_get_weekly_indicators__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_66'] = x_get_weekly_indicators__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_67'] = x_get_weekly_indicators__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_68'] = x_get_weekly_indicators__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_69'] = x_get_weekly_indicators__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_70'] = x_get_weekly_indicators__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_71'] = x_get_weekly_indicators__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_72'] = x_get_weekly_indicators__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_73'] = x_get_weekly_indicators__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_74'] = x_get_weekly_indicators__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_75'] = x_get_weekly_indicators__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_76'] = x_get_weekly_indicators__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_77'] = x_get_weekly_indicators__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_78'] = x_get_weekly_indicators__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_79'] = x_get_weekly_indicators__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_80'] = x_get_weekly_indicators__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_81'] = x_get_weekly_indicators__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_82'] = x_get_weekly_indicators__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_83'] = x_get_weekly_indicators__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_84'] = x_get_weekly_indicators__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_85'] = x_get_weekly_indicators__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_86'] = x_get_weekly_indicators__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_87'] = x_get_weekly_indicators__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_88'] = x_get_weekly_indicators__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_89'] = x_get_weekly_indicators__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_90'] = x_get_weekly_indicators__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_91'] = x_get_weekly_indicators__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_92'] = x_get_weekly_indicators__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_93'] = x_get_weekly_indicators__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_94'] = x_get_weekly_indicators__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_95'] = x_get_weekly_indicators__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_96'] = x_get_weekly_indicators__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_97'] = x_get_weekly_indicators__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_98'] = x_get_weekly_indicators__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_99'] = x_get_weekly_indicators__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_weekly_indicators__mutmut['x_get_weekly_indicators__mutmut_100'] = x_get_weekly_indicators__mutmut_100 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_blend_rsi__mutmut)
def blend_rsi(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(0.6 * polygon_rsi + 0.4 * pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_orig(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(0.6 * polygon_rsi + 0.4 * pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_1(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None or polygon_rsi is not None:
        return round(0.6 * polygon_rsi + 0.4 * pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_2(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is None and polygon_rsi is not None:
        return round(0.6 * polygon_rsi + 0.4 * pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_3(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is None:
        return round(0.6 * polygon_rsi + 0.4 * pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_4(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(None, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_5(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(0.6 * polygon_rsi + 0.4 * pandas_rsi, None)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_6(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_7(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(0.6 * polygon_rsi + 0.4 * pandas_rsi, )
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_8(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(0.6 * polygon_rsi - 0.4 * pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_9(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(0.6 / polygon_rsi + 0.4 * pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_10(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(1.6 * polygon_rsi + 0.4 * pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_11(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(0.6 * polygon_rsi + 0.4 / pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_12(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(0.6 * polygon_rsi + 1.4 * pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_13(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(0.6 * polygon_rsi + 0.4 * pandas_rsi, 3)
    return polygon_rsi if polygon_rsi is not None else pandas_rsi


def x_blend_rsi__mutmut_14(pandas_rsi: float | None, polygon_rsi: float | None) -> float | None:
    """
    Blend pandas-computed RSI with Polygon pre-computed RSI.
    If both available: 60% Polygon (Wilder's standard) / 40% pandas.
    """
    if pandas_rsi is not None and polygon_rsi is not None:
        return round(0.6 * polygon_rsi + 0.4 * pandas_rsi, 2)
    return polygon_rsi if polygon_rsi is None else pandas_rsi

mutants_x_blend_rsi__mutmut['_mutmut_orig'] = x_blend_rsi__mutmut_orig # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_1'] = x_blend_rsi__mutmut_1 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_2'] = x_blend_rsi__mutmut_2 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_3'] = x_blend_rsi__mutmut_3 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_4'] = x_blend_rsi__mutmut_4 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_5'] = x_blend_rsi__mutmut_5 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_6'] = x_blend_rsi__mutmut_6 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_7'] = x_blend_rsi__mutmut_7 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_8'] = x_blend_rsi__mutmut_8 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_9'] = x_blend_rsi__mutmut_9 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_10'] = x_blend_rsi__mutmut_10 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_11'] = x_blend_rsi__mutmut_11 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_12'] = x_blend_rsi__mutmut_12 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_13'] = x_blend_rsi__mutmut_13 # type: ignore # mutmut generated
mutants_x_blend_rsi__mutmut['x_blend_rsi__mutmut_14'] = x_blend_rsi__mutmut_14 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_polygon_sma_crossover__mutmut)
def polygon_sma_crossover(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_orig(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_1(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = None
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_2(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get(None)
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_3(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("XXsma200XX")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_4(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("SMA200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_5(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = None
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_6(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get(None)
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_7(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("XXsma50XX")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_8(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("SMA50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_9(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = None
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_10(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get(None)
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_11(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("XXsma20XX")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_12(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("SMA20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_13(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_14(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any(None):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_15(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "XXabove_200XX": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_16(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "ABOVE_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_17(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price >= sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_18(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "XXabove_50XX": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_19(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "ABOVE_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_20(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price >= sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_21(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "XXabove_20XX": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_22(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "ABOVE_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_23(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price >= sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_24(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "XXgolden_cross_setupXX": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_25(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "GOLDEN_CROSS_SETUP": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_26(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 or sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_27(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 or sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_28(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 >= sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_29(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 / 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_30(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 1.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_31(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 or sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_32(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "XXdeath_cross_setupXX": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_33(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "DEATH_CROSS_SETUP": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_34(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 or sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_35(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 or sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_36(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 <= sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_37(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 / 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_38(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 2.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_39(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 or sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_40(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "XXpct_from_200XX": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_41(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "PCT_FROM_200": round((price - sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_42(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round(None, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_43(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, None) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_44(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round(2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_45(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, ) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_46(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 / 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_47(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) * sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_48(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price + sma200) / sma200 * 100, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_49(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 101, 2) if sma200 else None,
    }


def x_polygon_sma_crossover__mutmut_50(indicators: dict, price: float) -> dict:
    """Derive SMA-based signals from Polygon pre-computed values."""
    sma200 = indicators.get("sma200")
    sma50 = indicators.get("sma50")
    sma20 = indicators.get("sma20")
    if not any([sma200, sma50, sma20]):
        return {}
    return {
        "above_200": price > sma200 if sma200 else None,
        "above_50": price > sma50 if sma50 else None,
        "above_20": price > sma20 if sma20 else None,
        "golden_cross_setup": (sma50 and sma200 and sma50 > sma200 * 0.99) if (sma50 and sma200) else None,
        "death_cross_setup": (sma50 and sma200 and sma50 < sma200 * 1.01) if (sma50 and sma200) else None,
        "pct_from_200": round((price - sma200) / sma200 * 100, 3) if sma200 else None,
    }

mutants_x_polygon_sma_crossover__mutmut['_mutmut_orig'] = x_polygon_sma_crossover__mutmut_orig # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_1'] = x_polygon_sma_crossover__mutmut_1 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_2'] = x_polygon_sma_crossover__mutmut_2 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_3'] = x_polygon_sma_crossover__mutmut_3 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_4'] = x_polygon_sma_crossover__mutmut_4 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_5'] = x_polygon_sma_crossover__mutmut_5 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_6'] = x_polygon_sma_crossover__mutmut_6 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_7'] = x_polygon_sma_crossover__mutmut_7 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_8'] = x_polygon_sma_crossover__mutmut_8 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_9'] = x_polygon_sma_crossover__mutmut_9 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_10'] = x_polygon_sma_crossover__mutmut_10 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_11'] = x_polygon_sma_crossover__mutmut_11 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_12'] = x_polygon_sma_crossover__mutmut_12 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_13'] = x_polygon_sma_crossover__mutmut_13 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_14'] = x_polygon_sma_crossover__mutmut_14 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_15'] = x_polygon_sma_crossover__mutmut_15 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_16'] = x_polygon_sma_crossover__mutmut_16 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_17'] = x_polygon_sma_crossover__mutmut_17 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_18'] = x_polygon_sma_crossover__mutmut_18 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_19'] = x_polygon_sma_crossover__mutmut_19 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_20'] = x_polygon_sma_crossover__mutmut_20 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_21'] = x_polygon_sma_crossover__mutmut_21 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_22'] = x_polygon_sma_crossover__mutmut_22 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_23'] = x_polygon_sma_crossover__mutmut_23 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_24'] = x_polygon_sma_crossover__mutmut_24 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_25'] = x_polygon_sma_crossover__mutmut_25 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_26'] = x_polygon_sma_crossover__mutmut_26 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_27'] = x_polygon_sma_crossover__mutmut_27 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_28'] = x_polygon_sma_crossover__mutmut_28 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_29'] = x_polygon_sma_crossover__mutmut_29 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_30'] = x_polygon_sma_crossover__mutmut_30 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_31'] = x_polygon_sma_crossover__mutmut_31 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_32'] = x_polygon_sma_crossover__mutmut_32 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_33'] = x_polygon_sma_crossover__mutmut_33 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_34'] = x_polygon_sma_crossover__mutmut_34 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_35'] = x_polygon_sma_crossover__mutmut_35 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_36'] = x_polygon_sma_crossover__mutmut_36 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_37'] = x_polygon_sma_crossover__mutmut_37 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_38'] = x_polygon_sma_crossover__mutmut_38 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_39'] = x_polygon_sma_crossover__mutmut_39 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_40'] = x_polygon_sma_crossover__mutmut_40 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_41'] = x_polygon_sma_crossover__mutmut_41 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_42'] = x_polygon_sma_crossover__mutmut_42 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_43'] = x_polygon_sma_crossover__mutmut_43 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_44'] = x_polygon_sma_crossover__mutmut_44 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_45'] = x_polygon_sma_crossover__mutmut_45 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_46'] = x_polygon_sma_crossover__mutmut_46 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_47'] = x_polygon_sma_crossover__mutmut_47 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_48'] = x_polygon_sma_crossover__mutmut_48 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_49'] = x_polygon_sma_crossover__mutmut_49 # type: ignore # mutmut generated
mutants_x_polygon_sma_crossover__mutmut['x_polygon_sma_crossover__mutmut_50'] = x_polygon_sma_crossover__mutmut_50 # type: ignore # mutmut generated
