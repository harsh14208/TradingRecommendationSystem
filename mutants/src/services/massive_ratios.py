"""
Polygon.io Financial Ratios — using vX/reference/financials (free tier accessible).

Computes FCF yield, gross margin, net margin, debt/equity, revenue growth,
share dilution, 3-year revenue acceleration (annual), dividend yield (TTM),
and dividend aristocrat streak from raw financial statements + dividend history.

Cache: 6 hours (quarterly earnings data).
"""

import logging
import os
import time

import aiohttp
from services.http_client import get_ssl_context, shared_session

log = logging.getLogger("signal.trade.polygon_financials")

_cache: dict[str, dict] = {}
_annual_cache: dict[str, dict] = {}
_dividend_cache: dict[str, dict] = {}
_TTL = 21600  # 6 hours
_DIV_TTL = 86400  # 24 hours — dividend data changes rarely
_BASE = "https://api.polygon.io/vX/reference/financials"


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__val__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__val__mutmut)
def _val(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(key)
    if isinstance(item, dict):
        v = item.get("value")
        return float(v) if v is not None else None
    if item is not None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_orig(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(key)
    if isinstance(item, dict):
        v = item.get("value")
        return float(v) if v is not None else None
    if item is not None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_1(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = None
    if isinstance(item, dict):
        v = item.get("value")
        return float(v) if v is not None else None
    if item is not None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_2(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(None)
    if isinstance(item, dict):
        v = item.get("value")
        return float(v) if v is not None else None
    if item is not None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_3(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(key)
    if isinstance(item, dict):
        v = None
        return float(v) if v is not None else None
    if item is not None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_4(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(key)
    if isinstance(item, dict):
        v = item.get(None)
        return float(v) if v is not None else None
    if item is not None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_5(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(key)
    if isinstance(item, dict):
        v = item.get("XXvalueXX")
        return float(v) if v is not None else None
    if item is not None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_6(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(key)
    if isinstance(item, dict):
        v = item.get("VALUE")
        return float(v) if v is not None else None
    if item is not None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_7(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(key)
    if isinstance(item, dict):
        v = item.get("value")
        return float(None) if v is not None else None
    if item is not None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_8(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(key)
    if isinstance(item, dict):
        v = item.get("value")
        return float(v) if v is None else None
    if item is not None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_9(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(key)
    if isinstance(item, dict):
        v = item.get("value")
        return float(v) if v is not None else None
    if item is None:
        try:
            return float(item)
        except (TypeError, ValueError):
            pass
    return None


def x__val__mutmut_10(section: dict, key: str) -> float | None:
    """Extract numeric value from a Polygon financials section dict."""
    item = section.get(key)
    if isinstance(item, dict):
        v = item.get("value")
        return float(v) if v is not None else None
    if item is not None:
        try:
            return float(None)
        except (TypeError, ValueError):
            pass
    return None

mutants_x__val__mutmut['_mutmut_orig'] = x__val__mutmut_orig # type: ignore # mutmut generated
mutants_x__val__mutmut['x__val__mutmut_1'] = x__val__mutmut_1 # type: ignore # mutmut generated
mutants_x__val__mutmut['x__val__mutmut_2'] = x__val__mutmut_2 # type: ignore # mutmut generated
mutants_x__val__mutmut['x__val__mutmut_3'] = x__val__mutmut_3 # type: ignore # mutmut generated
mutants_x__val__mutmut['x__val__mutmut_4'] = x__val__mutmut_4 # type: ignore # mutmut generated
mutants_x__val__mutmut['x__val__mutmut_5'] = x__val__mutmut_5 # type: ignore # mutmut generated
mutants_x__val__mutmut['x__val__mutmut_6'] = x__val__mutmut_6 # type: ignore # mutmut generated
mutants_x__val__mutmut['x__val__mutmut_7'] = x__val__mutmut_7 # type: ignore # mutmut generated
mutants_x__val__mutmut['x__val__mutmut_8'] = x__val__mutmut_8 # type: ignore # mutmut generated
mutants_x__val__mutmut['x__val__mutmut_9'] = x__val__mutmut_9 # type: ignore # mutmut generated
mutants_x__val__mutmut['x__val__mutmut_10'] = x__val__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_ratios__mutmut)
async def get_ratios(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_orig(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_1(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = None
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_2(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache or now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_3(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker not in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_4(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now + _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_5(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["XXtsXX"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_6(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["TS"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_7(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] <= _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_8(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["XXdataXX"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_9(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["DATA"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_10(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = None
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_11(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv(None)
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_12(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("XXMASSIVE_API_KEYXX")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_13(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("massive_api_key")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_14(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_15(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = None
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_16(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"XXtickerXX": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_17(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"TICKER": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_18(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "XXtimeframeXX": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_19(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "TIMEFRAME": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_20(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "XXquarterlyXX", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_21(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "QUARTERLY", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_22(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "XXlimitXX": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_23(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "LIMIT": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_24(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 3, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_25(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "XXorderXX": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_26(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "ORDER": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_27(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "XXdescXX", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_28(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "DESC", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_29(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "XXapiKeyXX": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_30(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apikey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_31(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "APIKEY": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_32(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = None

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_33(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(None, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_34(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=None, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_35(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=None, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_36(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=None) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_37(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_38(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_39(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_40(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, ) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_41(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=None)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_42(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=11)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_43(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status == 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_44(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 201:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_45(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = None
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_46(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = None
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_47(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") and []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_48(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get(None) or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_49(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("XXresultsXX") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_50(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("RESULTS") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_51(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_52(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(None)
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_53(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = None
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_54(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get(None, {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_55(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", None)
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_56(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get({})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_57(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", )
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_58(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[1].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_59(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("XXfinancialsXX", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_60(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("FINANCIALS", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_61(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = None

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_62(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get(None, {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_63(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", None) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_64(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get({}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_65(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", ) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_66(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[2].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_67(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("XXfinancialsXX", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_68(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("FINANCIALS", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_69(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) >= 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_70(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 2 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_71(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = None
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_72(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get(None, {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_73(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", None)
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_74(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get({})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_75(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", )
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_76(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("XXincome_statementXX", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_77(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("INCOME_STATEMENT", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_78(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = None
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_79(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get(None, {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_80(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", None)
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_81(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get({})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_82(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", )
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_83(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("XXcash_flow_statementXX", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_84(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("CASH_FLOW_STATEMENT", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_85(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = None
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_86(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get(None, {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_87(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", None)
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_88(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get({})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_89(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", )
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_90(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("XXbalance_sheetXX", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_91(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("BALANCE_SHEET", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_92(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = None

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_93(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get(None, {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_94(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", None) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_95(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get({}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_96(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", ) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_97(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("XXincome_statementXX", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_98(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("INCOME_STATEMENT", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_99(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = None
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_100(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(None, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_101(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, None)
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_102(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val("revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_103(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, )
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_104(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "XXrevenuesXX")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_105(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "REVENUES")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_106(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = None
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_107(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(None, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_108(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, None)
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_109(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val("gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_110(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, )
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_111(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "XXgross_profitXX")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_112(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "GROSS_PROFIT")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_113(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = None
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_114(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(None, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_115(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, None)
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_116(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val("income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_117(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, )
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_118(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "XXincome_loss_from_continuing_operations_after_taxXX")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_119(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "INCOME_LOSS_FROM_CONTINUING_OPERATIONS_AFTER_TAX")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_120(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = None
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_121(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") and _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_122(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(None, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_123(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, None) or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_124(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val("net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_125(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, ) or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_126(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "XXnet_cash_flow_from_operating_activities_continuingXX") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_127(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "NET_CASH_FLOW_FROM_OPERATING_ACTIVITIES_CONTINUING") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_128(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        None, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_129(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, None
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_130(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_131(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_132(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "XXnet_cash_flow_from_operating_activitiesXX"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_133(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "NET_CASH_FLOW_FROM_OPERATING_ACTIVITIES"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_134(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = None
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_135(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") and _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_136(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(None, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_137(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, None) or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_138(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val("net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_139(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, ) or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_140(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "XXnet_cash_flow_from_investing_activities_continuingXX") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_141(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "NET_CASH_FLOW_FROM_INVESTING_ACTIVITIES_CONTINUING") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_142(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        None, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_143(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, None
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_144(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_145(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_146(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "XXnet_cash_flow_from_investing_activitiesXX"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_147(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "NET_CASH_FLOW_FROM_INVESTING_ACTIVITIES"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_148(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = None
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_149(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(None, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_150(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, None)
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_151(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val("assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_152(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, )
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_153(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "XXassetsXX")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_154(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "ASSETS")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_155(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = None
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_156(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(None, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_157(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, None)
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_158(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val("current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_159(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, )
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_160(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "XXcurrent_assetsXX")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_161(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "CURRENT_ASSETS")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_162(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = None
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_163(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(None, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_164(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, None)
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_165(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val("current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_166(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, )
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_167(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "XXcurrent_liabilitiesXX")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_168(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "CURRENT_LIABILITIES")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_169(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = None
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_170(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(None, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_171(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, None)
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_172(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val("long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_173(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, )
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_174(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "XXlong_term_debtXX")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_175(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "LONG_TERM_DEBT")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_176(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = ""
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_177(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets or curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_178(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = None

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_179(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets + (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_180(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab - (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_181(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt and 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_182(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 1))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_183(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_184(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf - inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_185(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None or inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_186(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_187(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_188(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_189(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof * revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_190(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof or revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_191(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_192(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income * revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_193(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income or revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_194(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_195(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt * equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_196(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity or equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_197(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt or equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_198(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity >= 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_199(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 1 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_200(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_201(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets * curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_202(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab or curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_203(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets or curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_204(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab >= 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_205(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 1 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_206(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = None
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_207(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(None, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_208(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, None)
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_209(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val("revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_210(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, )
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_211(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "XXrevenuesXX")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_212(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "REVENUES")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_213(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = None

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_214(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) / 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_215(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) * abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_216(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue + prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_217(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(None) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_218(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 101 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_219(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue or prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_220(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue or prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_221(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue == 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_222(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 1 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_223(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = None

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_224(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "XXgross_marginXX": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_225(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "GROSS_MARGIN": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_226(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(None, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_227(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, None) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_228(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_229(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, ) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_230(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin / 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_231(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 101, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_232(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 3) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_233(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_234(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "XXnet_marginXX": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_235(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "NET_MARGIN": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_236(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(None, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_237(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, None) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_238(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_239(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, ) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_240(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin / 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_241(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 101, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_242(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 3) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_243(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_244(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "XXdebt_equityXX": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_245(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "DEBT_EQUITY": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_246(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(None, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_247(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, None) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_248(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_249(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, ) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_250(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 3) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_251(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_252(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "XXcurrent_ratioXX": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_253(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "CURRENT_RATIO": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_254(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(None, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_255(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, None) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_256(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_257(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, ) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_258(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 3) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_259(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_260(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "XXfcf_ttmXX": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_261(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "FCF_TTM": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_262(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(None, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_263(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, None) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_264(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_265(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, ) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_266(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf * 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_267(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1000001.0, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_268(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 2) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_269(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_270(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "XXrevenue_qoqXX": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_271(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "REVENUE_QOQ": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_272(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(None, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_273(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, None) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_274(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_275(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, ) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_276(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 2) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_277(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_278(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_279(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = None
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_280(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"XXdataXX": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_281(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"DATA": result, "ts": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_282(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "XXtsXX": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_283(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "TS": now}
    log.debug(f"[polygon_financials] {ticker}: {result}")
    return result


async def x_get_ratios__mutmut_284(ticker: str) -> dict:
    """
    Fetch the two most recent quarterly filings and compute key ratios.
    Returns dict compatible with signal_engine's fundamentals schema.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "quarterly", "limit": 2, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if not results:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_financials] {ticker}: {e}")
        return {}

    latest = results[0].get("financials", {})
    prev = results[1].get("financials", {}) if len(results) > 1 else {}

    inc = latest.get("income_statement", {})
    cf = latest.get("cash_flow_statement", {})
    bs = latest.get("balance_sheet", {})
    inc0 = prev.get("income_statement", {}) if prev else {}

    revenue = _val(inc, "revenues")
    gross_prof = _val(inc, "gross_profit")
    net_income = _val(inc, "income_loss_from_continuing_operations_after_tax")
    op_cf = _val(cf, "net_cash_flow_from_operating_activities_continuing") or _val(
        cf, "net_cash_flow_from_operating_activities"
    )
    inv_cf = _val(cf, "net_cash_flow_from_investing_activities_continuing") or _val(
        cf, "net_cash_flow_from_investing_activities"
    )
    total_assets = _val(bs, "assets")
    curr_assets = _val(bs, "current_assets")
    curr_liab = _val(bs, "current_liabilities")
    lt_debt = _val(bs, "long_term_debt")
    equity = None
    if total_assets and curr_liab:
        equity = total_assets - (curr_liab + (lt_debt or 0))

    # Derived ratios
    fcf = (op_cf + inv_cf) if (op_cf is not None and inv_cf is not None) else None
    gross_margin = (gross_prof / revenue) if gross_prof and revenue else None
    net_margin = (net_income / revenue) if net_income and revenue else None
    debt_equity = (lt_debt / equity) if lt_debt and equity and equity > 0 else None
    current_ratio = (curr_assets / curr_liab) if curr_assets and curr_liab and curr_liab > 0 else None

    # Revenue QoQ growth
    prev_revenue = _val(inc0, "revenues")
    revenue_growth = (
        (revenue - prev_revenue) / abs(prev_revenue) * 100 if revenue and prev_revenue and prev_revenue != 0 else None
    )

    result = {
        k: v
        for k, v in {
            "gross_margin": round(gross_margin * 100, 2) if gross_margin is not None else None,
            "net_margin": round(net_margin * 100, 2) if net_margin is not None else None,
            "debt_equity": round(debt_equity, 2) if debt_equity is not None else None,
            "current_ratio": round(current_ratio, 2) if current_ratio is not None else None,
            "fcf_ttm": round(fcf / 1e6, 1) if fcf is not None else None,
            "revenue_qoq": round(revenue_growth, 1) if revenue_growth is not None else None,
        }.items()
        if v is not None
    }

    _cache[ticker] = {"data": result, "ts": now}
    log.debug(None)
    return result

mutants_x_get_ratios__mutmut['_mutmut_orig'] = x_get_ratios__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_1'] = x_get_ratios__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_2'] = x_get_ratios__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_3'] = x_get_ratios__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_4'] = x_get_ratios__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_5'] = x_get_ratios__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_6'] = x_get_ratios__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_7'] = x_get_ratios__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_8'] = x_get_ratios__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_9'] = x_get_ratios__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_10'] = x_get_ratios__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_11'] = x_get_ratios__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_12'] = x_get_ratios__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_13'] = x_get_ratios__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_14'] = x_get_ratios__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_15'] = x_get_ratios__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_16'] = x_get_ratios__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_17'] = x_get_ratios__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_18'] = x_get_ratios__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_19'] = x_get_ratios__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_20'] = x_get_ratios__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_21'] = x_get_ratios__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_22'] = x_get_ratios__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_23'] = x_get_ratios__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_24'] = x_get_ratios__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_25'] = x_get_ratios__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_26'] = x_get_ratios__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_27'] = x_get_ratios__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_28'] = x_get_ratios__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_29'] = x_get_ratios__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_30'] = x_get_ratios__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_31'] = x_get_ratios__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_32'] = x_get_ratios__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_33'] = x_get_ratios__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_34'] = x_get_ratios__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_35'] = x_get_ratios__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_36'] = x_get_ratios__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_37'] = x_get_ratios__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_38'] = x_get_ratios__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_39'] = x_get_ratios__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_40'] = x_get_ratios__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_41'] = x_get_ratios__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_42'] = x_get_ratios__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_43'] = x_get_ratios__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_44'] = x_get_ratios__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_45'] = x_get_ratios__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_46'] = x_get_ratios__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_47'] = x_get_ratios__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_48'] = x_get_ratios__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_49'] = x_get_ratios__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_50'] = x_get_ratios__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_51'] = x_get_ratios__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_52'] = x_get_ratios__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_53'] = x_get_ratios__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_54'] = x_get_ratios__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_55'] = x_get_ratios__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_56'] = x_get_ratios__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_57'] = x_get_ratios__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_58'] = x_get_ratios__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_59'] = x_get_ratios__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_60'] = x_get_ratios__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_61'] = x_get_ratios__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_62'] = x_get_ratios__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_63'] = x_get_ratios__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_64'] = x_get_ratios__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_65'] = x_get_ratios__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_66'] = x_get_ratios__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_67'] = x_get_ratios__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_68'] = x_get_ratios__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_69'] = x_get_ratios__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_70'] = x_get_ratios__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_71'] = x_get_ratios__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_72'] = x_get_ratios__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_73'] = x_get_ratios__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_74'] = x_get_ratios__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_75'] = x_get_ratios__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_76'] = x_get_ratios__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_77'] = x_get_ratios__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_78'] = x_get_ratios__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_79'] = x_get_ratios__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_80'] = x_get_ratios__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_81'] = x_get_ratios__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_82'] = x_get_ratios__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_83'] = x_get_ratios__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_84'] = x_get_ratios__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_85'] = x_get_ratios__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_86'] = x_get_ratios__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_87'] = x_get_ratios__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_88'] = x_get_ratios__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_89'] = x_get_ratios__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_90'] = x_get_ratios__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_91'] = x_get_ratios__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_92'] = x_get_ratios__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_93'] = x_get_ratios__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_94'] = x_get_ratios__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_95'] = x_get_ratios__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_96'] = x_get_ratios__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_97'] = x_get_ratios__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_98'] = x_get_ratios__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_99'] = x_get_ratios__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_100'] = x_get_ratios__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_101'] = x_get_ratios__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_102'] = x_get_ratios__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_103'] = x_get_ratios__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_104'] = x_get_ratios__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_105'] = x_get_ratios__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_106'] = x_get_ratios__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_107'] = x_get_ratios__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_108'] = x_get_ratios__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_109'] = x_get_ratios__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_110'] = x_get_ratios__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_111'] = x_get_ratios__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_112'] = x_get_ratios__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_113'] = x_get_ratios__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_114'] = x_get_ratios__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_115'] = x_get_ratios__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_116'] = x_get_ratios__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_117'] = x_get_ratios__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_118'] = x_get_ratios__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_119'] = x_get_ratios__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_120'] = x_get_ratios__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_121'] = x_get_ratios__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_122'] = x_get_ratios__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_123'] = x_get_ratios__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_124'] = x_get_ratios__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_125'] = x_get_ratios__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_126'] = x_get_ratios__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_127'] = x_get_ratios__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_128'] = x_get_ratios__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_129'] = x_get_ratios__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_130'] = x_get_ratios__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_131'] = x_get_ratios__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_132'] = x_get_ratios__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_133'] = x_get_ratios__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_134'] = x_get_ratios__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_135'] = x_get_ratios__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_136'] = x_get_ratios__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_137'] = x_get_ratios__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_138'] = x_get_ratios__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_139'] = x_get_ratios__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_140'] = x_get_ratios__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_141'] = x_get_ratios__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_142'] = x_get_ratios__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_143'] = x_get_ratios__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_144'] = x_get_ratios__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_145'] = x_get_ratios__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_146'] = x_get_ratios__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_147'] = x_get_ratios__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_148'] = x_get_ratios__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_149'] = x_get_ratios__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_150'] = x_get_ratios__mutmut_150 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_151'] = x_get_ratios__mutmut_151 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_152'] = x_get_ratios__mutmut_152 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_153'] = x_get_ratios__mutmut_153 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_154'] = x_get_ratios__mutmut_154 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_155'] = x_get_ratios__mutmut_155 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_156'] = x_get_ratios__mutmut_156 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_157'] = x_get_ratios__mutmut_157 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_158'] = x_get_ratios__mutmut_158 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_159'] = x_get_ratios__mutmut_159 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_160'] = x_get_ratios__mutmut_160 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_161'] = x_get_ratios__mutmut_161 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_162'] = x_get_ratios__mutmut_162 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_163'] = x_get_ratios__mutmut_163 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_164'] = x_get_ratios__mutmut_164 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_165'] = x_get_ratios__mutmut_165 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_166'] = x_get_ratios__mutmut_166 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_167'] = x_get_ratios__mutmut_167 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_168'] = x_get_ratios__mutmut_168 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_169'] = x_get_ratios__mutmut_169 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_170'] = x_get_ratios__mutmut_170 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_171'] = x_get_ratios__mutmut_171 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_172'] = x_get_ratios__mutmut_172 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_173'] = x_get_ratios__mutmut_173 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_174'] = x_get_ratios__mutmut_174 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_175'] = x_get_ratios__mutmut_175 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_176'] = x_get_ratios__mutmut_176 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_177'] = x_get_ratios__mutmut_177 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_178'] = x_get_ratios__mutmut_178 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_179'] = x_get_ratios__mutmut_179 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_180'] = x_get_ratios__mutmut_180 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_181'] = x_get_ratios__mutmut_181 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_182'] = x_get_ratios__mutmut_182 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_183'] = x_get_ratios__mutmut_183 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_184'] = x_get_ratios__mutmut_184 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_185'] = x_get_ratios__mutmut_185 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_186'] = x_get_ratios__mutmut_186 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_187'] = x_get_ratios__mutmut_187 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_188'] = x_get_ratios__mutmut_188 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_189'] = x_get_ratios__mutmut_189 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_190'] = x_get_ratios__mutmut_190 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_191'] = x_get_ratios__mutmut_191 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_192'] = x_get_ratios__mutmut_192 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_193'] = x_get_ratios__mutmut_193 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_194'] = x_get_ratios__mutmut_194 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_195'] = x_get_ratios__mutmut_195 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_196'] = x_get_ratios__mutmut_196 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_197'] = x_get_ratios__mutmut_197 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_198'] = x_get_ratios__mutmut_198 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_199'] = x_get_ratios__mutmut_199 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_200'] = x_get_ratios__mutmut_200 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_201'] = x_get_ratios__mutmut_201 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_202'] = x_get_ratios__mutmut_202 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_203'] = x_get_ratios__mutmut_203 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_204'] = x_get_ratios__mutmut_204 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_205'] = x_get_ratios__mutmut_205 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_206'] = x_get_ratios__mutmut_206 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_207'] = x_get_ratios__mutmut_207 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_208'] = x_get_ratios__mutmut_208 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_209'] = x_get_ratios__mutmut_209 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_210'] = x_get_ratios__mutmut_210 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_211'] = x_get_ratios__mutmut_211 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_212'] = x_get_ratios__mutmut_212 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_213'] = x_get_ratios__mutmut_213 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_214'] = x_get_ratios__mutmut_214 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_215'] = x_get_ratios__mutmut_215 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_216'] = x_get_ratios__mutmut_216 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_217'] = x_get_ratios__mutmut_217 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_218'] = x_get_ratios__mutmut_218 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_219'] = x_get_ratios__mutmut_219 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_220'] = x_get_ratios__mutmut_220 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_221'] = x_get_ratios__mutmut_221 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_222'] = x_get_ratios__mutmut_222 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_223'] = x_get_ratios__mutmut_223 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_224'] = x_get_ratios__mutmut_224 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_225'] = x_get_ratios__mutmut_225 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_226'] = x_get_ratios__mutmut_226 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_227'] = x_get_ratios__mutmut_227 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_228'] = x_get_ratios__mutmut_228 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_229'] = x_get_ratios__mutmut_229 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_230'] = x_get_ratios__mutmut_230 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_231'] = x_get_ratios__mutmut_231 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_232'] = x_get_ratios__mutmut_232 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_233'] = x_get_ratios__mutmut_233 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_234'] = x_get_ratios__mutmut_234 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_235'] = x_get_ratios__mutmut_235 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_236'] = x_get_ratios__mutmut_236 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_237'] = x_get_ratios__mutmut_237 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_238'] = x_get_ratios__mutmut_238 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_239'] = x_get_ratios__mutmut_239 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_240'] = x_get_ratios__mutmut_240 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_241'] = x_get_ratios__mutmut_241 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_242'] = x_get_ratios__mutmut_242 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_243'] = x_get_ratios__mutmut_243 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_244'] = x_get_ratios__mutmut_244 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_245'] = x_get_ratios__mutmut_245 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_246'] = x_get_ratios__mutmut_246 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_247'] = x_get_ratios__mutmut_247 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_248'] = x_get_ratios__mutmut_248 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_249'] = x_get_ratios__mutmut_249 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_250'] = x_get_ratios__mutmut_250 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_251'] = x_get_ratios__mutmut_251 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_252'] = x_get_ratios__mutmut_252 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_253'] = x_get_ratios__mutmut_253 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_254'] = x_get_ratios__mutmut_254 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_255'] = x_get_ratios__mutmut_255 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_256'] = x_get_ratios__mutmut_256 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_257'] = x_get_ratios__mutmut_257 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_258'] = x_get_ratios__mutmut_258 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_259'] = x_get_ratios__mutmut_259 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_260'] = x_get_ratios__mutmut_260 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_261'] = x_get_ratios__mutmut_261 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_262'] = x_get_ratios__mutmut_262 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_263'] = x_get_ratios__mutmut_263 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_264'] = x_get_ratios__mutmut_264 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_265'] = x_get_ratios__mutmut_265 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_266'] = x_get_ratios__mutmut_266 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_267'] = x_get_ratios__mutmut_267 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_268'] = x_get_ratios__mutmut_268 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_269'] = x_get_ratios__mutmut_269 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_270'] = x_get_ratios__mutmut_270 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_271'] = x_get_ratios__mutmut_271 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_272'] = x_get_ratios__mutmut_272 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_273'] = x_get_ratios__mutmut_273 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_274'] = x_get_ratios__mutmut_274 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_275'] = x_get_ratios__mutmut_275 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_276'] = x_get_ratios__mutmut_276 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_277'] = x_get_ratios__mutmut_277 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_278'] = x_get_ratios__mutmut_278 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_279'] = x_get_ratios__mutmut_279 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_280'] = x_get_ratios__mutmut_280 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_281'] = x_get_ratios__mutmut_281 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_282'] = x_get_ratios__mutmut_282 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_283'] = x_get_ratios__mutmut_283 # type: ignore # mutmut generated
mutants_x_get_ratios__mutmut['x_get_ratios__mutmut_284'] = x_get_ratios__mutmut_284 # type: ignore # mutmut generated
mutants_x_merge_with_yfinance__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_merge_with_yfinance__mutmut)
def merge_with_yfinance(yf_fundamentals: dict, massive_ratios: dict) -> dict:
    """Polygon values take priority; yfinance fills any remaining gaps."""
    merged = dict(yf_fundamentals)
    for k, v in massive_ratios.items():
        if v is not None:
            merged[k] = v
    return merged


def x_merge_with_yfinance__mutmut_orig(yf_fundamentals: dict, massive_ratios: dict) -> dict:
    """Polygon values take priority; yfinance fills any remaining gaps."""
    merged = dict(yf_fundamentals)
    for k, v in massive_ratios.items():
        if v is not None:
            merged[k] = v
    return merged


def x_merge_with_yfinance__mutmut_1(yf_fundamentals: dict, massive_ratios: dict) -> dict:
    """Polygon values take priority; yfinance fills any remaining gaps."""
    merged = None
    for k, v in massive_ratios.items():
        if v is not None:
            merged[k] = v
    return merged


def x_merge_with_yfinance__mutmut_2(yf_fundamentals: dict, massive_ratios: dict) -> dict:
    """Polygon values take priority; yfinance fills any remaining gaps."""
    merged = dict(None)
    for k, v in massive_ratios.items():
        if v is not None:
            merged[k] = v
    return merged


def x_merge_with_yfinance__mutmut_3(yf_fundamentals: dict, massive_ratios: dict) -> dict:
    """Polygon values take priority; yfinance fills any remaining gaps."""
    merged = dict(yf_fundamentals)
    for k, v in massive_ratios.items():
        if v is None:
            merged[k] = v
    return merged


def x_merge_with_yfinance__mutmut_4(yf_fundamentals: dict, massive_ratios: dict) -> dict:
    """Polygon values take priority; yfinance fills any remaining gaps."""
    merged = dict(yf_fundamentals)
    for k, v in massive_ratios.items():
        if v is not None:
            merged[k] = None
    return merged

mutants_x_merge_with_yfinance__mutmut['_mutmut_orig'] = x_merge_with_yfinance__mutmut_orig # type: ignore # mutmut generated
mutants_x_merge_with_yfinance__mutmut['x_merge_with_yfinance__mutmut_1'] = x_merge_with_yfinance__mutmut_1 # type: ignore # mutmut generated
mutants_x_merge_with_yfinance__mutmut['x_merge_with_yfinance__mutmut_2'] = x_merge_with_yfinance__mutmut_2 # type: ignore # mutmut generated
mutants_x_merge_with_yfinance__mutmut['x_merge_with_yfinance__mutmut_3'] = x_merge_with_yfinance__mutmut_3 # type: ignore # mutmut generated
mutants_x_merge_with_yfinance__mutmut['x_merge_with_yfinance__mutmut_4'] = x_merge_with_yfinance__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_annual_revenue_acceleration__mutmut)
async def get_annual_revenue_acceleration(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_orig(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_1(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = None
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_2(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache or now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_3(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker not in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_4(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now + _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_5(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["XXtsXX"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_6(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["TS"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_7(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] <= _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_8(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["XXdataXX"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_9(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["DATA"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_10(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = None
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_11(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") and ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_12(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") and os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_13(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv(None) or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_14(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("XXMASSIVE_API_KEYXX") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_15(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("massive_api_key") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_16(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv(None) or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_17(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("XXPOLYGON_API_KEYXX") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_18(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("polygon_api_key") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_19(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or "XXXX"
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_20(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_21(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = None
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_22(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"XXtickerXX": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_23(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"TICKER": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_24(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "XXtimeframeXX": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_25(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "TIMEFRAME": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_26(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "XXannualXX", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_27(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "ANNUAL", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_28(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "XXlimitXX": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_29(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "LIMIT": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_30(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 5, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_31(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "XXorderXX": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_32(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "ORDER": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_33(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "XXdescXX", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_34(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "DESC", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_35(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "XXapiKeyXX": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_36(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apikey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_37(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "APIKEY": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_38(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = None

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_39(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(None, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_40(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=None, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_41(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=None, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_42(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=None) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_43(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_44(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_45(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_46(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, ) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_47(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=None)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_48(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=11)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_49(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status == 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_50(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 201:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_51(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = None
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_52(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = None
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_53(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") and []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_54(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get(None) or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_55(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("XXresultsXX") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_56(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("RESULTS") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_57(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) <= 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_58(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 5:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_59(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(None)
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_60(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = None
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_61(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = None
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_62(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get(None, {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_63(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", None)
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_64(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get({})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_65(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", )
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_66(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get(None, {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_67(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", None).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_68(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get({}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_69(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", ).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_70(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("XXfinancialsXX", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_71(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("FINANCIALS", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_72(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("XXincome_statementXX", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_73(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("INCOME_STATEMENT", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_74(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = None
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_75(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(None, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_76(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, None)
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_77(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val("revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_78(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, )
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_79(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "XXrevenuesXX")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_80(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "REVENUES")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_81(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_82(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(None)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_83(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) <= 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_84(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 5:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_85(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = None
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_86(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(None):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_87(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(4):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_88(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = None
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_89(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i - 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_90(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 2]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_91(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev or prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_92(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev == 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_93(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 1:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_94(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(None)

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_95(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round(None, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_96(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, None))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_97(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round(1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_98(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, ))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_99(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) / 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_100(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) * abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_101(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr + prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_102(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(None) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_103(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 101, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_104(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 2))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_105(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) <= 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_106(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 4:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_107(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = None
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_108(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = None
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_109(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(None)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_110(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g >= 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_111(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 1 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_112(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = None
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_113(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[1] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_114(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] >= growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_115(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[2] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_116(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] >= growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_117(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[3]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_118(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = None
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_119(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive or accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_120(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = None

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_121(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "XXrevenue_torpedoXX": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_122(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "REVENUE_TORPEDO": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_123(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "XXannual_rev_growthXX": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_124(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "ANNUAL_REV_GROWTH": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_125(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = None
    return result


async def x_get_annual_revenue_acceleration__mutmut_126(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"XXdataXX": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_127(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"DATA": result, "ts": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_128(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "XXtsXX": now}
    return result


async def x_get_annual_revenue_acceleration__mutmut_129(ticker: str) -> dict:
    """
    Fetch 3 years of annual revenue from vX/reference/financials?timeframe=annual.
    Returns:
      revenue_torpedo: True if 3 consecutive years of positive and accelerating YoY growth
      annual_rev_growth: list of [yr1_pct, yr2_pct, yr3_pct] (newest first)
    Driehaus/O'Neil research: this pattern precedes institutional accumulation in 68% of cases.
    """
    now = time.time()
    if ticker in _annual_cache and now - _annual_cache[ticker]["ts"] < _TTL:
        return _annual_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    params = {"ticker": ticker, "timeframe": "annual", "limit": 4, "order": "desc", "apiKey": api_key}
    ssl_ctx = get_ssl_context()

    try:
        async with shared_session() as session:
            async with session.get(_BASE, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                results = data.get("results") or []
                if len(results) < 4:
                    return {}
    except Exception as e:
        log.debug(f"[polygon_annual] {ticker}: {e}")
        return {}

    revenues = []
    for r in results:
        inc = r.get("financials", {}).get("income_statement", {})
        rev = _val(inc, "revenues")
        if rev is not None:
            revenues.append(rev)

    if len(revenues) < 4:
        return {}

    # revenues[0] = most recent year, revenues[3] = 3 years ago
    growths = []
    for i in range(3):
        curr, prev = revenues[i], revenues[i + 1]
        if prev and prev != 0:
            growths.append(round((curr - prev) / abs(prev) * 100, 1))

    if len(growths) < 3:
        result = {}
    else:
        # Torpedo: all positive AND each year's growth is higher than next year's
        # growths[0] = most recent, growths[2] = oldest
        all_positive = all(g > 0 for g in growths)
        accelerating = growths[0] > growths[1] > growths[2]
        revenue_torpedo = all_positive and accelerating
        result = {
            "revenue_torpedo": revenue_torpedo,
            "annual_rev_growth": growths,  # [yr0_pct, yr1_pct, yr2_pct] newest→oldest
        }

    _annual_cache[ticker] = {"data": result, "TS": now}
    return result

mutants_x_get_annual_revenue_acceleration__mutmut['_mutmut_orig'] = x_get_annual_revenue_acceleration__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_1'] = x_get_annual_revenue_acceleration__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_2'] = x_get_annual_revenue_acceleration__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_3'] = x_get_annual_revenue_acceleration__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_4'] = x_get_annual_revenue_acceleration__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_5'] = x_get_annual_revenue_acceleration__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_6'] = x_get_annual_revenue_acceleration__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_7'] = x_get_annual_revenue_acceleration__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_8'] = x_get_annual_revenue_acceleration__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_9'] = x_get_annual_revenue_acceleration__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_10'] = x_get_annual_revenue_acceleration__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_11'] = x_get_annual_revenue_acceleration__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_12'] = x_get_annual_revenue_acceleration__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_13'] = x_get_annual_revenue_acceleration__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_14'] = x_get_annual_revenue_acceleration__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_15'] = x_get_annual_revenue_acceleration__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_16'] = x_get_annual_revenue_acceleration__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_17'] = x_get_annual_revenue_acceleration__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_18'] = x_get_annual_revenue_acceleration__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_19'] = x_get_annual_revenue_acceleration__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_20'] = x_get_annual_revenue_acceleration__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_21'] = x_get_annual_revenue_acceleration__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_22'] = x_get_annual_revenue_acceleration__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_23'] = x_get_annual_revenue_acceleration__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_24'] = x_get_annual_revenue_acceleration__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_25'] = x_get_annual_revenue_acceleration__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_26'] = x_get_annual_revenue_acceleration__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_27'] = x_get_annual_revenue_acceleration__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_28'] = x_get_annual_revenue_acceleration__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_29'] = x_get_annual_revenue_acceleration__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_30'] = x_get_annual_revenue_acceleration__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_31'] = x_get_annual_revenue_acceleration__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_32'] = x_get_annual_revenue_acceleration__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_33'] = x_get_annual_revenue_acceleration__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_34'] = x_get_annual_revenue_acceleration__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_35'] = x_get_annual_revenue_acceleration__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_36'] = x_get_annual_revenue_acceleration__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_37'] = x_get_annual_revenue_acceleration__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_38'] = x_get_annual_revenue_acceleration__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_39'] = x_get_annual_revenue_acceleration__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_40'] = x_get_annual_revenue_acceleration__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_41'] = x_get_annual_revenue_acceleration__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_42'] = x_get_annual_revenue_acceleration__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_43'] = x_get_annual_revenue_acceleration__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_44'] = x_get_annual_revenue_acceleration__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_45'] = x_get_annual_revenue_acceleration__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_46'] = x_get_annual_revenue_acceleration__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_47'] = x_get_annual_revenue_acceleration__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_48'] = x_get_annual_revenue_acceleration__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_49'] = x_get_annual_revenue_acceleration__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_50'] = x_get_annual_revenue_acceleration__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_51'] = x_get_annual_revenue_acceleration__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_52'] = x_get_annual_revenue_acceleration__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_53'] = x_get_annual_revenue_acceleration__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_54'] = x_get_annual_revenue_acceleration__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_55'] = x_get_annual_revenue_acceleration__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_56'] = x_get_annual_revenue_acceleration__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_57'] = x_get_annual_revenue_acceleration__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_58'] = x_get_annual_revenue_acceleration__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_59'] = x_get_annual_revenue_acceleration__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_60'] = x_get_annual_revenue_acceleration__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_61'] = x_get_annual_revenue_acceleration__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_62'] = x_get_annual_revenue_acceleration__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_63'] = x_get_annual_revenue_acceleration__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_64'] = x_get_annual_revenue_acceleration__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_65'] = x_get_annual_revenue_acceleration__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_66'] = x_get_annual_revenue_acceleration__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_67'] = x_get_annual_revenue_acceleration__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_68'] = x_get_annual_revenue_acceleration__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_69'] = x_get_annual_revenue_acceleration__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_70'] = x_get_annual_revenue_acceleration__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_71'] = x_get_annual_revenue_acceleration__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_72'] = x_get_annual_revenue_acceleration__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_73'] = x_get_annual_revenue_acceleration__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_74'] = x_get_annual_revenue_acceleration__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_75'] = x_get_annual_revenue_acceleration__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_76'] = x_get_annual_revenue_acceleration__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_77'] = x_get_annual_revenue_acceleration__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_78'] = x_get_annual_revenue_acceleration__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_79'] = x_get_annual_revenue_acceleration__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_80'] = x_get_annual_revenue_acceleration__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_81'] = x_get_annual_revenue_acceleration__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_82'] = x_get_annual_revenue_acceleration__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_83'] = x_get_annual_revenue_acceleration__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_84'] = x_get_annual_revenue_acceleration__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_85'] = x_get_annual_revenue_acceleration__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_86'] = x_get_annual_revenue_acceleration__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_87'] = x_get_annual_revenue_acceleration__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_88'] = x_get_annual_revenue_acceleration__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_89'] = x_get_annual_revenue_acceleration__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_90'] = x_get_annual_revenue_acceleration__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_91'] = x_get_annual_revenue_acceleration__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_92'] = x_get_annual_revenue_acceleration__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_93'] = x_get_annual_revenue_acceleration__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_94'] = x_get_annual_revenue_acceleration__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_95'] = x_get_annual_revenue_acceleration__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_96'] = x_get_annual_revenue_acceleration__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_97'] = x_get_annual_revenue_acceleration__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_98'] = x_get_annual_revenue_acceleration__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_99'] = x_get_annual_revenue_acceleration__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_100'] = x_get_annual_revenue_acceleration__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_101'] = x_get_annual_revenue_acceleration__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_102'] = x_get_annual_revenue_acceleration__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_103'] = x_get_annual_revenue_acceleration__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_104'] = x_get_annual_revenue_acceleration__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_105'] = x_get_annual_revenue_acceleration__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_106'] = x_get_annual_revenue_acceleration__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_107'] = x_get_annual_revenue_acceleration__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_108'] = x_get_annual_revenue_acceleration__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_109'] = x_get_annual_revenue_acceleration__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_110'] = x_get_annual_revenue_acceleration__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_111'] = x_get_annual_revenue_acceleration__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_112'] = x_get_annual_revenue_acceleration__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_113'] = x_get_annual_revenue_acceleration__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_114'] = x_get_annual_revenue_acceleration__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_115'] = x_get_annual_revenue_acceleration__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_116'] = x_get_annual_revenue_acceleration__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_117'] = x_get_annual_revenue_acceleration__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_118'] = x_get_annual_revenue_acceleration__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_119'] = x_get_annual_revenue_acceleration__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_120'] = x_get_annual_revenue_acceleration__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_121'] = x_get_annual_revenue_acceleration__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_122'] = x_get_annual_revenue_acceleration__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_123'] = x_get_annual_revenue_acceleration__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_124'] = x_get_annual_revenue_acceleration__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_125'] = x_get_annual_revenue_acceleration__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_126'] = x_get_annual_revenue_acceleration__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_127'] = x_get_annual_revenue_acceleration__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_128'] = x_get_annual_revenue_acceleration__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_annual_revenue_acceleration__mutmut['x_get_annual_revenue_acceleration__mutmut_129'] = x_get_annual_revenue_acceleration__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_polygon_dividend_data__mutmut)
async def get_polygon_dividend_data(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_orig(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_1(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = None
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_2(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache or now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_3(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker not in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_4(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now + _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_5(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["XXtsXX"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_6(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["TS"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_7(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] <= _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_8(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["XXdataXX"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_9(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["DATA"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_10(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = None
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_11(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") and ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_12(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") and os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_13(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv(None) or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_14(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("XXMASSIVE_API_KEYXX") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_15(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("massive_api_key") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_16(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv(None) or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_17(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("XXPOLYGON_API_KEYXX") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_18(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("polygon_api_key") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_19(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or "XXXX"
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_20(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_21(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = None
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_22(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = None

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_23(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                None,
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_24(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params=None,
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_25(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=None,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_26(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=None,
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_27(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_28(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_29(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_30(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_31(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "XXhttps://api.polygon.io/v3/reference/dividendsXX",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_32(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "HTTPS://API.POLYGON.IO/V3/REFERENCE/DIVIDENDS",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_33(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"XXtickerXX": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_34(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"TICKER": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_35(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "XXorderXX": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_36(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "ORDER": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_37(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "XXdescXX", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_38(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "DESC", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_39(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "XXlimitXX": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_40(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "LIMIT": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_41(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 21, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_42(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "XXapiKeyXX": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_43(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apikey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_44(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "APIKEY": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_45(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=None),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_46(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=9),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_47(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status == 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_48(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 201:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_49(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = None
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_50(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"XXdataXX": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_51(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"DATA": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_52(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "XXtsXX": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_53(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "TS": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_54(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = None
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_55(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = None

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_56(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") and []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_57(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get(None) or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_58(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("XXresultsXX") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_59(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("RESULTS") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_60(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_61(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = None
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_62(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"XXdataXX": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_63(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"DATA": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_64(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "XXtsXX": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_65(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "TS": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_66(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = None
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_67(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") and 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_68(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get(None) or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_69(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("XXcash_amountXX") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_70(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("CASH_AMOUNT") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_71(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 1 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_72(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:5]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_73(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = None

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_74(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(None)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_75(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = None

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_76(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["XXdiv_ttm_amountXX"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_77(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["DIV_TTM_AMOUNT"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_78(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(None, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_79(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, None)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_80(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_81(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, )

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_82(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 5)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_83(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = None
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_84(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(None)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_85(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = None
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_86(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") and ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_87(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") and p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_88(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get(None) or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_89(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("XXex_dividend_dateXX") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_90(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("EX_DIVIDEND_DATE") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_91(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get(None) or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_92(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("XXpay_dateXX") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_93(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("PAY_DATE") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_94(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or "XXXX"
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_95(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = None
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_96(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(None)
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_97(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:5])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_98(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] = p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_99(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] -= p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_100(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") and 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_101(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get(None) or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_102(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("XXcash_amountXX") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_103(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("CASH_AMOUNT") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_104(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 1
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_105(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = None
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_106(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(None, reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_107(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=None)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_108(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_109(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), )
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_110(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=False)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_111(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = None
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_112(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 1
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_113(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) > 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_114(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 3:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_115(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(None):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_116(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) + 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_117(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 2):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_118(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] >= year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_119(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i - 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_120(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 2]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_121(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak = 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_122(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak -= 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_123(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 2
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_124(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    return

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_125(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = None
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_126(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["XXdiv_aristocrat_yearsXX"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_127(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["DIV_ARISTOCRAT_YEARS"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_128(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak > 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_129(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 26:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_130(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = None
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_131(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["XXdiv_aristocrat_levelXX"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_132(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["DIV_ARISTOCRAT_LEVEL"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_133(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "XXKingXX"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_134(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "king"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_135(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "KING"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_136(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak > 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_137(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 11:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_138(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = None
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_139(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["XXdiv_aristocrat_levelXX"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_140(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["DIV_ARISTOCRAT_LEVEL"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_141(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "XXAristocratXX"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_142(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_143(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "ARISTOCRAT"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_144(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak > 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_145(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 6:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_146(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = None
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_147(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["XXdiv_aristocrat_levelXX"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_148(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["DIV_ARISTOCRAT_LEVEL"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_149(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "XXAchieverXX"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_150(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_151(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "ACHIEVER"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_152(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = None

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_153(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["XXdiv_aristocrat_levelXX"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_154(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["DIV_ARISTOCRAT_LEVEL"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_155(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = "XXXX"

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_156(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(None)

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_157(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = None
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_158(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"XXdataXX": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_159(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"DATA": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_160(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "XXtsXX": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_161(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "TS": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_162(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            None
        )
    return result


async def x_get_polygon_dividend_data__mutmut_163(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get(None, '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_164(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', None)} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_165(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_166(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', )} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_167(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('XXdiv_ttm_amountXX', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_168(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('DIV_TTM_AMOUNT', '?')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_169(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', 'XX?XX')} "
            f"streak={result.get('div_aristocrat_years', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_170(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get(None, '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_171(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', None)}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_172(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_173(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', )}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_174(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('XXdiv_aristocrat_yearsXX', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_175(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('DIV_ARISTOCRAT_YEARS', '?')}yr"
        )
    return result


async def x_get_polygon_dividend_data__mutmut_176(ticker: str) -> dict:
    """
    Fetch last 4 quarterly dividends from v3/reference/dividends.
    Returns:
      div_yield_polygon: TTM dividend yield (%)
      div_aristocrat_years: consecutive years of annual increases (0 if none)
      div_aristocrat_level: "King" (≥25yr), "Aristocrat" (≥10yr), "Achiever" (≥5yr), ""
    yfinance dividendYield returns None for ~40% of tickers; Polygon gives exact amounts.
    """
    now = time.time()
    if ticker in _dividend_cache and now - _dividend_cache[ticker]["ts"] < _DIV_TTL:
        return _dividend_cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY") or os.getenv("POLYGON_API_KEY") or ""
    if not api_key:
        return {}

    ssl_ctx = get_ssl_context()
    result: dict = {}

    try:
        async with shared_session() as session:
            # Fetch last 20 payments — enough for 5 years of quarterly dividends
            async with session.get(
                "https://api.polygon.io/v3/reference/dividends",
                params={"ticker": ticker, "order": "desc", "limit": 20, "apiKey": api_key},
                ssl=ssl_ctx,
                timeout=aiohttp.ClientTimeout(total=8),
            ) as resp:
                if resp.status != 200:
                    _dividend_cache[ticker] = {"data": {}, "ts": now}
                    return {}
                data = await resp.json()
                payments = data.get("results") or []

        if not payments:
            _dividend_cache[ticker] = {"data": {}, "ts": now}
            return {}

        # TTM yield from last 4 quarterly payments
        ttm_payments = [p.get("cash_amount") or 0 for p in payments[:4]]
        ttm_total = sum(ttm_payments)

        # Fetch current price for yield calculation (use ex_dividend_date as proxy)
        # We'll store raw TTM amount and let signal_engine divide by price
        result["div_ttm_amount"] = round(ttm_total, 4)

        # Consecutive annual increase streak
        # Group by year, sum per year, check for increases
        from collections import defaultdict

        year_totals: dict[int, float] = defaultdict(float)
        for p in payments:
            dt_str = p.get("ex_dividend_date") or p.get("pay_date") or ""
            try:
                yr = int(dt_str[:4])
                year_totals[yr] += p.get("cash_amount") or 0
            except (ValueError, IndexError):
                pass

        sorted_years = sorted(year_totals.keys(), reverse=True)
        streak = 0
        if len(sorted_years) >= 2:
            for i in range(len(sorted_years) - 1):
                if year_totals[sorted_years[i]] > year_totals[sorted_years[i + 1]]:
                    streak += 1
                else:
                    break

        result["div_aristocrat_years"] = streak
        if streak >= 25:
            result["div_aristocrat_level"] = "King"
        elif streak >= 10:
            result["div_aristocrat_level"] = "Aristocrat"
        elif streak >= 5:
            result["div_aristocrat_level"] = "Achiever"
        else:
            result["div_aristocrat_level"] = ""

    except Exception as e:
        log.debug(f"[polygon_dividends] {ticker}: {e}")

    _dividend_cache[ticker] = {"data": result, "ts": now}
    if result:
        log.debug(
            f"[polygon_dividends] {ticker}: TTM={result.get('div_ttm_amount', '?')} "
            f"streak={result.get('div_aristocrat_years', 'XX?XX')}yr"
        )
    return result

mutants_x_get_polygon_dividend_data__mutmut['_mutmut_orig'] = x_get_polygon_dividend_data__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_1'] = x_get_polygon_dividend_data__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_2'] = x_get_polygon_dividend_data__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_3'] = x_get_polygon_dividend_data__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_4'] = x_get_polygon_dividend_data__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_5'] = x_get_polygon_dividend_data__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_6'] = x_get_polygon_dividend_data__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_7'] = x_get_polygon_dividend_data__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_8'] = x_get_polygon_dividend_data__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_9'] = x_get_polygon_dividend_data__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_10'] = x_get_polygon_dividend_data__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_11'] = x_get_polygon_dividend_data__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_12'] = x_get_polygon_dividend_data__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_13'] = x_get_polygon_dividend_data__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_14'] = x_get_polygon_dividend_data__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_15'] = x_get_polygon_dividend_data__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_16'] = x_get_polygon_dividend_data__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_17'] = x_get_polygon_dividend_data__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_18'] = x_get_polygon_dividend_data__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_19'] = x_get_polygon_dividend_data__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_20'] = x_get_polygon_dividend_data__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_21'] = x_get_polygon_dividend_data__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_22'] = x_get_polygon_dividend_data__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_23'] = x_get_polygon_dividend_data__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_24'] = x_get_polygon_dividend_data__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_25'] = x_get_polygon_dividend_data__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_26'] = x_get_polygon_dividend_data__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_27'] = x_get_polygon_dividend_data__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_28'] = x_get_polygon_dividend_data__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_29'] = x_get_polygon_dividend_data__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_30'] = x_get_polygon_dividend_data__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_31'] = x_get_polygon_dividend_data__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_32'] = x_get_polygon_dividend_data__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_33'] = x_get_polygon_dividend_data__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_34'] = x_get_polygon_dividend_data__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_35'] = x_get_polygon_dividend_data__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_36'] = x_get_polygon_dividend_data__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_37'] = x_get_polygon_dividend_data__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_38'] = x_get_polygon_dividend_data__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_39'] = x_get_polygon_dividend_data__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_40'] = x_get_polygon_dividend_data__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_41'] = x_get_polygon_dividend_data__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_42'] = x_get_polygon_dividend_data__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_43'] = x_get_polygon_dividend_data__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_44'] = x_get_polygon_dividend_data__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_45'] = x_get_polygon_dividend_data__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_46'] = x_get_polygon_dividend_data__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_47'] = x_get_polygon_dividend_data__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_48'] = x_get_polygon_dividend_data__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_49'] = x_get_polygon_dividend_data__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_50'] = x_get_polygon_dividend_data__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_51'] = x_get_polygon_dividend_data__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_52'] = x_get_polygon_dividend_data__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_53'] = x_get_polygon_dividend_data__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_54'] = x_get_polygon_dividend_data__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_55'] = x_get_polygon_dividend_data__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_56'] = x_get_polygon_dividend_data__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_57'] = x_get_polygon_dividend_data__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_58'] = x_get_polygon_dividend_data__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_59'] = x_get_polygon_dividend_data__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_60'] = x_get_polygon_dividend_data__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_61'] = x_get_polygon_dividend_data__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_62'] = x_get_polygon_dividend_data__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_63'] = x_get_polygon_dividend_data__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_64'] = x_get_polygon_dividend_data__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_65'] = x_get_polygon_dividend_data__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_66'] = x_get_polygon_dividend_data__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_67'] = x_get_polygon_dividend_data__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_68'] = x_get_polygon_dividend_data__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_69'] = x_get_polygon_dividend_data__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_70'] = x_get_polygon_dividend_data__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_71'] = x_get_polygon_dividend_data__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_72'] = x_get_polygon_dividend_data__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_73'] = x_get_polygon_dividend_data__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_74'] = x_get_polygon_dividend_data__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_75'] = x_get_polygon_dividend_data__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_76'] = x_get_polygon_dividend_data__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_77'] = x_get_polygon_dividend_data__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_78'] = x_get_polygon_dividend_data__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_79'] = x_get_polygon_dividend_data__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_80'] = x_get_polygon_dividend_data__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_81'] = x_get_polygon_dividend_data__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_82'] = x_get_polygon_dividend_data__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_83'] = x_get_polygon_dividend_data__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_84'] = x_get_polygon_dividend_data__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_85'] = x_get_polygon_dividend_data__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_86'] = x_get_polygon_dividend_data__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_87'] = x_get_polygon_dividend_data__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_88'] = x_get_polygon_dividend_data__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_89'] = x_get_polygon_dividend_data__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_90'] = x_get_polygon_dividend_data__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_91'] = x_get_polygon_dividend_data__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_92'] = x_get_polygon_dividend_data__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_93'] = x_get_polygon_dividend_data__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_94'] = x_get_polygon_dividend_data__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_95'] = x_get_polygon_dividend_data__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_96'] = x_get_polygon_dividend_data__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_97'] = x_get_polygon_dividend_data__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_98'] = x_get_polygon_dividend_data__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_99'] = x_get_polygon_dividend_data__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_100'] = x_get_polygon_dividend_data__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_101'] = x_get_polygon_dividend_data__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_102'] = x_get_polygon_dividend_data__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_103'] = x_get_polygon_dividend_data__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_104'] = x_get_polygon_dividend_data__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_105'] = x_get_polygon_dividend_data__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_106'] = x_get_polygon_dividend_data__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_107'] = x_get_polygon_dividend_data__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_108'] = x_get_polygon_dividend_data__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_109'] = x_get_polygon_dividend_data__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_110'] = x_get_polygon_dividend_data__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_111'] = x_get_polygon_dividend_data__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_112'] = x_get_polygon_dividend_data__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_113'] = x_get_polygon_dividend_data__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_114'] = x_get_polygon_dividend_data__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_115'] = x_get_polygon_dividend_data__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_116'] = x_get_polygon_dividend_data__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_117'] = x_get_polygon_dividend_data__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_118'] = x_get_polygon_dividend_data__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_119'] = x_get_polygon_dividend_data__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_120'] = x_get_polygon_dividend_data__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_121'] = x_get_polygon_dividend_data__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_122'] = x_get_polygon_dividend_data__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_123'] = x_get_polygon_dividend_data__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_124'] = x_get_polygon_dividend_data__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_125'] = x_get_polygon_dividend_data__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_126'] = x_get_polygon_dividend_data__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_127'] = x_get_polygon_dividend_data__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_128'] = x_get_polygon_dividend_data__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_129'] = x_get_polygon_dividend_data__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_130'] = x_get_polygon_dividend_data__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_131'] = x_get_polygon_dividend_data__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_132'] = x_get_polygon_dividend_data__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_133'] = x_get_polygon_dividend_data__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_134'] = x_get_polygon_dividend_data__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_135'] = x_get_polygon_dividend_data__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_136'] = x_get_polygon_dividend_data__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_137'] = x_get_polygon_dividend_data__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_138'] = x_get_polygon_dividend_data__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_139'] = x_get_polygon_dividend_data__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_140'] = x_get_polygon_dividend_data__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_141'] = x_get_polygon_dividend_data__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_142'] = x_get_polygon_dividend_data__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_143'] = x_get_polygon_dividend_data__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_144'] = x_get_polygon_dividend_data__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_145'] = x_get_polygon_dividend_data__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_146'] = x_get_polygon_dividend_data__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_147'] = x_get_polygon_dividend_data__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_148'] = x_get_polygon_dividend_data__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_149'] = x_get_polygon_dividend_data__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_150'] = x_get_polygon_dividend_data__mutmut_150 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_151'] = x_get_polygon_dividend_data__mutmut_151 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_152'] = x_get_polygon_dividend_data__mutmut_152 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_153'] = x_get_polygon_dividend_data__mutmut_153 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_154'] = x_get_polygon_dividend_data__mutmut_154 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_155'] = x_get_polygon_dividend_data__mutmut_155 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_156'] = x_get_polygon_dividend_data__mutmut_156 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_157'] = x_get_polygon_dividend_data__mutmut_157 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_158'] = x_get_polygon_dividend_data__mutmut_158 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_159'] = x_get_polygon_dividend_data__mutmut_159 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_160'] = x_get_polygon_dividend_data__mutmut_160 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_161'] = x_get_polygon_dividend_data__mutmut_161 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_162'] = x_get_polygon_dividend_data__mutmut_162 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_163'] = x_get_polygon_dividend_data__mutmut_163 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_164'] = x_get_polygon_dividend_data__mutmut_164 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_165'] = x_get_polygon_dividend_data__mutmut_165 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_166'] = x_get_polygon_dividend_data__mutmut_166 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_167'] = x_get_polygon_dividend_data__mutmut_167 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_168'] = x_get_polygon_dividend_data__mutmut_168 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_169'] = x_get_polygon_dividend_data__mutmut_169 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_170'] = x_get_polygon_dividend_data__mutmut_170 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_171'] = x_get_polygon_dividend_data__mutmut_171 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_172'] = x_get_polygon_dividend_data__mutmut_172 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_173'] = x_get_polygon_dividend_data__mutmut_173 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_174'] = x_get_polygon_dividend_data__mutmut_174 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_175'] = x_get_polygon_dividend_data__mutmut_175 # type: ignore # mutmut generated
mutants_x_get_polygon_dividend_data__mutmut['x_get_polygon_dividend_data__mutmut_176'] = x_get_polygon_dividend_data__mutmut_176 # type: ignore # mutmut generated
