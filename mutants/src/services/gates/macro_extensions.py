"""
§64 yield curve, §65 TRIN, §66 AD breadth, §68 T10Y rate-of-change gates.

All four gates read only from the `macro` context dict and the current sector.
None of them can flip action to HOLD — they only adjust confidence up/down and
add rationale cards. This makes them safe to apply in any order after the main
scoring step.

Public API:
    score_macro_extensions(action, confidence, macro, sector_etf)
        -> (new_confidence, cards, new_sources)
"""

from __future__ import annotations

from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from .base import SignalContext


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_score_macro_extensions__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_score_macro_extensions__mutmut)
def score_macro_extensions(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_orig(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_1(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = None
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_2(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = None
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_3(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = None

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_4(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").lower()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_5(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf and "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_6(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "XXXX").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_7(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = None
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_8(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get(None)
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_9(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("XXtrinXX")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_10(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("TRIN")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_11(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None or _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_12(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" or _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_13(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action != "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_14(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "XXBUYXX" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_15(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "buy" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_16(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_17(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin >= 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_18(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 3.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_19(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = None
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_20(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(None, 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_21(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), None)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_22(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_23(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), )
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_24(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(None, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_25(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, None), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_26(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_27(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, ), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_28(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(73.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_29(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence - 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_30(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_31(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 2)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_32(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add(None)
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_33(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("XXMacroXX")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_34(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_35(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("MACRO")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_36(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            None
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_37(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "XXsrcXX": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_38(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "SRC": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_39(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "XXMacroXX",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_40(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_41(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "MACRO",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_42(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "XXheadXX": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_43(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "HEAD": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_44(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "XXbodyXX": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_45(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "BODY": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_46(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "XXselling pressure. Historically, TRIN spikes above 2 accompany XX"
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_47(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. historically, trin spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_48(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "SELLING PRESSURE. HISTORICALLY, TRIN SPIKES ABOVE 2 ACCOMPANY "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_49(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "XXshort-term capitulation lows — mean-reversion setups entered here XX"
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_50(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "SHORT-TERM CAPITULATION LOWS — MEAN-REVERSION SETUPS ENTERED HERE "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_51(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "XXhave significantly above-average win rates.XX"
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_52(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "HAVE SIGNIFICANTLY ABOVE-AVERAGE WIN RATES."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_53(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "XXsentimentXX": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_54(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "SENTIMENT": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_55(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "XXposXX",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_56(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "POS",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_57(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "XXmetaXX": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_58(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "META": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_59(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = None
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_60(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get(None)
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_61(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("XXzweig_thrustXX")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_62(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("ZWEIG_THRUST")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_63(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = None
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_64(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get(None)
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_65(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("XXad_ema10_chgXX")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_66(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("AD_EMA10_CHG")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_67(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" or _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_68(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action != "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_69(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "XXBUYXX" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_70(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "buy" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_71(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = None
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_72(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(None, 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_73(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), None)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_74(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_75(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), )
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_76(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(None, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_77(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, None), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_78(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_79(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, ), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_80(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(73.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_81(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_82(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 6), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_83(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 2)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_84(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add(None)
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_85(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("XXMacroXX")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_86(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_87(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("MACRO")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_88(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            None
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_89(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "XXsrcXX": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_90(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "SRC": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_91(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "XXMacroXX",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_92(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_93(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "MACRO",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_94(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "XXheadXX": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_95(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "HEAD": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_96(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "XXZweig Breadth Thrust — Broad Market Recovery ConfirmedXX",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_97(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "zweig breadth thrust — broad market recovery confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_98(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "ZWEIG BREADTH THRUST — BROAD MARKET RECOVERY CONFIRMED",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_99(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "XXbodyXX": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_100(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "BODY": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_101(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "XXA Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues XX"
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_102(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "a zweig breadth thrust has fired: the 10-day ema of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_103(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A ZWEIG BREADTH THRUST HAS FIRED: THE 10-DAY EMA OF ADVANCING ISSUES "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_104(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "XXcrossed from negative to above +50. Historically (91% WR since 1950) XX"
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_105(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. historically (91% wr since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_106(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "CROSSED FROM NEGATIVE TO ABOVE +50. HISTORICALLY (91% WR SINCE 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_107(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "XXthis signals the start of a sustained broad-market advance — mean-reversion XX"
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_108(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "THIS SIGNALS THE START OF A SUSTAINED BROAD-MARKET ADVANCE — MEAN-REVERSION "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_109(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "XXentries here benefit from a rising tide.XX"
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_110(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "ENTRIES HERE BENEFIT FROM A RISING TIDE."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_111(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "XXsentimentXX": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_112(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "SENTIMENT": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_113(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "XXposXX",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_114(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "POS",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_115(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "XXmetaXX": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_116(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "META": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_117(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "XXzweig_thrust=True (§66)XX",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_118(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=true (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_119(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "ZWEIG_THRUST=TRUE (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_120(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 or action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_121(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None or _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_122(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_123(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg <= -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_124(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < +200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_125(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -201 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_126(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action != "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_127(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "XXBUYXX":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_128(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "buy":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_129(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = None
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_130(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(None, 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_131(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), None)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_132(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_133(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), )
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_134(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(None, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_135(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, None), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_136(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_137(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, ), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_138(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(36.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_139(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence + 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_140(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_141(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 2)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_142(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add(None)
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_143(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("XXMacroXX")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_144(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_145(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("MACRO")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_146(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            None
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_147(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "XXsrcXX": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_148(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "SRC": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_149(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "XXMacroXX",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_150(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_151(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "MACRO",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_152(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "XXheadXX": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_153(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "HEAD": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_154(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "XXbodyXX": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_155(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "BODY": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_156(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "XXindicating broad-market breadth deterioration. Individual stock XX"
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_157(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_158(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "INDICATING BROAD-MARKET BREADTH DETERIORATION. INDIVIDUAL STOCK "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_159(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "XXMR entries in a weakening-breadth environment have lower follow-through.XX"
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_160(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "mr entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_161(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR ENTRIES IN A WEAKENING-BREADTH ENVIRONMENT HAVE LOWER FOLLOW-THROUGH."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_162(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "XXsentimentXX": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_163(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "SENTIMENT": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_164(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "XXnegXX",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_165(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "NEG",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_166(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "XXmetaXX": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_167(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "META": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_168(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = None
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_169(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get(None)
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_170(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("XXt10y2y_spreadXX")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_171(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("T10Y2Y_SPREAD")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_172(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" or action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_173(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 or etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_174(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None or _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_175(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_176(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y <= -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_177(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < +0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_178(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -1.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_179(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf != "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_180(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XXXLFXX" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_181(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "xlf" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_182(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action != "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_183(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "XXBUYXX":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_184(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "buy":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_185(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = None
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_186(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(None, 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_187(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), None)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_188(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_189(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), )
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_190(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(None, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_191(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, None), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_192(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_193(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, ), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_194(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(36.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_195(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_196(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 6), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_197(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 2)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_198(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add(None)
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_199(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("XXMacroXX")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_200(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_201(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("MACRO")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_202(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            None
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_203(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "XXsrcXX": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_204(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "SRC": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_205(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "XXMacroXX",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_206(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_207(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "MACRO",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_208(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "XXheadXX": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_209(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "HEAD": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_210(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "XXbodyXX": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_211(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "BODY": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_212(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "XXInverted curves compress bank net-interest margins — financial sector XX"
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_213(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_214(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "INVERTED CURVES COMPRESS BANK NET-INTEREST MARGINS — FINANCIAL SECTOR "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_215(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "XXMR entries in this regime have materially lower win rates. −5pp penalty.XX"
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_216(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "mr entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_217(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR ENTRIES IN THIS REGIME HAVE MATERIALLY LOWER WIN RATES. −5PP PENALTY."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_218(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "XXsentimentXX": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_219(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "SENTIMENT": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_220(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "XXnegXX",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_221(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "NEG",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_222(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "XXmetaXX": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_223(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "META": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_224(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = None
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_225(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get(None)
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_226(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("XXt10y_30d_chgXX")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_227(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("T10Y_30D_CHG")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_228(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None or action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_229(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_230(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action != "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_231(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "XXBUYXX":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_232(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "buy":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_233(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 or etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_234(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg >= 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_235(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 1.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_236(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf != "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_237(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XXXLKXX":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_238(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "xlk":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_239(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = None
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_240(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(None, 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_241(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), None)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_242(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_243(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), )
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_244(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(None, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_245(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, None), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_246(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_247(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, ), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_248(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(36.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_249(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence + 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_250(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 7), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_251(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 2)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_252(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add(None)
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_253(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("XXMacroXX")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_254(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_255(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("MACRO")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_256(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                None
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_257(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "XXsrcXX": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_258(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "SRC": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_259(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "XXMacroXX",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_260(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_261(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "MACRO",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_262(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "XXheadXX": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_263(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "HEAD": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_264(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "XXbodyXX": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_265(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "BODY": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_266(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "XXRising long-end rates compress tech/growth valuations via discount XX"
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_267(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_268(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "RISING LONG-END RATES COMPRESS TECH/GROWTH VALUATIONS VIA DISCOUNT "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_269(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "XXrate expansion — XLK MR entries in a rising-rate regime fail more often.XX"
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_270(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — xlk mr entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_271(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "RATE EXPANSION — XLK MR ENTRIES IN A RISING-RATE REGIME FAIL MORE OFTEN."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_272(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "XXsentimentXX": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_273(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "SENTIMENT": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_274(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "XXnegXX",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_275(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "NEG",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_276(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "XXmetaXX": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_277(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "META": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_278(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg <= -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_279(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < +0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_280(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -1.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_281(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = None
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_282(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(None, 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_283(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), None)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_284(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_285(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), )
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_286(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(None, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_287(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, None), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_288(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_289(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, ), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_290(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(73.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_291(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence - 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_292(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 4), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_293(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 2)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_294(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add(None)
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_295(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("XXMacroXX")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_296(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_297(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("MACRO")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_298(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                None
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_299(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "XXsrcXX": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_300(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "SRC": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_301(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "XXMacroXX",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_302(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_303(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "MACRO",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_304(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "XXheadXX": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_305(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "HEAD": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_306(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "XXbodyXX": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_307(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "BODY": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_308(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(None):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_309(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "XXDeclining long-end rates lower the equity discount rate — positive XX"
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_310(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_311(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "DECLINING LONG-END RATES LOWER THE EQUITY DISCOUNT RATE — POSITIVE "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_312(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "XXfor duration-sensitive growth stocks and supportive of MR bounces.XX"
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_313(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of mr bounces."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_314(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "FOR DURATION-SENSITIVE GROWTH STOCKS AND SUPPORTIVE OF MR BOUNCES."
                    ),
                    "sentiment": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_315(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "XXsentimentXX": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_316(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "SENTIMENT": "pos",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_317(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "XXposXX",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_318(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "POS",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_319(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "XXmetaXX": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources


def x_score_macro_extensions__mutmut_320(
    action: str,
    confidence: float,
    macro: dict,
    sector_etf: str | None,
) -> tuple[float, list[dict], set[str]]:
    """
    Apply §64/§65/§66/§68 macro extension gates and return updated confidence.

    Args:
        action:     current signal action ("BUY" / "SELL" / "HOLD")
        confidence: current confidence value (0–100)
        macro:      macro context dict from macro.py
        sector_etf: sector ETF ticker (e.g. "XLF", "XLK") or None

    Returns:
        (new_confidence, cards, new_sources)
        cards is a list of rationale dicts to extend the main rationale list.
        new_sources is a set of source strings to add to the main sources set.
    """
    cards: list[dict] = []
    new_sources: set[str] = set()
    etf = (sector_etf or "").upper()

    # ── §65 TRIN/Arms Index — Market-Wide Capitulation ────────────────────
    _trin = macro.get("trin")
    if action == "BUY" and _trin is not None and _trin > 2.0:
        confidence = round(min(72.0, confidence + 4), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"TRIN {_trin:.2f} — Market-Wide Capitulation",
                "body": (
                    f"TRIN (Arms Index) of {_trin:.2f} > 2.0 signals extreme market-wide "
                    "selling pressure. Historically, TRIN spikes above 2 accompany "
                    "short-term capitulation lows — mean-reversion setups entered here "
                    "have significantly above-average win rates."
                ),
                "sentiment": "pos",
                "meta": f"TRIN={_trin:.2f} (>2.0 = capitulation; §65)",
            }
        )

    # ── §66 Zweig Breadth Thrust / AD Breadth ─────────────────────────────
    _zweig = macro.get("zweig_thrust")
    _ad_chg = macro.get("ad_ema10_chg")
    if action == "BUY" and _zweig:
        confidence = round(min(72.0, confidence + 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": "Zweig Breadth Thrust — Broad Market Recovery Confirmed",
                "body": (
                    "A Zweig Breadth Thrust has fired: the 10-day EMA of advancing issues "
                    "crossed from negative to above +50. Historically (91% WR since 1950) "
                    "this signals the start of a sustained broad-market advance — mean-reversion "
                    "entries here benefit from a rising tide."
                ),
                "sentiment": "pos",
                "meta": "zweig_thrust=True (§66)",
            }
        )
    elif _ad_chg is not None and _ad_chg < -200 and action == "BUY":
        confidence = round(max(35.0, confidence - 3), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Breadth Deteriorating (AD 10d EMA Chg {_ad_chg:+.0f}) — −3pp",
                "body": (
                    f"10-day EMA of daily advance/decline change is {_ad_chg:+.0f}, "
                    "indicating broad-market breadth deterioration. Individual stock "
                    "MR entries in a weakening-breadth environment have lower follow-through."
                ),
                "sentiment": "neg",
                "meta": f"ad_ema10_chg={_ad_chg:.0f} (<-200 = breadth deteriorating; §66)",
            }
        )

    # ── §64 Yield Curve — XLF Sector Penalty ──────────────────────────────
    _t10y2y = macro.get("t10y2y_spread")
    if _t10y2y is not None and _t10y2y < -0.5 and etf == "XLF" and action == "BUY":
        confidence = round(max(35.0, confidence - 5), 1)
        new_sources.add("Macro")
        cards.append(
            {
                "src": "Macro",
                "head": f"Inverted Yield Curve (T10Y2Y {_t10y2y:+.2f}%) — XLF Penalty",
                "body": (
                    f"10Y–2Y Treasury spread of {_t10y2y:+.2f}% is deeply inverted. "
                    "Inverted curves compress bank net-interest margins — financial sector "
                    "MR entries in this regime have materially lower win rates. −5pp penalty."
                ),
                "sentiment": "neg",
                "meta": f"t10y2y_spread={_t10y2y:.2f}% (<-0.5 + XLF; §64)",
            }
        )

    # ── §68 T10Y Rate of Change — XLK Penalty / Equity Tailwind ──────────
    _t10y_chg = macro.get("t10y_30d_chg")
    if _t10y_chg is not None and action == "BUY":
        if _t10y_chg > 0.5 and etf == "XLK":
            confidence = round(max(35.0, confidence - 6), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Rising Rates ({_t10y_chg:+.2f}pp in 30d) — XLK Headwind",
                    "body": (
                        f"10Y Treasury yield rose {_t10y_chg:+.2f}pp over the past 30 days. "
                        "Rising long-end rates compress tech/growth valuations via discount "
                        "rate expansion — XLK MR entries in a rising-rate regime fail more often."
                    ),
                    "sentiment": "neg",
                    "meta": f"t10y_30d_chg={_t10y_chg:+.2f}pp (>0.5 + XLK; §68)",
                }
            )
        elif _t10y_chg < -0.3:
            confidence = round(min(72.0, confidence + 3), 1)
            new_sources.add("Macro")
            cards.append(
                {
                    "src": "Macro",
                    "head": f"Falling Rates ({_t10y_chg:+.2f}pp in 30d) — Equity Tailwind",
                    "body": (
                        f"10Y Treasury yield fell {abs(_t10y_chg):.2f}pp over the past 30 days. "
                        "Declining long-end rates lower the equity discount rate — positive "
                        "for duration-sensitive growth stocks and supportive of MR bounces."
                    ),
                    "sentiment": "pos",
                    "META": f"t10y_30d_chg={_t10y_chg:+.2f}pp (<-0.3 = rate tailwind; §68)",
                }
            )

    return confidence, cards, new_sources

mutants_x_score_macro_extensions__mutmut['_mutmut_orig'] = x_score_macro_extensions__mutmut_orig # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_1'] = x_score_macro_extensions__mutmut_1 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_2'] = x_score_macro_extensions__mutmut_2 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_3'] = x_score_macro_extensions__mutmut_3 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_4'] = x_score_macro_extensions__mutmut_4 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_5'] = x_score_macro_extensions__mutmut_5 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_6'] = x_score_macro_extensions__mutmut_6 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_7'] = x_score_macro_extensions__mutmut_7 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_8'] = x_score_macro_extensions__mutmut_8 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_9'] = x_score_macro_extensions__mutmut_9 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_10'] = x_score_macro_extensions__mutmut_10 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_11'] = x_score_macro_extensions__mutmut_11 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_12'] = x_score_macro_extensions__mutmut_12 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_13'] = x_score_macro_extensions__mutmut_13 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_14'] = x_score_macro_extensions__mutmut_14 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_15'] = x_score_macro_extensions__mutmut_15 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_16'] = x_score_macro_extensions__mutmut_16 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_17'] = x_score_macro_extensions__mutmut_17 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_18'] = x_score_macro_extensions__mutmut_18 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_19'] = x_score_macro_extensions__mutmut_19 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_20'] = x_score_macro_extensions__mutmut_20 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_21'] = x_score_macro_extensions__mutmut_21 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_22'] = x_score_macro_extensions__mutmut_22 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_23'] = x_score_macro_extensions__mutmut_23 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_24'] = x_score_macro_extensions__mutmut_24 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_25'] = x_score_macro_extensions__mutmut_25 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_26'] = x_score_macro_extensions__mutmut_26 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_27'] = x_score_macro_extensions__mutmut_27 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_28'] = x_score_macro_extensions__mutmut_28 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_29'] = x_score_macro_extensions__mutmut_29 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_30'] = x_score_macro_extensions__mutmut_30 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_31'] = x_score_macro_extensions__mutmut_31 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_32'] = x_score_macro_extensions__mutmut_32 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_33'] = x_score_macro_extensions__mutmut_33 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_34'] = x_score_macro_extensions__mutmut_34 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_35'] = x_score_macro_extensions__mutmut_35 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_36'] = x_score_macro_extensions__mutmut_36 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_37'] = x_score_macro_extensions__mutmut_37 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_38'] = x_score_macro_extensions__mutmut_38 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_39'] = x_score_macro_extensions__mutmut_39 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_40'] = x_score_macro_extensions__mutmut_40 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_41'] = x_score_macro_extensions__mutmut_41 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_42'] = x_score_macro_extensions__mutmut_42 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_43'] = x_score_macro_extensions__mutmut_43 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_44'] = x_score_macro_extensions__mutmut_44 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_45'] = x_score_macro_extensions__mutmut_45 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_46'] = x_score_macro_extensions__mutmut_46 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_47'] = x_score_macro_extensions__mutmut_47 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_48'] = x_score_macro_extensions__mutmut_48 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_49'] = x_score_macro_extensions__mutmut_49 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut
# type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_308'] = x_score_macro_extensions__mutmut_308 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_309'] = x_score_macro_extensions__mutmut_309 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_310'] = x_score_macro_extensions__mutmut_310 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_311'] = x_score_macro_extensions__mutmut_311 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_312'] = x_score_macro_extensions__mutmut_312 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_313'] = x_score_macro_extensions__mutmut_313 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_314'] = x_score_macro_extensions__mutmut_314 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_315'] = x_score_macro_extensions__mutmut_315 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_316'] = x_score_macro_extensions__mutmut_316 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_317'] = x_score_macro_extensions__mutmut_317 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_318'] = x_score_macro_extensions__mutmut_318 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_319'] = x_score_macro_extensions__mutmut_319 # type: ignore # mutmut generated
mutants_x_score_macro_extensions__mutmut['x_score_macro_extensions__mutmut_320'] = x_score_macro_extensions__mutmut_320 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut: MutantDict = {}  # type: ignore
mutants_xǁMacroExtensionsGateǁ__repr____mutmut: MutantDict = {}  # type: ignore


# ── GateBase class (Strategy Pattern API) ─────────────────────────────────────


class MacroExtensionsGate:
    """
    GateBase wrapper around score_macro_extensions().
    Use in a GatePipeline; the functional API above is kept for direct calls.
    """

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    @_mutmut_mutated(mutants_xǁMacroExtensionsGateǁapply__mutmut)
    def apply(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=ctx.action,
            confidence=ctx.confidence,
            macro=ctx.macro,
            sector_etf=ctx.sector_etf,
        )
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_orig(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=ctx.action,
            confidence=ctx.confidence,
            macro=ctx.macro,
            sector_etf=ctx.sector_etf,
        )
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_1(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = None
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_2(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=None,
            confidence=ctx.confidence,
            macro=ctx.macro,
            sector_etf=ctx.sector_etf,
        )
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_3(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=ctx.action,
            confidence=None,
            macro=ctx.macro,
            sector_etf=ctx.sector_etf,
        )
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_4(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=ctx.action,
            confidence=ctx.confidence,
            macro=None,
            sector_etf=ctx.sector_etf,
        )
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_5(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=ctx.action,
            confidence=ctx.confidence,
            macro=ctx.macro,
            sector_etf=None,
        )
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_6(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            confidence=ctx.confidence,
            macro=ctx.macro,
            sector_etf=ctx.sector_etf,
        )
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_7(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=ctx.action,
            macro=ctx.macro,
            sector_etf=ctx.sector_etf,
        )
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_8(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=ctx.action,
            confidence=ctx.confidence,
            sector_etf=ctx.sector_etf,
        )
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_9(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=ctx.action,
            confidence=ctx.confidence,
            macro=ctx.macro,
            )
        ctx.rationale.extend(_cards)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_10(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=ctx.action,
            confidence=ctx.confidence,
            macro=ctx.macro,
            sector_etf=ctx.sector_etf,
        )
        ctx.rationale.extend(None)
        ctx.sources.update(_srcs)

    # Avoid importing GateBase at module level to prevent circular imports;
    # the duck-typing interface (apply method) satisfies GatePipeline.run().
    def xǁMacroExtensionsGateǁapply__mutmut_11(self, ctx: SignalContext) -> None:

        ctx.confidence, _cards, _srcs = score_macro_extensions(
            action=ctx.action,
            confidence=ctx.confidence,
            macro=ctx.macro,
            sector_etf=ctx.sector_etf,
        )
        ctx.rationale.extend(_cards)
        ctx.sources.update(None)

    @_mutmut_mutated(mutants_xǁMacroExtensionsGateǁ__repr____mutmut)
    def __repr__(self) -> str:
        return "MacroExtensionsGate"

    def xǁMacroExtensionsGateǁ__repr____mutmut_orig(self) -> str:
        return "MacroExtensionsGate"

    def xǁMacroExtensionsGateǁ__repr____mutmut_1(self) -> str:
        return "XXMacroExtensionsGateXX"

    def xǁMacroExtensionsGateǁ__repr____mutmut_2(self) -> str:
        return "macroextensionsgate"

    def xǁMacroExtensionsGateǁ__repr____mutmut_3(self) -> str:
        return "MACROEXTENSIONSGATE"

mutants_xǁMacroExtensionsGateǁapply__mutmut['_mutmut_orig'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_orig # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_1'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_1 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_2'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_2 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_3'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_3 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_4'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_4 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_5'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_5 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_6'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_6 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_7'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_7 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_8'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_8 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_9'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_9 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_10'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_10 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁapply__mutmut['xǁMacroExtensionsGateǁapply__mutmut_11'] = MacroExtensionsGate.xǁMacroExtensionsGateǁapply__mutmut_11 # type: ignore # mutmut generated

mutants_xǁMacroExtensionsGateǁ__repr____mutmut['_mutmut_orig'] = MacroExtensionsGate.xǁMacroExtensionsGateǁ__repr____mutmut_orig # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁ__repr____mutmut['xǁMacroExtensionsGateǁ__repr____mutmut_1'] = MacroExtensionsGate.xǁMacroExtensionsGateǁ__repr____mutmut_1 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁ__repr____mutmut['xǁMacroExtensionsGateǁ__repr____mutmut_2'] = MacroExtensionsGate.xǁMacroExtensionsGateǁ__repr____mutmut_2 # type: ignore # mutmut generated
mutants_xǁMacroExtensionsGateǁ__repr____mutmut['xǁMacroExtensionsGateǁ__repr____mutmut_3'] = MacroExtensionsGate.xǁMacroExtensionsGateǁ__repr____mutmut_3 # type: ignore # mutmut generated
