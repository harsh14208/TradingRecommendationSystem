"""
Fundamental quality gates extracted from generate_signal() and _assemble_signal().

Functional API (for gates inside generate_signal — they modify accumulated score):
    apply_short_interest_velocity(score, info)
        -> (new_score, cards, sources)  [§52]
    apply_insider_clustering(score, insider, is_lev_etf)
        -> (new_score, cards, sources)  [§73]
    apply_quality_screens(score, info, fundamentals, is_lev_etf, action)
        -> (new_score, cards, sources)  [§50 Piotroski, §74 Beneish, §76 Altman, §51 ForwardPE]

GateBase class (for gate inside _assemble_signal — can change action):
    FundamentalValueTrapGate
        Revenue declining >20% YoY AND FCF yield < −5% → HOLD.  Live only (not
        point-in-time); excluded from backtest_technicals.py to avoid look-ahead bias.

Public API summary:
    apply_short_interest_velocity(score, info) -> (score, cards, sources)
    apply_insider_clustering(score, insider, is_lev_etf) -> (score, cards, sources)
    apply_quality_screens(score, info, fundamentals, is_lev_etf, action) -> (score, cards, sources)
    FundamentalValueTrapGate — GateBase; use in a GatePipeline inside _assemble_signal
"""

from __future__ import annotations

from .base import GateBase, SignalContext


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_apply_short_interest_velocity__mutmut: MutantDict = {}  # type: ignore


# ── Functional API — used in generate_signal() ────────────────────────────────


@_mutmut_mutated(mutants_x_apply_short_interest_velocity__mutmut)
def apply_short_interest_velocity(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_orig(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_1(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = None
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_2(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = None

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_3(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = None
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_4(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get(None)
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_5(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("XXshares_shortXX")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_6(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("SHARES_SHORT")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_7(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = None
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_8(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get(None)
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_9(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("XXshares_short_priorXX")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_10(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("SHARES_SHORT_PRIOR")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_11(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None and si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_12(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None and si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_13(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is not None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_14(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is not None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_15(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior < 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_16(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 1:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_17(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = None
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_18(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) * si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_19(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur + si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_20(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 or score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_21(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel <= -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_22(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < +0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_23(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -1.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_24(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score > 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_25(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 1:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_26(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score = 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_27(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score -= 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_28(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_29(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add(None)
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_30(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("XXShort InterestXX")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_31(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("short interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_32(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("SHORT INTEREST")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_33(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            None
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_34(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "XXsrcXX": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_35(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "SRC": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_36(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "XXShort InterestXX",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_37(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "short interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_38(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "SHORT INTEREST",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_39(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "XXheadXX": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_40(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "HEAD": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_41(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel / 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_42(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 101:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_43(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "XXbodyXX": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_44(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "BODY": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_45(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) / 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_46(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(None) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_47(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 101:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_48(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(None):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_49(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(None):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_50(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "XXRapidly decreasing SI while oversold = shorts losing conviction and covering into XX"
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_51(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "rapidly decreasing si while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_52(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "RAPIDLY DECREASING SI WHILE OVERSOLD = SHORTS LOSING CONVICTION AND COVERING INTO "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_53(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "XXweakness. Their covering adds buy pressure on top of the fundamental reversal. XX"
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_54(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_55(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "WEAKNESS. THEIR COVERING ADDS BUY PRESSURE ON TOP OF THE FUNDAMENTAL REVERSAL. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_56(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "XXBoehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. XX"
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_57(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "boehmer et al. (2008): si velocity is predictive; covering + oversold = prime mr setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_58(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "BOEHMER ET AL. (2008): SI VELOCITY IS PREDICTIVE; COVERING + OVERSOLD = PRIME MR SETUP. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_59(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "XXConfidence +4pp (§52).XX"
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_60(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_61(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "CONFIDENCE +4PP (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_62(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "XXsentimentXX": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_63(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "SENTIMENT": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_64(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "XXposXX",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_65(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "POS",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_66(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "XXmetaXX": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_67(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "META": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_68(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(None):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_69(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(None):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_70(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 or score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_71(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel >= 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_72(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 1.2 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_73(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score > 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_74(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 1:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_75(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score = 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_76(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score += 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_77(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 6
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_78(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add(None)
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_79(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("XXShort InterestXX")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_80(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("short interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_81(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("SHORT INTEREST")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_82(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            None
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_83(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "XXsrcXX": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_84(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "SRC": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_85(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "XXShort InterestXX",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_86(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "short interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_87(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "SHORT INTEREST",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_88(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "XXheadXX": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_89(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "HEAD": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_90(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel / 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_91(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 101:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_92(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "XXbodyXX": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_93(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "BODY": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_94(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel / 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_95(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 101:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_96(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(None):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_97(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(None):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_98(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "XXRapidly increasing SI signals informed institutional conviction in the bearish XX"
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_99(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "rapidly increasing si signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_100(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "RAPIDLY INCREASING SI SIGNALS INFORMED INSTITUTIONAL CONVICTION IN THE BEARISH "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_101(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "XXthesis — short sellers are not panicking, they are adding. XX"
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_102(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "THESIS — SHORT SELLERS ARE NOT PANICKING, THEY ARE ADDING. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_103(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "XXThis fundamentally undermines the MR bounce thesis. Confidence −5pp (§52).XX"
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_104(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "this fundamentally undermines the mr bounce thesis. confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_105(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "THIS FUNDAMENTALLY UNDERMINES THE MR BOUNCE THESIS. CONFIDENCE −5PP (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_106(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "XXsentimentXX": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_107(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "SENTIMENT": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_108(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "XXnegXX",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_109(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "NEG",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_110(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "XXmetaXX": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_111(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "META": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_112(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(None):,} si_prior={int(si_prior):,} §52",
            }
        )

    return score, cards, new_sources


# ── Functional API — used in generate_signal() ────────────────────────────────


def x_apply_short_interest_velocity__mutmut_113(
    score: float,
    info: dict,
) -> tuple[float, list[dict], set[str]]:
    """
    §52 Short Interest Velocity — Squeeze Signal.

    Rapidly decreasing SI while oversold = shorts losing conviction + covering
    pressure amplifies the MR snap-back. Increasing SI = informed bear conviction.
    Data: yfinance sharesShort (current) vs sharesShortPriorMonth (prior).

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    si_cur = info.get("shares_short")
    si_prior = info.get("shares_short_prior")
    if si_cur is None or si_prior is None or si_prior <= 0:
        return score, cards, new_sources

    si_vel = (si_cur - si_prior) / si_prior
    if si_vel < -0.15 and score >= 0:
        score += 4
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Short Covering Acceleration ({si_vel * 100:+.0f}% MoM) — Squeeze Incoming (§52)",
                "body": (
                    f"Short interest fell {abs(si_vel) * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly decreasing SI while oversold = shorts losing conviction and covering into "
                    "weakness. Their covering adds buy pressure on top of the fundamental reversal. "
                    "Boehmer et al. (2008): SI velocity is predictive; covering + oversold = prime MR setup. "
                    "Confidence +4pp (§52)."
                ),
                "sentiment": "pos",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(si_prior):,} §52",
            }
        )
    elif si_vel > 0.20 and score >= 0:
        score -= 5
        new_sources.add("Short Interest")
        cards.append(
            {
                "src": "Short Interest",
                "head": f"Shorts Adding Aggressively (+{si_vel * 100:.0f}% MoM) — Bear Conviction (§52)",
                "body": (
                    f"Short interest rose {si_vel * 100:.0f}% vs prior reporting period "
                    f"({int(si_prior):,} → {int(si_cur):,} shares). "
                    "Rapidly increasing SI signals informed institutional conviction in the bearish "
                    "thesis — short sellers are not panicking, they are adding. "
                    "This fundamentally undermines the MR bounce thesis. Confidence −5pp (§52)."
                ),
                "sentiment": "neg",
                "meta": f"si_vel={si_vel:+.2f} si_cur={int(si_cur):,} si_prior={int(None):,} §52",
            }
        )

    return score, cards, new_sources

mutants_x_apply_short_interest_velocity__mutmut['_mutmut_orig'] = x_apply_short_interest_velocity__mutmut_orig # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_1'] = x_apply_short_interest_velocity__mutmut_1 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_2'] = x_apply_short_interest_velocity__mutmut_2 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_3'] = x_apply_short_interest_velocity__mutmut_3 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_4'] = x_apply_short_interest_velocity__mutmut_4 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_5'] = x_apply_short_interest_velocity__mutmut_5 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_6'] = x_apply_short_interest_velocity__mutmut_6 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_7'] = x_apply_short_interest_velocity__mutmut_7 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_8'] = x_apply_short_interest_velocity__mutmut_8 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_9'] = x_apply_short_interest_velocity__mutmut_9 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_10'] = x_apply_short_interest_velocity__mutmut_10 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_11'] = x_apply_short_interest_velocity__mutmut_11 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_12'] = x_apply_short_interest_velocity__mutmut_12 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_13'] = x_apply_short_interest_velocity__mutmut_13 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_14'] = x_apply_short_interest_velocity__mutmut_14 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_15'] = x_apply_short_interest_velocity__mutmut_15 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_16'] = x_apply_short_interest_velocity__mutmut_16 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_17'] = x_apply_short_interest_velocity__mutmut_17 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_18'] = x_apply_short_interest_velocity__mutmut_18 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_19'] = x_apply_short_interest_velocity__mutmut_19 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_20'] = x_apply_short_interest_velocity__mutmut_20 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_21'] = x_apply_short_interest_velocity__mutmut_21 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_22'] = x_apply_short_interest_velocity__mutmut_22 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_23'] = x_apply_short_interest_velocity__mutmut_23 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_24'] = x_apply_short_interest_velocity__mutmut_24 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_25'] = x_apply_short_interest_velocity__mutmut_25 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_26'] = x_apply_short_interest_velocity__mutmut_26 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_27'] = x_apply_short_interest_velocity__mutmut_27 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_28'] = x_apply_short_interest_velocity__mutmut_28 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_29'] = x_apply_short_interest_velocity__mutmut_29 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_30'] = x_apply_short_interest_velocity__mutmut_30 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_31'] = x_apply_short_interest_velocity__mutmut_31 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_32'] = x_apply_short_interest_velocity__mutmut_32 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_33'] = x_apply_short_interest_velocity__mutmut_33 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_34'] = x_apply_short_interest_velocity__mutmut_34 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_35'] = x_apply_short_interest_velocity__mutmut_35 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_36'] = x_apply_short_interest_velocity__mutmut_36 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_37'] = x_apply_short_interest_velocity__mutmut_37 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_38'] = x_apply_short_interest_velocity__mutmut_38 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_39'] = x_apply_short_interest_velocity__mutmut_39 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_40'] = x_apply_short_interest_velocity__mutmut_40 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_41'] = x_apply_short_interest_velocity__mutmut_41 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_42'] = x_apply_short_interest_velocity__mutmut_42 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_43'] = x_apply_short_interest_velocity__mutmut_43 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_44'] = x_apply_short_interest_velocity__mutmut_44 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_45'] = x_apply_short_interest_velocity__mutmut_45 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_46'] = x_apply_short_interest_velocity__mutmut_46 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_47'] = x_apply_short_interest_velocity__mutmut_47 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_48'] = x_apply_short_interest_velocity__mutmut_48 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_49'] = x_apply_short_interest_velocity__mutmut_49 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_50'] = x_apply_short_interest_velocity__mutmut_50 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_51'] = x_apply_short_interest_velocity__mutmut_51 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_52'] = x_apply_short_interest_velocity__mutmut_52 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_53'] = x_apply_short_interest_velocity__mutmut_53 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_54'] = x_apply_short_interest_velocity__mutmut_54 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_55'] = x_apply_short_interest_velocity__mutmut_55 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_56'] = x_apply_short_interest_velocity__mutmut_56 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_57'] = x_apply_short_interest_velocity__mutmut_57 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_58'] = x_apply_short_interest_velocity__mutmut_58 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_59'] = x_apply_short_interest_velocity__mutmut_59 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_60'] = x_apply_short_interest_velocity__mutmut_60 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_61'] = x_apply_short_interest_velocity__mutmut_61 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_62'] = x_apply_short_interest_velocity__mutmut_62 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_63'] = x_apply_short_interest_velocity__mutmut_63 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_64'] = x_apply_short_interest_velocity__mutmut_64 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_65'] = x_apply_short_interest_velocity__mutmut_65 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_66'] = x_apply_short_interest_velocity__mutmut_66 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_67'] = x_apply_short_interest_velocity__mutmut_67 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_68'] = x_apply_short_interest_velocity__mutmut_68 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_69'] = x_apply_short_interest_velocity__mutmut_69 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_70'] = x_apply_short_interest_velocity__mutmut_70 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_71'] = x_apply_short_interest_velocity__mutmut_71 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_72'] = x_apply_short_interest_velocity__mutmut_72 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_73'] = x_apply_short_interest_velocity__mutmut_73 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_74'] = x_apply_short_interest_velocity__mutmut_74 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_75'] = x_apply_short_interest_velocity__mutmut_75 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_76'] = x_apply_short_interest_velocity__mutmut_76 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_77'] = x_apply_short_interest_velocity__mutmut_77 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_78'] = x_apply_short_interest_velocity__mutmut_78 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_79'] = x_apply_short_interest_velocity__mutmut_79 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_80'] = x_apply_short_interest_velocity__mutmut_80 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_81'] = x_apply_short_interest_velocity__mutmut_81 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_82'] = x_apply_short_interest_velocity__mutmut_82 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_83'] = x_apply_short_interest_velocity__mutmut_83 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_84'] = x_apply_short_interest_velocity__mutmut_84 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_85'] = x_apply_short_interest_velocity__mutmut_85 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_86'] = x_apply_short_interest_velocity__mutmut_86 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_87'] = x_apply_short_interest_velocity__mutmut_87 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_88'] = x_apply_short_interest_velocity__mutmut_88 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_89'] = x_apply_short_interest_velocity__mutmut_89 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_90'] = x_apply_short_interest_velocity__mutmut_90 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_91'] = x_apply_short_interest_velocity__mutmut_91 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_92'] = x_apply_short_interest_velocity__mutmut_92 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_93'] = x_apply_short_interest_velocity__mutmut_93 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_94'] = x_apply_short_interest_velocity__mutmut_94 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_95'] = x_apply_short_interest_velocity__mutmut_95 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_96'] = x_apply_short_interest_velocity__mutmut_96 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_97'] = x_apply_short_interest_velocity__mutmut_97 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_98'] = x_apply_short_interest_velocity__mutmut_98 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_99'] = x_apply_short_interest_velocity__mutmut_99 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_100'] = x_apply_short_interest_velocity__mutmut_100 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_101'] = x_apply_short_interest_velocity__mutmut_101 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_102'] = x_apply_short_interest_velocity__mutmut_102 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_103'] = x_apply_short_interest_velocity__mutmut_103 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_104'] = x_apply_short_interest_velocity__mutmut_104 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_105'] = x_apply_short_interest_velocity__mutmut_105 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_106'] = x_apply_short_interest_velocity__mutmut_106 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_107'] = x_apply_short_interest_velocity__mutmut_107 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_108'] = x_apply_short_interest_velocity__mutmut_108 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_109'] = x_apply_short_interest_velocity__mutmut_109 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_110'] = x_apply_short_interest_velocity__mutmut_110 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_111'] = x_apply_short_interest_velocity__mutmut_111 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_112'] = x_apply_short_interest_velocity__mutmut_112 # type: ignore # mutmut generated
mutants_x_apply_short_interest_velocity__mutmut['x_apply_short_interest_velocity__mutmut_113'] = x_apply_short_interest_velocity__mutmut_113 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_apply_insider_clustering__mutmut)
def apply_insider_clustering(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_orig(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_1(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = None
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_2(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = None

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_3(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider and is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_4(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_5(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = None
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_6(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) and 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_7(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get(None, 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_8(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", None) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_9(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get(0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_10(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", ) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_11(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("XXunique_buyersXX", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_12(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("UNIQUE_BUYERS", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_13(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 1) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_14(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 1
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_15(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers > 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_16(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 3:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_17(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = None
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_18(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 9 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_19(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers > 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_20(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 4 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_21(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 6
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_22(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score = bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_23(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score -= bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_24(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add(None)
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_25(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("XXSEC EDGARXX")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_26(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("sec edgar")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_27(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            None
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_28(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "XXsrcXX": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_29(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "SRC": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_30(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "XXSEC EDGARXX",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_31(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "sec edgar",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_32(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "XXheadXX": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_33(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "HEAD": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_34(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "XXbodyXX": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_35(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "BODY": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_36(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "XXCluster buys (multiple independent decision-makers) carry far higher XX"
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_37(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_38(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "CLUSTER BUYS (MULTIPLE INDEPENDENT DECISION-MAKERS) CARRY FAR HIGHER "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_39(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "XXpredictive power than single-insider transactions. XX"
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_40(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "PREDICTIVE POWER THAN SINGLE-INSIDER TRANSACTIONS. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_41(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "XXAcademic research: insider cluster buys outperform by ~12% in the XX"
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_42(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_43(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "ACADEMIC RESEARCH: INSIDER CLUSTER BUYS OUTPERFORM BY ~12% IN THE "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_44(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "XX6 months following the filings.XX"
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_45(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 MONTHS FOLLOWING THE FILINGS."
                ),
                "sentiment": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_46(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "XXsentimentXX": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_47(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "SENTIMENT": "pos",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_48(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "XXposXX",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_49(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "POS",
                "meta": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_50(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "XXmetaXX": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources


def x_apply_insider_clustering__mutmut_51(
    score: float,
    insider: dict,
    is_lev_etf: bool,
) -> tuple[float, list[dict], set[str]]:
    """
    §73 Insider Clustering — multiple distinct insider buyers.

    Multiple distinct insiders buying simultaneously signals consensus on
    undervaluation — far stronger than a single large-holder transaction
    that may be routine compensation or scheduled diversification.

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    if not insider or is_lev_etf:
        return score, cards, new_sources

    unique_buyers = insider.get("unique_buyers", 0) or 0
    if unique_buyers >= 2:
        bonus = 8 if unique_buyers >= 3 else 5
        score += bonus
        new_sources.add("SEC EDGAR")
        cards.append(
            {
                "src": "SEC EDGAR",
                "head": f"Insider Cluster Buy — {unique_buyers} Distinct Insiders",
                "body": (
                    f"{unique_buyers} distinct insiders filed Form 4 purchases in the last 30 days. "
                    "Cluster buys (multiple independent decision-makers) carry far higher "
                    "predictive power than single-insider transactions. "
                    "Academic research: insider cluster buys outperform by ~12% in the "
                    "6 months following the filings."
                ),
                "sentiment": "pos",
                "META": f"unique_insider_buyers={unique_buyers} bonus=+{bonus}",
            }
        )

    return score, cards, new_sources

mutants_x_apply_insider_clustering__mutmut['_mutmut_orig'] = x_apply_insider_clustering__mutmut_orig # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_1'] = x_apply_insider_clustering__mutmut_1 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_2'] = x_apply_insider_clustering__mutmut_2 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_3'] = x_apply_insider_clustering__mutmut_3 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_4'] = x_apply_insider_clustering__mutmut_4 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_5'] = x_apply_insider_clustering__mutmut_5 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_6'] = x_apply_insider_clustering__mutmut_6 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_7'] = x_apply_insider_clustering__mutmut_7 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_8'] = x_apply_insider_clustering__mutmut_8 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_9'] = x_apply_insider_clustering__mutmut_9 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_10'] = x_apply_insider_clustering__mutmut_10 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_11'] = x_apply_insider_clustering__mutmut_11 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_12'] = x_apply_insider_clustering__mutmut_12 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_13'] = x_apply_insider_clustering__mutmut_13 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_14'] = x_apply_insider_clustering__mutmut_14 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_15'] = x_apply_insider_clustering__mutmut_15 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_16'] = x_apply_insider_clustering__mutmut_16 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_17'] = x_apply_insider_clustering__mutmut_17 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_18'] = x_apply_insider_clustering__mutmut_18 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_19'] = x_apply_insider_clustering__mutmut_19 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_20'] = x_apply_insider_clustering__mutmut_20 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_21'] = x_apply_insider_clustering__mutmut_21 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_22'] = x_apply_insider_clustering__mutmut_22 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_23'] = x_apply_insider_clustering__mutmut_23 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_24'] = x_apply_insider_clustering__mutmut_24 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_25'] = x_apply_insider_clustering__mutmut_25 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_26'] = x_apply_insider_clustering__mutmut_26 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_27'] = x_apply_insider_clustering__mutmut_27 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_28'] = x_apply_insider_clustering__mutmut_28 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_29'] = x_apply_insider_clustering__mutmut_29 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_30'] = x_apply_insider_clustering__mutmut_30 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_31'] = x_apply_insider_clustering__mutmut_31 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_32'] = x_apply_insider_clustering__mutmut_32 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_33'] = x_apply_insider_clustering__mutmut_33 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_34'] = x_apply_insider_clustering__mutmut_34 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_35'] = x_apply_insider_clustering__mutmut_35 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_36'] = x_apply_insider_clustering__mutmut_36 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_37'] = x_apply_insider_clustering__mutmut_37 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_38'] = x_apply_insider_clustering__mutmut_38 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_39'] = x_apply_insider_clustering__mutmut_39 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_40'] = x_apply_insider_clustering__mutmut_40 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_41'] = x_apply_insider_clustering__mutmut_41 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_42'] = x_apply_insider_clustering__mutmut_42 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_43'] = x_apply_insider_clustering__mutmut_43 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_44'] = x_apply_insider_clustering__mutmut_44 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_45'] = x_apply_insider_clustering__mutmut_45 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_46'] = x_apply_insider_clustering__mutmut_46 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_47'] = x_apply_insider_clustering__mutmut_47 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_48'] = x_apply_insider_clustering__mutmut_48 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_49'] = x_apply_insider_clustering__mutmut_49 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_50'] = x_apply_insider_clustering__mutmut_50 # type: ignore # mutmut generated
mutants_x_apply_insider_clustering__mutmut['x_apply_insider_clustering__mutmut_51'] = x_apply_insider_clustering__mutmut_51 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_apply_quality_screens__mutmut)
def apply_quality_screens(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_orig(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_1(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = None
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_2(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = None

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_3(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = None
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_4(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get(None)
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_5(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("XXpiotroski_fXX")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_6(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("PIOTROSKI_F")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_7(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_8(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add(None)
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_9(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("XXFundamentalsXX")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_10(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_11(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("FUNDAMENTALS")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_12(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score > 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_13(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 8:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_14(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score = 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_15(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_16(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 13
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_17(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                None
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_18(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "XXsrcXX": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_19(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "SRC": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_20(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "XXFundamentalsXX",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_21(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_22(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "FUNDAMENTALS",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_23(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "XXheadXX": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_24(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "HEAD": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_25(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "XXbodyXX": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_26(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "BODY": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_27(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "XXleverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks XX"
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_28(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. high-f-score stocks outperform low-f-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_29(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "LEVERAGE, AND EFFICIENCY TESTS. HIGH-F-SCORE STOCKS OUTPERFORM LOW-F-SCORE STOCKS "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_30(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "XXby 7–9% annually in academic studies.XX"
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_31(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "BY 7–9% ANNUALLY IN ACADEMIC STUDIES."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_32(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "XXsentimentXX": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_33(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "SENTIMENT": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_34(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "XXposXX",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_35(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "POS",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_36(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "XXmetaXX": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_37(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "META": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_38(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score > 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_39(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 6:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_40(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score = 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_41(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score -= 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_42(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 6
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_43(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score < 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_44(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 3:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_45(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score = 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_46(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score += 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_47(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 11
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_48(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                None
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_49(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "XXsrcXX": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_50(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "SRC": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_51(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "XXFundamentalsXX",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_52(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_53(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "FUNDAMENTALS",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_54(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "XXheadXX": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_55(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "HEAD": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_56(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "XXbodyXX": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_57(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "BODY": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_58(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "XXand deteriorating efficiency. Low-F-score stocks are academic short candidates.XX"
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_59(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. low-f-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_60(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "AND DETERIORATING EFFICIENCY. LOW-F-SCORE STOCKS ARE ACADEMIC SHORT CANDIDATES."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_61(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "XXsentimentXX": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_62(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "SENTIMENT": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_63(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "XXnegXX",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_64(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "NEG",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_65(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "XXmetaXX": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_66(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "META": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_67(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score < 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_68(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 5:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_69(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score = 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_70(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score += 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_71(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 5

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_72(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = None
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_73(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get(None)
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_74(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("XXbeneish_mXX")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_75(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("BENEISH_M")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_76(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None or not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_77(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_78(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_79(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add(None)
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_80(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("XXFundamentalsXX")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_81(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_82(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("FUNDAMENTALS")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_83(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m >= -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_84(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > +1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_85(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -2.7800000000000002:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_86(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score = 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_87(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_88(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 13
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_89(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                None
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_90(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "XXsrcXX": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_91(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "SRC": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_92(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "XXFundamentalsXX",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_93(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_94(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "FUNDAMENTALS",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_95(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "XXheadXX": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_96(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "HEAD": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_97(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "XXbodyXX": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_98(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "BODY": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_99(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "XXthreshold. The 8-variable model (Beneish 1999, 76% accuracy) flags XX"
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_100(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. the 8-variable model (beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_101(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "THRESHOLD. THE 8-VARIABLE MODEL (BENEISH 1999, 76% ACCURACY) FLAGS "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_102(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "XXabnormal accruals, receivables growth, and asset quality deterioration XX"
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_103(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "ABNORMAL ACCRUALS, RECEIVABLES GROWTH, AND ASSET QUALITY DETERIORATION "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_104(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "XXconsistent with earnings inflation. WorldCom, Enron, and many major XX"
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_105(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. worldcom, enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_106(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "CONSISTENT WITH EARNINGS INFLATION. WORLDCOM, ENRON, AND MANY MAJOR "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_107(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "XXfrauds were detected by this model pre-collapse. Reduce sizing significantly.XX"
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_108(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_109(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "FRAUDS WERE DETECTED BY THIS MODEL PRE-COLLAPSE. REDUCE SIZING SIGNIFICANTLY."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_110(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "XXsentimentXX": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_111(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "SENTIMENT": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_112(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "XXnegXX",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_113(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "NEG",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_114(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "XXmetaXX": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_115(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "META": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_116(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = None
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_117(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get(None)
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_118(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("XXforward_peXX")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_119(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("FORWARD_PE")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_120(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf or action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_121(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None or not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_122(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_123(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_124(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action != "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_125(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "XXBUYXX":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_126(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "buy":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_127(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add(None)
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_128(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("XXFundamentalsXX")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_129(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_130(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("FUNDAMENTALS")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_131(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe >= 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_132(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 31:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_133(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score = 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_134(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score += 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_135(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 6
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_136(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                None
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_137(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "XXsrcXX": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_138(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "SRC": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_139(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "XXFundamentalsXX",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_140(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_141(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "FUNDAMENTALS",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_142(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "XXheadXX": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_143(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "HEAD": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_144(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "XXbodyXX": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_145(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "BODY": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_146(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "XXAQR research shows high-PE oversold stocks frequently fail to revert: XX"
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_147(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "aqr research shows high-pe oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_148(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR RESEARCH SHOWS HIGH-PE OVERSOLD STOCKS FREQUENTLY FAIL TO REVERT: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_149(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "XXthe market is correctly pricing deteriorating fundamentals, not panic. XX"
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_150(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "THE MARKET IS CORRECTLY PRICING DETERIORATING FUNDAMENTALS, NOT PANIC. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_151(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "XXConfidence reduced −5pp (§51 value trap filter).XX"
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_152(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_153(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "CONFIDENCE REDUCED −5PP (§51 VALUE TRAP FILTER)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_154(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "XXsentimentXX": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_155(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "SENTIMENT": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_156(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "XXnegXX",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_157(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "NEG",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_158(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "XXmetaXX": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_159(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "META": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_160(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe <= 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_161(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 16:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_162(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score = 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_163(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score -= 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_164(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 4
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_165(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                None
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_166(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "XXsrcXX": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_167(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "SRC": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_168(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "XXFundamentalsXX",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_169(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_170(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "FUNDAMENTALS",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_171(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "XXheadXX": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_172(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "HEAD": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_173(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "XXbodyXX": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_174(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "BODY": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_175(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "XXthe stock is oversold AND priced cheaply relative to future earnings. XX"
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_176(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold and priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_177(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "THE STOCK IS OVERSOLD AND PRICED CHEAPLY RELATIVE TO FUTURE EARNINGS. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_178(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "XXLow-PE MR setups have historically stronger reversion: XX"
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_179(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "low-pe mr setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_180(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "LOW-PE MR SETUPS HAVE HISTORICALLY STRONGER REVERSION: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_181(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "XXvaluation provides fundamental support for the bounce. Confidence +3pp (§51).XX"
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_182(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_183(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "VALUATION PROVIDES FUNDAMENTAL SUPPORT FOR THE BOUNCE. CONFIDENCE +3PP (§51)."
                    ),
                    "sentiment": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_184(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "XXsentimentXX": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_185(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "SENTIMENT": "pos",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_186(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "XXposXX",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_187(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "POS",
                    "meta": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_188(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "XXmetaXX": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources


def x_apply_quality_screens__mutmut_189(
    score: float,
    info: dict,
    fundamentals: dict,
    is_lev_etf: bool,
    action: str,
) -> tuple[float, list[dict], set[str]]:
    """
    Composite fundamental quality screens applied at the end of the scoring pass.

    Includes:
      §50 Piotroski F-Score     — financial strength 0-9 scale
      §74 Beneish M-Score       — earnings manipulation screen
      §76 Altman Z-Score        — financial distress screen
      §51 Forward PE            — value trap vs genuinely cheap filter

    Args:
        score:        current composite score (to be adjusted)
        info:         ticker info dict (from yfinance)
        fundamentals: fundamentals dict (from get_fundamentals())
        is_lev_etf:   True for leveraged/inverse ETFs (screens skipped)
        action:       current preliminary action ("BUY"/"SELL"/"HOLD")

    Returns:
        (new_score, cards, sources)
    """
    cards: list[dict] = []
    new_sources: set[str] = set()

    # ── §50 Piotroski F-Score ─────────────────────────────────────────────────
    f_score = fundamentals.get("piotroski_f")
    if f_score is not None:
        new_sources.add("Fundamentals")
        if f_score >= 7:
            score += 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Strong",
                    "body": (
                        f"Piotroski F-Score of {f_score}/9: company scores strongly across profitability, "
                        "leverage, and efficiency tests. High-F-score stocks outperform low-F-score stocks "
                        "by 7–9% annually in academic studies."
                    ),
                    "sentiment": "pos",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score >= 5:
            score += 5
        elif f_score <= 2:
            score -= 10
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Piotroski F-Score {f_score}/9 — Financially Weak",
                    "body": (
                        f"Piotroski F-Score of only {f_score}/9: poor profitability, increasing leverage, "
                        "and deteriorating efficiency. Low-F-score stocks are academic short candidates."
                    ),
                    "sentiment": "neg",
                    "meta": f"F-Score: {f_score}/9",
                }
            )
        elif f_score <= 4:
            score -= 4

    # ── §74 Beneish M-Score — Earnings Manipulation Screen ───────────────────
    beneish_m = fundamentals.get("beneish_m")
    if beneish_m is not None and not is_lev_etf:
        new_sources.add("Fundamentals")
        if beneish_m > -1.78:
            score -= 12
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Beneish M-Score {beneish_m:.2f} — Possible Earnings Manipulation",
                    "body": (
                        f"Beneish M-Score of {beneish_m:.2f} exceeds the −1.78 manipulation "
                        "threshold. The 8-variable model (Beneish 1999, 76% accuracy) flags "
                        "abnormal accruals, receivables growth, and asset quality deterioration "
                        "consistent with earnings inflation. WorldCom, Enron, and many major "
                        "frauds were detected by this model pre-collapse. Reduce sizing significantly."
                    ),
                    "sentiment": "neg",
                    "meta": f"M-Score={beneish_m:.2f} (threshold=-1.78)",
                }
            )

    # §76 Altman Z-Score — REMOVED 2026-06-03
    # EDGAR validation showed 79/106 IS tickers (74%) are permanently below Z'<1.23
    # (the distress threshold) due to structural reasons unrelated to bankruptcy:
    #   - Financial companies (banks): high leverage is business model, not distress
    #   - Tech companies: high goodwill/intangibles deflate book equity
    #   - Service/retail: asset-light models have low fixed assets vs liabilities
    # The formula (Altman 1968) was calibrated on manufacturing companies.
    # Applying it to the IS universe was penalising ~80% of signals by -15 pts,
    # contributing to the IS/live WR gap. Removed after backtest_edgar.py confirmed
    # standalone Altman hurts IS Sharpe: N 230→79, Sh 0.20→0.16 (-0.03).

    # ── §51 Forward PE Value Trap Filter ─────────────────────────────────────
    fwd_pe = info.get("forward_pe")
    if fwd_pe is not None and not is_lev_etf and action == "BUY":
        new_sources.add("Fundamentals")
        if fwd_pe > 30:
            score -= 5
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Forward PE Value Trap Warning ({fwd_pe:.1f}×)",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× — expensive valuation on a declining stock. "
                        "AQR research shows high-PE oversold stocks frequently fail to revert: "
                        "the market is correctly pricing deteriorating fundamentals, not panic. "
                        "Confidence reduced −5pp (§51 value trap filter)."
                    ),
                    "sentiment": "neg",
                    "meta": f"forward_pe={fwd_pe:.1f} value_trap=high §51",
                }
            )
        elif fwd_pe < 15:
            score += 3
            cards.append(
                {
                    "src": "Fundamentals",
                    "head": f"Genuinely Cheap — Forward PE {fwd_pe:.1f}×",
                    "body": (
                        f"Forward PE of {fwd_pe:.1f}× confirms this is not a value trap: "
                        "the stock is oversold AND priced cheaply relative to future earnings. "
                        "Low-PE MR setups have historically stronger reversion: "
                        "valuation provides fundamental support for the bounce. Confidence +3pp (§51)."
                    ),
                    "sentiment": "pos",
                    "META": f"forward_pe={fwd_pe:.1f} value_confirmed §51",
                }
            )

    return score, cards, new_sources

mutants_x_apply_quality_screens__mutmut['_mutmut_orig'] = x_apply_quality_screens__mutmut_orig # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_1'] = x_apply_quality_screens__mutmut_1 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_2'] = x_apply_quality_screens__mutmut_2 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_3'] = x_apply_quality_screens__mutmut_3 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_4'] = x_apply_quality_screens__mutmut_4 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_5'] = x_apply_quality_screens__mutmut_5 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_6'] = x_apply_quality_screens__mutmut_6 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_7'] = x_apply_quality_screens__mutmut_7 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_8'] = x_apply_quality_screens__mutmut_8 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_9'] = x_apply_quality_screens__mutmut_9 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_10'] = x_apply_quality_screens__mutmut_10 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_11'] = x_apply_quality_screens__mutmut_11 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_12'] = x_apply_quality_screens__mutmut_12 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_13'] = x_apply_quality_screens__mutmut_13 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_14'] = x_apply_quality_screens__mutmut_14 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_15'] = x_apply_quality_screens__mutmut_15 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_16'] = x_apply_quality_screens__mutmut_16 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_17'] = x_apply_quality_screens__mutmut_17 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_18'] = x_apply_quality_screens__mutmut_18 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_19'] = x_apply_quality_screens__mutmut_19 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_20'] = x_apply_quality_screens__mutmut_20 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_21'] = x_apply_quality_screens__mutmut_21 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_22'] = x_apply_quality_screens__mutmut_22 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_23'] = x_apply_quality_screens__mutmut_23 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_24'] = x_apply_quality_screens__mutmut_24 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_25'] = x_apply_quality_screens__mutmut_25 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_26'] = x_apply_quality_screens__mutmut_26 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_27'] = x_apply_quality_screens__mutmut_27 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_28'] = x_apply_quality_screens__mutmut_28 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_29'] = x_apply_quality_screens__mutmut_29 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_30'] = x_apply_quality_screens__mutmut_30 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_31'] = x_apply_quality_screens__mutmut_31 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_32'] = x_apply_quality_screens__mutmut_32 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_33'] = x_apply_quality_screens__mutmut_33 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_34'] = x_apply_quality_screens__mutmut_34 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_35'] = x_apply_quality_screens__mutmut_35 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_36'] = x_apply_quality_screens__mutmut_36 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_37'] = x_apply_quality_screens__mutmut_37 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_38'] = x_apply_quality_screens__mutmut_38 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_39'] = x_apply_quality_screens__mutmut_39 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_40'] = x_apply_quality_screens__mutmut_40 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_41'] = x_apply_quality_screens__mutmut_41 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_42'] = x_apply_quality_screens__mutmut_42 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_43'] = x_apply_quality_screens__mutmut_43 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_44'] = x_apply_quality_screens__mutmut_44 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_45'] = x_apply_quality_screens__mutmut_45 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_46'] = x_apply_quality_screens__mutmut_46 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_47'] = x_apply_quality_screens__mutmut_47 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_48'] = x_apply_quality_screens__mutmut_48 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_49'] = x_apply_quality_screens__mutmut_49 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_50'] = x_apply_quality_screens__mutmut_50 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_51'] = x_apply_quality_screens__mutmut_51 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_52'] = x_apply_quality_screens__mutmut_52 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_53'] = x_apply_quality_screens__mutmut_53 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_54'] = x_apply_quality_screens__mutmut_54 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_55'] = x_apply_quality_screens__mutmut_55 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_56'] = x_apply_quality_screens__mutmut_56 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_57'] = x_apply_quality_screens__mutmut_57 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_58'] = x_apply_quality_screens__mutmut_58 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_59'] = x_apply_quality_screens__mutmut_59 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_60'] = x_apply_quality_screens__mutmut_60 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_61'] = x_apply_quality_screens__mutmut_61 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_62'] = x_apply_quality_screens__mutmut_62 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_63'] = x_apply_quality_screens__mutmut_63 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_64'] = x_apply_quality_screens__mutmut_64 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_65'] = x_apply_quality_screens__mutmut_65 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_66'] = x_apply_quality_screens__mutmut_66 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_67'] = x_apply_quality_screens__mutmut_67 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_68'] = x_apply_quality_screens__mutmut_68 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_69'] = x_apply_quality_screens__mutmut_69 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_70'] = x_apply_quality_screens__mutmut_70 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_71'] = x_apply_quality_screens__mutmut_71 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_72'] = x_apply_quality_screens__mutmut_72 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_73'] = x_apply_quality_screens__mutmut_73 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_74'] = x_apply_quality_screens__mutmut_74 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_75'] = x_apply_quality_screens__mutmut_75 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_76'] = x_apply_quality_screens__mutmut_76 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_77'] = x_apply_quality_screens__mutmut_77 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_78'] = x_apply_quality_screens__mutmut_78 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_79'] = x_apply_quality_screens__mutmut_79 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_80'] = x_apply_quality_screens__mutmut_80 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_81'] = x_apply_quality_screens__mutmut_81 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_82'] = x_apply_quality_screens__mutmut_82 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_83'] = x_apply_quality_screens__mutmut_83 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_84'] = x_apply_quality_screens__mutmut_84 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_85'] = x_apply_quality_screens__mutmut_85 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_86'] = x_apply_quality_screens__mutmut_86 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_87'] = x_apply_quality_screens__mutmut_87 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_88'] = x_apply_quality_screens__mutmut_88 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_89'] = x_apply_quality_screens__mutmut_89 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_90'] = x_apply_quality_screens__mutmut_90 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_91'] = x_apply_quality_screens__mutmut_91 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_92'] = x_apply_quality_screens__mutmut_92 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_93'] = x_apply_quality_screens__mutmut_93 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_94'] = x_apply_quality_screens__mutmut_94 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_95'] = x_apply_quality_screens__mutmut_95 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_96'] = x_apply_quality_screens__mutmut_96 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_97'] = x_apply_quality_screens__mutmut_97 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_98'] = x_apply_quality_screens__mutmut_98 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_99'] = x_apply_quality_screens__mutmut_99 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_100'] = x_apply_quality_screens__mutmut_100 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_101'] = x_apply_quality_screens__mutmut_101 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_102'] = x_apply_quality_screens__mutmut_102 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_103'] = x_apply_quality_screens__mutmut_103 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_104'] = x_apply_quality_screens__mutmut_104 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_105'] = x_apply_quality_screens__mutmut_105 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_106'] = x_apply_quality_screens__mutmut_106 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_107'] = x_apply_quality_screens__mutmut_107 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_108'] = x_apply_quality_screens__mutmut_108 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_109'] = x_apply_quality_screens__mutmut_109 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_110'] = x_apply_quality_screens__mutmut_110 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_111'] = x_apply_quality_screens__mutmut_111 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_112'] = x_apply_quality_screens__mutmut_112 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_113'] = x_apply_quality_screens__mutmut_113 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_114'] = x_apply_quality_screens__mutmut_114 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_115'] = x_apply_quality_screens__mutmut_115 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_116'] = x_apply_quality_screens__mutmut_116 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_117'] = x_apply_quality_screens__mutmut_117 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_118'] = x_apply_quality_screens__mutmut_118 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_119'] = x_apply_quality_screens__mutmut_119 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_120'] = x_apply_quality_screens__mutmut_120 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_121'] = x_apply_quality_screens__mutmut_121 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_122'] = x_apply_quality_screens__mutmut_122 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_123'] = x_apply_quality_screens__mutmut_123 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_124'] = x_apply_quality_screens__mutmut_124 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_125'] = x_apply_quality_screens__mutmut_125 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_126'] = x_apply_quality_screens__mutmut_126 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_127'] = x_apply_quality_screens__mutmut_127 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_128'] = x_apply_quality_screens__mutmut_128 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_129'] = x_apply_quality_screens__mutmut_129 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_130'] = x_apply_quality_screens__mutmut_130 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_131'] = x_apply_quality_screens__mutmut_131 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_132'] = x_apply_quality_screens__mutmut_132 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_133'] = x_apply_quality_screens__mutmut_133 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_134'] = x_apply_quality_screens__mutmut_134 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_135'] = x_apply_quality_screens__mutmut_135 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_136'] = x_apply_quality_screens__mutmut_136 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_137'] = x_apply_quality_screens__mutmut_137 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_138'] = x_apply_quality_screens__mutmut_138 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_139'] = x_apply_quality_screens__mutmut_139 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_140'] = x_apply_quality_screens__mutmut_140 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_141'] = x_apply_quality_screens__mutmut_141 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_142'] = x_apply_quality_screens__mutmut_142 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_143'] = x_apply_quality_screens__mutmut_143 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_144'] = x_apply_quality_screens__mutmut_144 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_145'] = x_apply_quality_screens__mutmut_145 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_146'] = x_apply_quality_screens__mutmut_146 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_147'] = x_apply_quality_screens__mutmut_147 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_148'] = x_apply_quality_screens__mutmut_148 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_149'] = x_apply_quality_screens__mutmut_149 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_150'] = x_apply_quality_screens__mutmut_150 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_151'] = x_apply_quality_screens__mutmut_151 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_152'] = x_apply_quality_screens__mutmut_152 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_153'] = x_apply_quality_screens__mutmut_153 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_154'] = x_apply_quality_screens__mutmut_154 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_155'] = x_apply_quality_screens__mutmut_155 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_156'] = x_apply_quality_screens__mutmut_156 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_157'] = x_apply_quality_screens__mutmut_157 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_158'] = x_apply_quality_screens__mutmut_158 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_159'] = x_apply_quality_screens__mutmut_159 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_160'] = x_apply_quality_screens__mutmut_160 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_161'] = x_apply_quality_screens__mutmut_161 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_162'] = x_apply_quality_screens__mutmut_162 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_163'] = x_apply_quality_screens__mutmut_163 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_164'] = x_apply_quality_screens__mutmut_164 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_165'] = x_apply_quality_screens__mutmut_165 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_166'] = x_apply_quality_screens__mutmut_166 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_167'] = x_apply_quality_screens__mutmut_167 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_168'] = x_apply_quality_screens__mutmut_168 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_169'] = x_apply_quality_screens__mutmut_169 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_170'] = x_apply_quality_screens__mutmut_170 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_171'] = x_apply_quality_screens__mutmut_171 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_172'] = x_apply_quality_screens__mutmut_172 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_173'] = x_apply_quality_screens__mutmut_173 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_174'] = x_apply_quality_screens__mutmut_174 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_175'] = x_apply_quality_screens__mutmut_175 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_176'] = x_apply_quality_screens__mutmut_176 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_177'] = x_apply_quality_screens__mutmut_177 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_178'] = x_apply_quality_screens__mutmut_178 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_179'] = x_apply_quality_screens__mutmut_179 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_180'] = x_apply_quality_screens__mutmut_180 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_181'] = x_apply_quality_screens__mutmut_181 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_182'] = x_apply_quality_screens__mutmut_182 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_183'] = x_apply_quality_screens__mutmut_183 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_184'] = x_apply_quality_screens__mutmut_184 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_185'] = x_apply_quality_screens__mutmut_185 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_186'] = x_apply_quality_screens__mutmut_186 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_187'] = x_apply_quality_screens__mutmut_187 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_188'] = x_apply_quality_screens__mutmut_188 # type: ignore # mutmut generated
mutants_x_apply_quality_screens__mutmut['x_apply_quality_screens__mutmut_189'] = x_apply_quality_screens__mutmut_189 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut: MutantDict = {}  # type: ignore


# ── GateBase class — used in _assemble_signal() ───────────────────────────────


class FundamentalValueTrapGate(GateBase):
    """
    Fundamental Value-Trap Gate — gates on fundamentally deteriorating companies.

    MR bounces on companies with BOTH revenue declining >20% YoY AND FCF yield
    below −5% are value traps — the stock is oversold for a real reason.

    DATA WARNING: revenue_growth and freeCashflow come from yfinance .info,
    which reflects current-day values, NOT point-in-time data. Applying this
    gate inside a historical backtest constitutes look-ahead bias. This gate
    is intentionally excluded from backtest_technicals.py for this reason.
    """

    @_mutmut_mutated(mutants_xǁFundamentalValueTrapGateǁapply__mutmut)
    def apply(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_orig(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_1(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" and ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_2(self, ctx: SignalContext) -> None:
        if ctx.action == "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_3(self, ctx: SignalContext) -> None:
        if ctx.action != "XXBUYXX" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_4(self, ctx: SignalContext) -> None:
        if ctx.action != "buy" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_5(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = None
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_6(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get(None)
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_7(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("XXrevenue_growthXX")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_8(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("REVENUE_GROWTH")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_9(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = None
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_10(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get(None)
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_11(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("XXfreeCashflowXX")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_12(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freecashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_13(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("FREECASHFLOW")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_14(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = None
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_15(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get(None)
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_16(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("XXmarket_capXX")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_17(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("MARKET_CAP")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_18(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_19(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap / 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_20(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs * mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_21(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 101) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_22(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap or mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_23(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None or mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_24(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_25(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap >= 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_26(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 1) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_27(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = None
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_28(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None or rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_29(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_30(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow <= -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_31(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < +0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_32(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -1.2
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_33(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = None
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_34(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None or fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_35(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_36(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield <= -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_37(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < +5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_38(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -6.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_39(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_40(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap or fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_41(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = None
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_42(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "XXHOLDXX"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_43(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "hold"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_44(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add(None)
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_45(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("XXRisk GateXX")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_46(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("risk gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_47(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("RISK GATE")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_48(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = None
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_49(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow / 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_50(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 101:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_51(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_52(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "XX—XX"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_53(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = None
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_54(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_55(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "XX—XX"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_56(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            None
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_57(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "XXsrcXX": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_58(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "SRC": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_59(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "XXRisk GateXX",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_60(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "risk gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_61(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "RISK GATE",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_62(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "XXheadXX": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_63(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "HEAD": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_64(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "XXbodyXX": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_65(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "BODY": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_66(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "XXStocks that are technically oversold AND fundamentally deteriorating XX"
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_67(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "stocks that are technically oversold and fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_68(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "STOCKS THAT ARE TECHNICALLY OVERSOLD AND FUNDAMENTALLY DETERIORATING "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_69(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "XXare value traps — the technical signal reflects a real business problem, XX"
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_70(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "ARE VALUE TRAPS — THE TECHNICAL SIGNAL REFLECTS A REAL BUSINESS PROBLEM, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_71(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "XXnot a temporary dip. MR bounces on cash-burning revenue-decliners XX"
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_72(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. mr bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_73(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "NOT A TEMPORARY DIP. MR BOUNCES ON CASH-BURNING REVENUE-DECLINERS "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_74(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "XXhave significantly lower win rates. Signal gated to HOLD.XX"
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_75(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. signal gated to hold."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_76(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "HAVE SIGNIFICANTLY LOWER WIN RATES. SIGNAL GATED TO HOLD."
                ),
                "sentiment": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_77(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "XXsentimentXX": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_78(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "SENTIMENT": "neg",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_79(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "XXnegXX",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_80(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "NEG",
                "meta": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_81(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "XXmetaXX": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

    def xǁFundamentalValueTrapGateǁapply__mutmut_82(self, ctx: SignalContext) -> None:
        if ctx.action != "BUY" or ctx.is_lev_etf:
            return
        rev_grow = ctx.info.get("revenue_growth")
        fcf_abs = ctx.info.get("freeCashflow")
        mktcap = ctx.info.get("market_cap")
        fcf_yield = (fcf_abs / mktcap * 100) if (fcf_abs is not None and mktcap and mktcap > 0) else None
        rev_trap = rev_grow is not None and rev_grow < -0.20
        fcf_trap = fcf_yield is not None and fcf_yield < -5.0
        if not (rev_trap and fcf_trap):
            return
        ctx.action = "HOLD"
        ctx.sources.add("Risk Gate")
        rev_str = f"{rev_grow * 100:+.1f}%" if rev_grow is not None else "—"
        fcf_str = f"{fcf_yield:+.1f}%" if fcf_yield is not None else "—"
        ctx.rationale.append(
            {
                "src": "Risk Gate",
                "head": f"Value-Trap Gate — Revenue {rev_str} YoY, FCF Yield {fcf_str}",
                "body": (
                    f"Revenue declining {rev_str} year-over-year with FCF yield of {fcf_str}. "
                    "Stocks that are technically oversold AND fundamentally deteriorating "
                    "are value traps — the technical signal reflects a real business problem, "
                    "not a temporary dip. MR bounces on cash-burning revenue-decliners "
                    "have significantly lower win rates. Signal gated to HOLD."
                ),
                "sentiment": "neg",
                "META": f"rev_growth={rev_str} | fcf_yield={fcf_str} | gate=value_trap",
            }
        )

mutants_xǁFundamentalValueTrapGateǁapply__mutmut['_mutmut_orig'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_orig # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_1'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_1 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_2'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_2 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_3'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_3 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_4'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_4 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_5'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_5 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_6'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_6 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_7'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_7 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_8'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_8 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_9'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_9 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_10'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_10 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_11'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_11 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_12'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_12 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_13'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_13 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_14'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_14 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_15'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_15 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_16'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_16 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_17'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_17 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_18'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_18 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_19'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_19 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_20'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_20 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_21'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_21 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_22'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_22 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_23'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_23 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_24'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_24 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_25'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_25 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_26'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_26 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_27'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_27 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_28'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_28 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_29'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_29 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_30'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_30 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_31'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_31 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_32'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_32 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_33'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_33 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_34'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_34 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_35'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_35 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_36'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_36 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_37'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_37 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_38'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_38 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_39'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_39 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_40'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_40 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_41'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_41 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_42'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_42 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_43'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_43 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_44'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_44 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_45'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_45 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_46'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_46 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_47'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_47 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_48'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_48 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_49'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_49 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_50'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_50 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_51'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_51 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_52'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_52 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_53'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_53 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_54'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_54 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_55'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_55 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_56'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_56 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_57'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_57 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_58'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_58 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_59'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_59 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_60'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_60 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_61'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_61 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_62'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_62 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_63'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_63 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_64'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_64 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_65'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_65 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_66'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_66 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_67'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_67 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_68'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_68 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_69'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_69 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_70'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_70 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_71'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_71 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_72'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_72 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_73'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_73 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_74'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_74 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_75'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_75 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_76'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_76 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_77'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_77 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_78'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_78 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_79'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_79 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_80'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_80 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_81'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_81 # type: ignore # mutmut generated
mutants_xǁFundamentalValueTrapGateǁapply__mutmut['xǁFundamentalValueTrapGateǁapply__mutmut_82'] = FundamentalValueTrapGate.xǁFundamentalValueTrapGateǁapply__mutmut_82 # type: ignore # mutmut generated
